# 작업 명세서 및 오토세이브 관리

이 문서는 모먼트 인사이트 개발 작업의 기준 문서입니다.
앞으로 새 기능을 만들거나 기존 기능을 수정할 때는 이 문서에 작업 의도, 실행 내역, 검증 결과를 남기고 개발 완료 시 체크합니다.

## 2026-08-10 Windows 우선·Mac 대기 공용 수집 조정

- 요청: 외부 명령은 Windows 작업용 데스크탑이 처리하고, Windows와 Mac이 중복 조회하지 않으며, Windows 미처리 항목만 Mac이 이어받도록 구성한다. 제한을 줄이기 위한 전용 프로필 쿠키 상시 삭제의 타당성도 판단한다.
- 원인: 기존 DB lease는 동일 tracker 중복 반영만 차단해 서로 다른 키워드의 두 기기 동시 조회는 가능했다. Git/Production 배포와 unpacked Chrome 확장 설치는 별도 경계다.
- 구현: service-role 전용 Supabase 공용 lane, Windows primary heartbeat, Mac standby handoff, 전 기기 제한 cooldown, 확장 대기 상태 표시를 추가했다. Windows launcher와 Mac wrapper가 명시적 역할 ID를 전송한다.
- 보호: 공용 lane 한 개, 회차당 키워드 한 개, oldest-first, 정확 상품·광고 제외·300개 원자 저장, 오류 시 마지막 정상값·30일 이력을 유지한다. 쿠키 자동 삭제·CAPTCHA 풀이·VPN·제한 우회는 금지한다.
- 검증: 운영 migration과 RLS/권한 확인, 대상 58/58, 앱·API 405/405, 플레이스·쇼핑 각 51/51, 서버 계약 39/39, Production 인증 18/18, 보호 잠금 22함수·65파일·16마이그레이션, 전체 `check:release`와 `git diff --check`를 통과했다.
- 남은 경계: Mac native runtime은 갱신됐으나 Chrome 보안 정책으로 확장 재로드는 원격 수행할 수 없다. Windows는 로그오프 상태이며 `%LOCALAPPDATA%` 설치본이 자동 갱신되지 않으므로, 로그인 후 1.0.18 설치기 재실행·양쪽 확장 재로드를 거쳐 primary heartbeat·standby 대기·신규 300개를 운영 확인한다.

## 2026-08-10 N 쇼핑 Windows 수동 갱신 무한로딩 복구

- 현상: Windows `동빈(개발)` 프로필에서 `지금 안전 갱신`을 누르면 확장 팝업이 계속 대기하지만 Production 작업기 요청·tracker claim은 발생하지 않는다.
- 원인 증거: Windows에서 `MomentInsightNaverShoppingHost`와 자식 `node.exe`는 남아 있었고, 같은 시각 Supabase는 처리 중 tracker 0건·worker 요청 10분간 0건·due tracker 58건이었다. C# launcher가 자식 Node의 native messaging 표준 입출력을 명시적으로 중계하지 않아 실기에서 연결이 멈췄다.
- 수정 범위: C# launcher에 binary stdin/stdout relay를 추가하고, 확장프로그램은 native host 첫 응답을 짧은 별도 시간으로 제한하며 수동 실행 요청을 즉시 접수 상태로 돌려 팝업이 전체 수집 시간 동안 잠기지 않게 한다.
- 보호: 정확 상품·광고 제외·300개 원자 반영·회차 1건·네이버 제한 즉시 중단·마지막 정상값 보존·DB 스키마·관리자/광고주 화면은 변경하지 않는다.
- 완료 조건: Windows·native host·확장 대상 검사, 보호 잠금과 전체 릴리스 검사, Windows 설치본 갱신, Production 요청/claim 또는 안전 오류의 실기 증거를 모두 확인한다.
- 구현·배포: 확장 1.0.16과 Windows stdio relay를 코드 `5049602`로 GitHub `main`에 반영했고 Production `/health`·`/ready`에서 같은 릴리스·서울 `icn1`·Supabase ready를 확인했다.
- Windows 설치: 실제 작업 프로필 `동빈 (개발)`의 내부 디렉터리 `Profile 3`에 1.0.16을 로드했다. DPAPI 운영 비밀키 해시는 설치 전후 동일하며, 정상 `.exe` 출력명으로 재컴파일한 launcher는 Windows 실기에서 `MI_EXE_TEST_RUNNING=True`였다.
- 운영 실증: `지금 안전 갱신`은 팝업을 오래 잠그지 않고 즉시 접수 상태로 돌아왔다. Supabase에 `2026-08-10 04:20:59 KST` 이후 신규 HMAC nonce가 기록됐고 수동 실행 뒤 활성 tracker 59건 중 1건이 lease를 획득해 실제 처리를 시작했다.
- 추가 원인·보완: 첫 실회차가 정확히 4분 뒤 `local_worker_collection_failed`로 안전 종료됐다. 11분 1차 보완은 4분 경계를 통과했지만 느린 실페이지 회차가 11분을 사용해 명시적 `native_host_response_timeout`으로 종료됐다. 45~75초 요청 간격은 유지하고 native/deadline 18분, lease 20분으로 정렬했다.
- 최종 내부 정지 보완: 18분·20분 정렬 실회차는 4분과 11분을 통과했으나 Chrome 내부 스크립트가 반환되지 않아 18분 안전 종료됐다. 확장 1.0.17은 `executeScript`를 45초로 제한하고 오류 탭을 정리한다. 코드 `9ed047c`는 Production과 Windows 설치 경로에 반영됐지만 Windows 사용자 로그오프로 새 Chrome 로드 실증은 로그인 후 이어간다.

## 2026-08-10 N 쇼핑 Windows 작업용 데스크탑 브리지

- 요청: Windows 데스크탑을 순위 수집 전용으로 사용하려는데 Chrome 확장 관리에 아무것도 나타나지 않는 원인을 해결하고 자동 실행 구조를 마련한다.
- 원인: 기존 연결기는 macOS Keychain·Chrome NativeMessagingHosts 폴더·LaunchAgent 전용이므로 Windows에는 확장·native host·watchdog가 설치되지 않았다.
- 구현: 표시 이름 `프로그램 개발` 정확 매칭, 사용자 전용 런타임 ACL, HKCU Native Messaging, 현재 사용자 DPAPI 비밀키, 바이너리 stdio 상속 C# launcher, 로그인 직후·10분 간격 Chrome 작업 스케줄러, 관리자 실행 진입 파일과 설치 문서를 추가했다.
- 보호: Production URL·회차 1건·고정 확장 ID만 허용하고 `remote-debugging`·`no-sandbox`·별도 user-data-dir·CAPTCHA 우회·평문 비밀키를 금지한다. 기존 20분 전체 순환·30/60/120분 재시도·원자 300개·실패 시 마지막 정상값 보존은 유지한다.
- 검증 상태: Windows 정적 계약 4/4, native host 12/12, API·서버 401/401, 플레이스·쇼핑 각 51/51, 서버 계약 39/39, Production 인증 18/18, 보호 잠금 22함수·64파일·15마이그레이션과 전체 릴리스 검사를 통과했다. GitHub 반영 후 Windows 실기 설치·신규 원자 300개를 별도 확인한다.

## 2026-08-08 개발·제품 이사 공동 책임 운영 원칙

- 역할: Codex의 프로젝트상 업무 역할을 `모먼트 인사이트 개발·제품 이사`로 명시합니다. 법적 임원·대표권·결제 권한이 아니라 개발, 제품 구조, UI/UX, 검증 품질을 대표님과 함께 책임지는 협업 역할입니다.
- 코워크 기준: 승인된 작업은 `현상 확인 -> 원인 확정 -> 최소 범위 구현 -> 대상 테스트 -> 역할·기능 2차 회귀검수 -> 문서화 -> 승인 시 배포 -> 운영 확인`을 종료 조건으로 삼습니다.
- 지속성: 작업이 대화나 외부 조건으로 끊겨도 Git 상태, `WORK_STATUS`, `NEXT_ACTIONS`, 본 작업명세를 근거로 이어서 진행하며 완료된 일을 중복하지 않습니다.
- 보고 기준: 실행 증거가 없는 완료·정상화·배포 보고를 금지합니다. 사용자 로그인, 보안확인, 결제, 외부 승인처럼 직접 수행할 수 없는 단계만 정확한 차단 원인과 다음 한 가지 행동으로 남깁니다.
- 승인 경계: 제품 방향, 결제·유료 자원, 운영 데이터 삭제, 외부 전송, 최종 배포는 대표님 승인 범위 안에서만 실행합니다.
- 보호 기준: 기존 미커밋 변경, 정상 운영 데이터, 광고주별 격리, 보호 잠금 기능을 보존하며 다른 작업과 섞어 임의로 되돌리거나 변경하지 않습니다.
- 검증: `npm run check:baseline` 전체 기준선 통과, `git diff --check` 통과, 세 운영 문서의 역할 문구 일치를 확인했습니다. 런타임·UI·API·DB는 변경하지 않았고 배포도 진행하지 않았습니다.

## 2026-08-08 N상품 Mac 처리량 확대와 전체 갱신 중복 방지

- 범위: 중앙 Mac 일반 Chrome의 10분 회차 상한을 2건에서 4건으로 올리되 순차 수집, 페이지 간 3.5~6초 간격, 보안확인·418·429 즉시 중단, 1시간 쿨다운은 유지합니다.
- 대기열: 계정별·사이트 전체 갱신은 미래 예약이면서 처리 중이 아닌 행만 현재 due로 전환합니다. 이미 due인 행은 시각을 다시 쓰지 않고 `alreadyQueued`로 집계해 반복 클릭이 중복 작업이나 우선순위 변경을 만들지 않게 합니다.
- 비변경: 화면, DB 스키마, 정확 상품·광고 제외·300개 원자 수집, 마지막 정상값과 30일 이력은 변경하지 않습니다.
- 완료 조건: 반복 요청 회귀검사, native host·worker 대상 검사, 보호 잠금 self-test와 전체 `check:release`를 통과한 뒤 별도 승인으로 Production과 중앙 Mac 설치본을 같은 릴리스로 반영합니다.
- 검증 상태: 대상 96/96, 전체 앱·API 389/389, 플레이스·쇼핑 각 51/51, 서버 계약 37/37, Production 인증 18/18, 보호 잠금 self-test와 전체 `npm run check:release`, `git diff --check`를 통과했으며 사용자 배포 승인을 받았습니다.
- 배포 상태: 코드 `1d7b773`을 GitHub `main`과 Production `dpl_H5Jtb4sZR3yNGV75PKAxZnwLgvYp`·운영 별칭에 반영했습니다. 운영 릴리스 `1d7b77338bfc`, 서울 `icn1`, Supabase ready와 중앙 Mac 설치 wrapper 해시 일치·`max_jobs=4` 설정·LaunchAgent exit 0을 확인했습니다.
- 남은 관찰: 설치 후 새 native-host 작업이 아직 없어 첫 자연 회차의 `max_jobs=4` 및 신규 `checked_count=300`은 미확인입니다. 보안확인은 자동 우회하지 않으며 실제 증거 전에는 실수집 완료로 보고하지 않습니다.

## 2026-08-08 N상품 보안확인 반복 차단 안정화

- 상태: 확장 1.0.7 추가 보완·전체 릴리스 검증·Production 배포 완료, 현재 `당뇨쌀` 보안확인 완료와 1.0.7 신규 300개 실수집 증거 대기.
- 원인 증거: 10:11 UTC의 `max_jobs=2` 실행에서 이미 보안확인이 발생했고, 11:19 UTC의 `max_jobs=4` 실행은 첫 작업에서 같은 보안확인을 다시 감지해 5초 안에 중단했습니다. 4건을 실제 연속 수집한 결과가 아니라 해결되지 않은 확인 탭을 한 시간 뒤 다시 요청한 구조가 반복 원인입니다.
- 조치 범위: 회차 상한을 검증된 2건으로 복귀하고, 자동·수동 실행 전에 기존 보안확인 탭을 먼저 검사합니다. 해결 전에는 서버 job을 claim하지 않으며 사용자가 확인을 완료해 정상 `__NEXT_DATA__`가 나타나면 다음 실행에서 상태를 자동 해제합니다.
- 추가 실증·조치: 1.0.6 자연 실행은 11:42:54 UTC `max_jobs=2`로 시작했지만 해결한 정상 탭을 닫고 `당뇨쌀` 새 탭을 생성할 때 다시 보안확인에 진입했습니다. 1.0.7은 해결된 탭을 닫지 않고 페이지 1~8과 같은 회차 최대 2개 작업에 재사용하며, 성공 요약 이후에만 탭을 닫습니다.
- 비변경: 대기열 멱등화, oldest-first, 조건부 lease, 광고 제외, 정확 상품 판정, 300개 원자 저장, 마지막 정상값·30일 이력과 관리자·광고주 화면은 변경하지 않습니다.
- 검증: 대상 native host·worker, 앱·API 389/389, 플레이스·쇼핑 각 51/51, 서버 계약 37/37, Production 인증 18/18, 보호 잠금 22함수·58파일·14마이그레이션과 전체 `check:release`, `git diff --check`를 통과했습니다.
- 설치: 중앙 Mac 브리지 설치본 wrapper와 저장소 SHA-256은 `58c062eca4c392f2696110bc3840417ef71237c728aa066b9f5ce49f819615fd`로 일치하며 기본 `max_jobs=2`, LaunchAgent 600초 실행을 확인했습니다. Chrome 확장은 보안상 자동 재로드하지 않습니다.
- 배포: 코드 `9f03a72`를 GitHub `main`과 Production `dpl_DuwFf2wiBejh1iR5MQmmNQsQjEdU`·`https://insight.momentlabs.co.kr`에 반영했습니다. 운영 `/health`·`/ready`는 릴리스 `9f03a726a673`, 서울 `icn1`, Supabase ready입니다. Vercel live gate는 기존 최근 원자 300개 증거를 통과했지만, 1.0.7 자체 신규 실수집 증거는 사용자 보안확인 이후에 별도 확인합니다.

## 2026-08-08 N상품 네트워크 제한 수동 해제·신규 키워드 우선 처리

- 상태: 구현·전체 검증·중앙 Mac 설치·Chrome 확장 1.0.8 재로드·Production 배포 완료, 네이버 네트워크 제한 해제 후 첫 신규 300개 실수집 확인 대기.
- 원인: 중앙 Chrome의 네이버 쇼핑 탭에서 `쇼핑 서비스 접속이 일시적으로 제한되었습니다`가 확인됐고 정상 `__NEXT_DATA__`가 없었습니다. 자동 회차가 짧은 간격으로 누적된 뒤 네이버가 동일 네트워크의 쇼핑 접속을 임시 제한한 상태입니다.
- 제한 회로: 네트워크 제한을 일반 보안확인과 분리해 영구 수동 해제 상태로 저장합니다. 제한 탭이 정상 검색 화면으로 돌아오고 담당자가 확장의 수동 재개 버튼을 누르기 전에는 09:00·15:00·10분 보정 회차가 서버 작업을 claim하거나 네이버 페이지를 새로 요청하지 않습니다.
- 대기열: `last_checked_at`이 없는 신규·미초기화 키워드 그룹을 기존 30일 due 대기열보다 먼저 한 번 처리하고, 초기 수집이 끝나면 기존 `next_check_at` 오름차순으로 복귀합니다. 같은 신규 그룹끼리는 먼저 등록된 순서를 유지합니다.
- 비변경: 정확 상품·원부 판정, 광고 제외, 300개 원자 저장, 계정 격리, 화면·기존 정상 순위·30일 이력, 전체 갱신·신규 등록 기능은 변경하지 않습니다.
- 완료 조건: 네트워크 제한 자동 재시도 차단, 수동 해제, 신규 우선·기존 순차 복귀 대상 테스트와 보호 잠금 self-test, 전체 `check:release`, 설치본 일치, Production 상태 확인을 모두 통과합니다. 네이버 제한이 실제 해제되기 전에는 신규 300개 실수집을 완료로 과장하지 않습니다.
- 검증: 대상 45/45, 전체 앱·API 392/392, 플레이스·쇼핑 수집기 각 51/51, Production 인증 18/18, 보호 잠금 22함수·58파일·14마이그레이션, self-test, 공개 빌드·CSP와 전체 `npm run check:release`, `git diff --check`를 통과했습니다.
- 설치 확인: 중앙 Mac 브리지 설치본과 저장소 worker가 동일하며 Chrome `동빈` 프로필에서 확장 1.0.8 활성화를 확인했습니다. 현재 네이버 쇼핑은 네트워크 임시 제한 화면이므로 추가 검색 요청이나 300개 수집은 실행하지 않았습니다.
- 배포: 코드 `3cb3557`을 GitHub `main`과 Production `dpl_AANxSpPzQXeD5XNSm3keASq7CQg4`·`https://insight.momentlabs.co.kr`에 반영했습니다. 운영 `/health`·`/ready`는 릴리스 `3cb355707396`, 서울 `icn1`, Supabase ready입니다. 빌드 live gate는 기존 최근 300개 원자 증거를 통과했지만 1.0.8 신규 실수집 증거를 뜻하지 않습니다.

## 운영 규칙

- 개발 착수 전: Git 상태, 최근 커밋, 현재 작업명세와 다음 작업을 확인하고 `npm run work:autosave`를 실행합니다.
- 중복 방지: 새 구현 전에 기존 코드, 문서, 테스트를 검색하고 이미 있는 기능은 확장하거나 재사용합니다.
- 충돌 방지: 기존 미커밋 변경과 `admin.html`/`client.html`의 공통 동작을 보존하고 범위가 겹치면 현재 diff를 먼저 확인합니다.
- 작업 항목을 추가하고 상태를 `진행 중`으로 둡니다.
- 개발 완료 후: 작업 항목을 `완료`로 체크하고 검증 결과를 적습니다.
- 대상 테스트와 다른 관점의 2차 검증, `git diff --check`, 최종 diff 확인을 모두 통과한 논리 단위만 로컬 커밋합니다.
- 완료·보류·배포 상태와 근거를 핵심 운영 문서에 동기화합니다.
- `git push`, Vercel 배포, Supabase 배포는 대표님이 명시적으로 지시할 때만 진행합니다.
- 배포 산출물(`dist`, 임시 캐시)은 필요 시 재생성하고 기본적으로 저장소에 남기지 않습니다.
- API 키가 들어가는 `.env.local` 파일은 삭제하거나 커밋하지 않습니다.

### 로그인 화면 검수 전달 규칙

- 로그인 권한이 필요한 관리자·운영팀·광고주 화면은 로그인할 수 없는 링크나 `다음에서 열기` 카드만 사용자 검수 자료로 전달하지 않습니다.
- 배포 전 화면 확인이 필요하면 실제 고객 데이터·계정 코드·비밀값을 사용하지 않는 로그인 없는 격리 목업 또는 목표 해상도의 직접 확인 가능한 화면 캡처를 함께 제공합니다.
- 총관리자·운영팀·광고주처럼 역할별 UI가 다르면 역할명을 명확히 표시한 별도 검수 화면 또는 비교 캡처를 제공합니다. 한 역할의 화면으로 다른 역할까지 확인됐다고 보고하지 않습니다.
- 검수용 목업은 로컬·임시 환경에서만 사용하고 Production 인증을 우회하거나 운영 인증정보를 노출하지 않습니다. 검수가 끝나면 임시 서버와 파일을 종료·삭제합니다.
- 실제 로그인 세션에서만 확인할 수 있는 기능은 사용자가 접근할 검수 수단이 없으면 `육안 검수 미완료`로 기록하며, 링크 접근 가능 여부를 확인하지 않은 상태에서 검수 완료나 배포 준비 완료로 보고하지 않습니다.

### 5대 핵심 조회·추적 기능 잠금

- 다른 기능 작업에서는 `scripts/protected-rank-features.lock.json`의 보호 함수·파일·마이그레이션을 수정하지 않습니다.
- 보호 범위는 `키워드 조회`, `SEO 확인`, `N 상품 순위`, `N 30일 순위`, `N 플레이스 30일 순위`의 운영팀·광고주 진입 함수, SEO 공통 점수 엔진과 핵심 서버 계약입니다.
- 이 잠금은 개발 소스 변경을 배포 전에 차단하는 품질 게이트이며, 운영 사용자의 신규 키워드 조회·SEO 점검·상품 순위 조회·상품/플레이스 추적 등록·갱신은 막지 않습니다. 보호 범위 밖의 새로운 별도 기능 개발도 계속 허용합니다.
- 보호 대상이 바뀌면 `npm run check:rank-feature-lock`이 품질·릴리스 단계에서 즉시 실패하므로 잠금을 제거하거나 검사를 우회하지 않습니다.
- 다섯 기능의 변경은 대표님의 명시적 승인, 변경 사유 기록, 대상 테스트, `npm run check:rank-feature-lock:self-test`, 전체 `npm run check:release` 통과 후에만 허용합니다.
- 모든 검증이 끝난 의도된 변경만 `node scripts/check-protected-rank-features.mjs --print-current` 결과와 대조해 해시 기준을 갱신합니다.

## 오토세이브 상태

<!-- autosave:start -->
- 마지막 자동 저장: 2026. 08. 10. 05:36:15
- 기준 커밋: b2bbf67
- 작업트리: clean
<!-- autosave:end -->

## 작업 상태 기준

### 2026-08-08 N상품 30일 순위 날짜별 단일 표시

- 상태: 완료·Production 배포 확인
- 변경: 관리자·광고주 화면에서 AM/PM 2칸 대신 해당 날짜의 최신 유효 순위 1개를 표시하고, 한 시점만 수집된 날은 해당 값을 사용합니다.
- 비범위: 수집 주기, 저장 데이터, 정확 상품 판정, 광고 제외, 300개 확인 규칙은 변경하지 않습니다.
- 검증: 날짜별 양쪽/한쪽/공란 조합 `10/10`, 보호 잠금 self-test와 전체 `npm run check:release` 통과.
- 배포: 코드 `01dd688`, GitHub `main`, 운영 릴리스 `01dd688b814b`. `/health`·`/ready` 200, 서울 `icn1`, Supabase ready, 운영 관리자·광고주 HTML과 검증 빌드 SHA-256 일치를 확인했습니다.

- `대기`: 아직 시작하지 않은 작업
- `진행 중`: 코드 또는 문서 작업 중
- `완료`: 개발과 검증이 끝나 로컬 커밋된 작업
- `보류`: 외부 키, 계정, 정책, 운영 판단이 필요한 작업
- `배포 대기`: 커밋은 완료됐지만 대표님 배포 지시 전 상태

## 진행 중 개발 내역

> 아래 완료 이력의 `공식 쇼핑 검색 API 순번` 표현은 2026-07-31 이전 당시 구현 기록이다. 현재 순위 원천은 2026-08-02 hybrid 로컬 워커와 `mi.naver-shopping-organic-window.v1` 계약이며 과거 API 순번을 신규 순위에 재사용하지 않는다.

| 상태 | 작업 | 핵심 내용 | 검증 | 배포 |
| --- | --- | --- | --- | --- |
| Production 반영·확장 재로드 대기 | 확장 수동 갱신 → 사이트 전체 N 30일 추적 대기열 연동 | 담당자가 확장프로그램의 `지금 안전 갱신`을 한 번 누르면 계정과 무관하게 운영 중인 모든 상품 순위 추적을 현재 대기열에 등록한다. 신규 등록 키워드도 같은 활성 추적 기준에 포함하며, 기존 10분 안전 회차·회차당 2건·보안확인 1시간 휴지·300개 원자 저장·실패 시 기존 정상값 보존 계약은 유지한다. 자동 09:00·15:00 실행은 전체 강제 재등록 없이 기존 due만 처리한다. | 대상 42/42, API·서버 389/389, 플레이스 51/51, 쇼핑 51/51, 계약 37/37, 인증 18/18, 역할 5상태, 보호 잠금 22함수·58파일·14마이그레이션과 전체 `check:release` 통과. 운영 DB 읽기 전용 확인 active 71·고유 키워드 58·기존 due 61·미도래 10·활성 lease 0 | 코드 `2924d82`, Production `dpl_9zLh7gMu554Uo1tHnF7BcUtmWid9`·운영 별칭 반영. `/health`·`/ready` 릴리스 `2924d82801e5`·서울 `icn1`·Supabase ready. 중앙 Mac 설치본 해시와 저장소 일치·LaunchAgent exit 0. Chrome 정책상 확장 1.0.5 재로드와 담당자 수동 버튼 실증만 남음 |
| Production 반영·보안확인 해제 대기 | N 30일 7월 31일 정체 전수 복구·보안확인 완화 | 운영 DB active 71건을 전수 점검해 29건·28개 키워드가 종료 legacy `naver_shopping_search_api` 최신값에 남고, 나머지 신규 300위 수집이 네이버 보안확인 이후 멈춘 원인을 확정. 사이트 전체 공정 처리를 위해 중앙 실행을 회차당 2건으로 제한하고 마지막 슬롯을 30일 tracker에 예약하며, 페이지 간 3.5~6초 분산·보안확인 1시간 휴지·10분 보정으로 과다 반복을 차단. 실패·부분 수집은 기존 정상 순위와 이력을 보존 | 워커·native host·단건 큐 45/45, 보호 잠금 self-test 22함수/58파일/14마이그레이션, API·서버 387/387, 플레이스 51/51, 쇼핑 51/51, 계약 37/37, 인증 18/18과 전체 `check:release` 통과. 중앙 Mac 설치본과 저장소 worker·wrapper SHA-256 일치. 확장 재로드 뒤 04:10:33 KST 운영 실행이 `max_jobs=2`로 시작했고 기존 네이버 보안확인을 감지해 5초 안에 안전 중단 | 코드 `06443c4`, Production `dpl_4MX12SCV1m6jqKB3EHchCYuWLPzu`·운영 별칭 반영. `/health`·`/ready` 릴리스 `06443c402610`·서울 `icn1`·Supabase ready. 열린 네이버 보안확인을 사용자 완료한 뒤 신규 `pw-chrome-*`·`checked_count=300` 실증과 멈춘 29건 감소를 계속 확인 |
| 완료·Production 반영 | N 30일 계정별 전체 갱신·중앙 자동 드레인 정상화 | 전체 갱신을 tracker별 장시간 호출이 아닌 현재 계정 범위의 단일 대기열 등록으로 전환하고, 중앙 Chrome 확장이 한 회차 한도 뒤에도 자동으로 다음 회차를 이어 처리하도록 보완. 일부 실패를 완료로 표시하지 않고 마지막 정상 순위·30일 이력과 실사용 신규 조회는 보존 | 계정 격리·양 역할·확장 후속 회차 대상 검사, API·서버 386/386, 플레이스 51/51, 쇼핑 51/51, 계약 37/37, 인증 18/18, 보호 잠금 22함수/58파일/14마이그레이션, 전체 `check:release`; 운영 health/ready 200·서울 icn1·양 역할 마커·비인증 401 | 코드 `b70848c`, Production `dpl_FPGs2Mt6kyimepmktqvNyBcWv4tL`·운영 별칭, 중앙 Mac 설치본 반영. Chrome 제어 연결 부재로 확장 1회 재로드와 운영 due 완주 실증은 후속 확인 |
| 완료·Production 반영 | 8월 API 운영 공지·개발중 표기·미배포 단건 큐 통합 배포 | 메인 서비스 안내를 네이버 API 환경 변경·개발중 3개·순위 정기 갱신 09:00/15:00 기준으로 정돈. 운영팀·광고주·모바일의 키워드 조회·SEO 확인·N 상품 순위에 `(개발중)` 표기. 직전 N 상품 단건 중앙 Mac 300위 대기열과 함께 안전 순서로 배포 | 1280×720·390×844 공지 육안검수, 가로 넘침 0, 양 역할·모바일 메뉴 parity·기준선·보호 잠금, API·서버 384/384, 플레이스 51/51, 쇼핑 51/51, 계약 37/37, 인증 18/18과 전체 `check:release`; 운영 health/ready 200·서울 icn1·인증 401 | 코드 `2c44c2e`, Production `dpl_2n8X63qVqaUzScvyUqurSNqgUWGs`·운영 별칭 반영. 중앙 Mac 설치본 재설치 완료 |
| 완료·Production 반영·실증 관찰 | N 상품 단건 중앙 Mac 300위 대기열 | `N 상품 순위` 단건 조회가 서버 상위 검증 범위 밖이면 별도 임시 작업 큐에 등록하고, 중앙 Mac이 정확히 300개를 원자 수집한 뒤 같은 계정 세션에만 결과를 반환. 30일 tracker·snapshot과 분리. 계정 원문 대신 단방향 범위 해시, 강제 RLS·service-role 전용 RPC·중복 방지·임대 잠금·최소 DML 권한 적용 | API·서버 384/384, 플레이스 51/51, 쇼핑 51/51, 서버 계약 37/37, 인증 18/18, 역할 5상태·양 역할 parity, 보호 잠금 22함수/58파일/14마이그레이션, 공개 빌드/CSP와 전체 `check:release` 통과. Production DB 실제 권한 4개 확인 | DB → Vercel → 중앙 Mac 설치본 순서 반영. Chrome 원격 연결 부재로 사용자 탭을 강제 재시작하지 않았으며 이번 버전 실제 `checkedCount=300`은 다음 자연 실행에서 확인 |
| 완료·Production 반영 | 사이트 갱신 → 중앙 Mac 300위 대기열 | `N 30일 순위` 단건/전체 갱신의 상위 즉시범위 miss를 현재 due로 되돌리고 hybrid `sync-due`가 서버 fallback으로 이를 소비하지 않게 분리. Mac/Chrome 준비 및 확장 catch-up은 10분, 정규 09:00·15:00 유지. 상단 즉시조회 일시 변형은 fail-closed하되 최근 중앙 원자 300개 증거가 있으면 사이트 대기열 배포를 허용. 실패·부분 수집은 마지막 정상값 보존 | 대상 63/63, API·서버 376/376, 플레이스 51/51, 쇼핑 51/51, 계약 37/37, 인증 18/18, 배포정책 3/3, 보호 잠금 22함수/55파일/12마이그레이션, plist·exit 0, 전체 `check:release`; 운영 health/ready 200·서울 icn1·권한 401 | 코드 `3061f7b`, Production `dpl_F7Csp8xqRwVDyJSF7E3UnqK92KWB`·운영 별칭 반영 |
| 완료·실증 통과·Production 반영 | N 쇼핑 hybrid 즉시 exact·중앙 Chrome 300위 브리지 | 종료 API와 Hub를 순위에서 분리. 서버는 반환된 광고 제외 SAS 상품의 공식 절대 순위 exact hit만 즉시 확정하고, 빠진 슬롯은 압축하거나 미노출로 단정하지 않는다. 중앙 `동빈` Chrome 확장은 공개 쇼핑 페이지의 `__NEXT_DATA__`만 읽어 정확히 300개를 macOS Native Messaging으로 전달한다. 네이티브 실행 파일은 Desktop 저장소가 아닌 사용자 보호 폴더에 독립 설치한다. 고객별 설치 없이 사이트 전체 due tracker를 처리하며 HMAC 비밀값은 키체인에만 둔다 | Chrome 수동 갱신 3회 연속 `status=0`, DB `collection_id=pw-chrome-*`·`checked_count=300`·exact 16위 원자 snapshot 확인. 네이버 현재 `pageSize=49` 응답은 실제 슬롯 수 이상·최대 50과 절대 순위 증가를 검증해 광고 제외 SAS 44개·최고 exact 45위·미발견 연속 범위 9위로 처리한다. live gate `SHOPPING_RANK_HYBRID_LIVE_READY`, API·서버 364/364, 플레이스 51/51, 쇼핑 49/49, 계약 37/37, 인증 18/18, 보호 잠금 22함수/53파일/12마이그레이션과 전체 `check:release` 통과 | 코드 `98254b3`, Production `momentinsight-5vefl2vyq-momentlabs.vercel.app`·운영 별칭 반영. `/health`·`/ready` 릴리스 `98254b32c54d`·서울 `icn1`, 무서명 401 및 키체인 서명 통과 후 무해한 잘못된 action 400으로 HMAC 경계 확인 |
| 과거 전환 이력·현재 구조로 대체 | N 쇼핑 원격 원자 수집 아키텍처 실험 | 종료 API 페이지를 이어 붙이지 않고 독립 Playwright 서비스, 교체 가능한 provider, 단일 `collectionId`의 오가닉 1~300 원자 계약을 구축. 광고·중복 ID·부분/혼합 응답을 거부하고 키워드·단건·30일 추적을 같은 계약으로 통합. 당시 Render의 `/health`와 `/ready`를 분리하고 공개 headless 수집 가능성을 검토했으나 현재는 전용 Mac hybrid 구조로 대체 | 당시 로컬 집중 검사만 통과했고 공개 Chromium은 HTTP 418로 실수집에 실패. 현재 실수집 근거로 사용하지 않음 | 배포되지 않은 과거 실험 이력. 현재 배포 판단에 사용하지 않음 |
| 과거 2026-07-31 전환 이력 | NAVER API Hub 전수 전환 감사·종료 legacy 경로 제거 | 지원 Search·Search Trend·Shopping Insight는 Hub 고정, Search Ads는 별도 공식 계약 유지. 키워드 조회와 N 상품 순위의 종료 쇼핑 검색 직접 호출·인증·fallback을 제거하고 동일 검증 수집원 계약으로 통합했다. DB tracker·30일 snapshot·감사 이력은 삭제하지 않았다 | 종료 URL 런타임 0건, API·서버 252/252, 플레이스 51/51, 계약 29/29, 인증 18/18, 역할·양 화면 parity, 잠금 21함수/23파일/11마이그레이션, 공개 빌드·CSP·전체 `check:release` 통과. Hub 실호출 blog 1건·trend 31건·age 12건 HTTP 200 | 당시 수집원 부재로 실상품 300위·25/25·전체 71개·cron 2회 미완료. 현재 판단은 위 hybrid 항목을 따름 |
| 과거 2026-07-31 보호 이력 | N 상품 30일 전체 갱신 장애 재진단·실수집 복구 | 운영 `성공 0개·재시도 25개`와 신규 snapshot 0건을 교차 확인. 종료된 legacy 쇼핑 검색 API를 준비 완료로 잘못 판단한 문제, 영구 오류 전건 2차 재시도, cron 502가 공통 보안층에서 500으로 변환되는 문제를 분리 수정했다. 실제 오가닉 300 수집원 미연결 시 단건·전체·cron은 DB claim·순위 변경·snapshot 생성 없이 중단하고 마지막 정상값을 유지한다 | API·서버 251/251, 플레이스 51/51, 서버 계약 29/29, Production 인증 18/18, 역할 5상태·양 역할 parity·보호 잠금 21함수/23파일/11마이그레이션·공개 빌드/CSP·전체 `check:release` 통과. 당시 실상품 canary·`mml93-a01` 25/25·사이트 전체 71개·cron 2회는 미완료 | 당시 Production 수집원 URL/key 게이트 이력. 현재 판단은 위 hybrid 항목을 따름 |
| 완료·배포 | 2026-07-31 NAVER API Hub 운영 고정·오류 격리 | 등록된 공식 Hub 키를 Production `hub` 모드로 고정하고 Search·Search Trend·Shopping Insight의 새 인증 헤더와 경로를 유지. 키 쌍 누락·`auto/legacy` 오설정은 배포 전 실패시키고, Hub에 없는 `shop` 등 종료·미지원 검색 리소스는 네트워크 호출 전에 차단한다. 네트워크·408·425·429·5xx만 1회 제한 재시도하고 인증·권한·잘못된 요청은 즉시 실패하며 기존 부분 응답·30분 캐시·동시 2개 배치 구조를 유지 | 환경값·hub 고정 모드 ready. 배포 후 실키 `search_blog` 200/154ms, `search_trend` 200/139ms, `shopping_insight_age` 200/53ms. 신규 Hub 단위 9/9, 키워드·상품·플레이스 대상 105/105, 본체 242/242·플레이스 수집기 51/51·서버 계약 29/29·Production 인증 18/18·보호 잠금 21함수/23파일/11마이그레이션과 로컬·Vercel 전체 `check:release` 통과 | 코드 `6d70f56` GitHub `main`·Production `dpl_99XvbXnx3qnk94Xs9R5P4NPrEtLe` 운영 반영. 릴리스 `6d70f56364a7`·서울 `icn1`·health/ready 200, 관리자·광고주 검증 빌드 일치·보호 API 401 확인 |
| 완료·보호 배포 | 2026-07-31 쇼핑 검색 API 종료 대응·서버 자동 추적 보호 | 운영 DB 활성 상품 추적 71건·고유 키워드 58개를 확인하고 17:05 KST부터 35건에 `Invalid search api`가 발생한 원인을 기존 `/v1/search/shop.json` 종료로 확정. 새 API Hub 키는 지원 API에 정상 적용되지만 `/search/v1/shop`은 공식 미지원이다. 종료 오류가 나도 마지막 정상 순위·30일 이력을 보존하고 다음 정규 슬롯까지 대기하며, 실패 항목이 있어도 전체 큐를 완주한다. 검증된 수집원용 계약은 비활성 준비만 유지하고 승인되지 않은 외부 서비스·결제·환경값은 연결하지 않는다 | 대상 35/35, 본체 242/242, 플레이스 51/51, 서버 계약 29/29, Production 인증 18/18, 역할 5상태·양 역할 parity·보호 잠금 21함수·23파일·11마이그레이션·전체 `check:release` 통과. 운영 서버·Supabase 정상, 마지막 정상 snapshot 보존 확인 | 코드 `b6466a3`을 위 Production 릴리스에 함께 반영. 맥과 무관한 GitHub Actions·Vercel 09:00/15:00·매시 catch-up 구조 유지. 공식 대체 순위 API가 없어 신규 N 상품 순위는 검증 수집원 승인 전 생성하지 않음 |
| 완료·배포 | 업무 완료 체크 양방향 해제 | 완료된 업무 버튼이 비활성화되고 처리 함수가 조기 종료해 해제할 수 없는 결함 수정. 같은 체크 버튼으로 `완료 ↔ 예정`을 전환하되 일정·유형·우선순위·내부 메모·광고주 공개값은 보존. 완료 버튼에 `aria-pressed`와 완료 처리/해제 안내를 적용 | 완료·해제 보존 단위검사 8/8, API·서버 237/237, 플레이스 51/51, 서버 계약 29/29, Production 인증 18/18, 역할 5상태, 양 역할 parity, 보호 잠금 21함수·23파일·11마이그레이션, 공개 빌드 9파일·인라인 6개·CSP 해시 4개와 전체 `check:release` 통과 | 코드 `ba5fc23` GitHub `main`·운영 릴리스 `ba5fc233c111` 반영. `/health` live·`/ready` Supabase ready·서울 `icn1`·누락 0건, 운영 HTML의 양방향 함수·완료 해제·접근성 마커와 기존 disabled 제거 확인 |
| 완료·배포 | 업무 운영 실행 중심 압축·즉시 완료 | 기존 3개 요약을 `오늘 업무·지연 업무·확인 필요` 실행 필터로 교체하고 같은 버튼을 다시 누르거나 `전체`로 해제. 가까운 업무 카드에 기존 일정·유형·메모·공개값을 보존한 1클릭 완료를 추가. 등록창은 `제목·시작·상태·공개`를 먼저 보여주고 유형·종료·담당자·우선순위·내부 메모는 `상세 설정`에 접음. 모바일 요약은 3열 압축. 서버·DB·광고주 공개 계약·`client.html`·보호된 조회/추적 기능은 변경하지 않음 | 새 기준선 `workOperationExecutionSummaryAndQuickComplete`, 즉시 완료 보존 단위검사 포함 API·서버 236/236, 플레이스 51/51, 서버 계약 29/29, Production 인증 18/18, 역할 5상태, 양 역할 parity, 보호 잠금 21함수·23파일·11마이그레이션, 공개 빌드 9파일·인라인 6개·CSP 해시 4개와 전체 `check:release` 통과. 앱 브라우저의 localhost 차단으로 변경 후 실화면 육안 검수는 대표님 확인 항목으로 유지 | 코드 `2aeb67d` GitHub `main`·운영 릴리스 `2aeb67d3c563` 반영. `/health` live·`/ready` Supabase ready·서울 `icn1`·누락 0건, 운영 HTML의 세 필터·빠른 완료·상세 설정 마커 확인 |
| 완료·배포 | 업무 운영 화면 격리·광고주 코드 열거 차단 | `업무 운영`의 상시 `display:grid`가 화면 숨김 규칙을 덮던 결함을 활성 화면 전용 grid와 비활성 화면 강제 숨김 계약으로 수정. 총관리자 업무 범위 입력의 전체 코드 `datalist`를 제거하고 직접 입력만 허용하며 다른 메뉴 이동 시 입력값·업무 범위를 폐기한다. 업무 API는 입력한 단일 코드만 서버에서 활성 여부를 확인하고 코드 목록을 응답하지 않는 기존 경계를 유지. 5대 조회·추적 기능과 `client.html`·DB는 변경하지 않음 | 브라우저 `대행사 연결`·`키워드 조회`·`업무 운영` 각각 단일 화면만 노출, 코드 목록·list 연결 0건. 전용 기준선, 역할 5상태, 보호 잠금 21함수/23파일/11마이그레이션, 서버 계약 29/29, API·서버 234/234, 플레이스 51/51, 인증 18/18, 공개 빌드·CSP와 로컬·Vercel 전체 `check:release` 통과 | 기능 코드 `0e8a4b0`·배포 증거 `f9be8ca`·Production `dpl_9qB4m8GJrsEfUPSKWRt6PVLkxcaY` 운영 반영. 릴리스 `f9be8cadf997`·서울 `icn1`·Supabase ready, 운영 메뉴 단독 노출·코드 목록 0건 재확인 |

## 완료된 개발 내역

| 상태 | 작업 | 핵심 내용 | 검증 | 배포 |
| --- | --- | --- | --- | --- |
| 완료·배포 대기 | 전체 제품 E2E 고도화 1단계·운영팀 코드 수동 입력 고정 | Production 총관리자 화면을 읽기 전용 점검해 `대행사 연결`의 운영팀 코드가 기존 규칙값으로 자동 채워지는 보안·사용성 결함을 확인. UI 자동 제안과 서버의 빈 요청 자동 생성을 함께 제거하고, 코드 미입력 시 400으로 거부하도록 고정. 기존 운영팀·광고주 계정, 연결, 권한, DB, `client.html`, 5대 보호 기능은 변경하지 않음 | 총관리자 운영 홈·광고주 미리보기·대행사 연결·운영 입력·보고서·공개 관리 Production 육안 점검. 대상 10/10, 보호 잠금 21함수/23파일/11마이그레이션, 역할 5상태, 양 역할 parity, 서버 계약 28/28, API·서버 227/227, 플레이스 51/51, 공개 빌드·CSP와 전체 `check:quality` 통과. 앱의 localhost 차단과 Chrome 뷰포트 미적용 때문에 변경 후 실계정 모바일 육안 증명은 미완료이며 자동 반응형 계약만 통과 | 로컬 커밋만 보존. push·Production 배포 없음. 배포 전 Preview 또는 실계정에서 입력칸 공백·빈 요청 400을 확인하고 대표님 승인 필요 |
| 완료·배포 | N 상품 30일 순위 이미지 숫자 굵기 원복 | 직전 export 전용 600 굵기 규칙과 해당 기준선만 제거해 `N위` 숫자를 이전 굵기로 복원. 날짜·15일×2단 배치·실제 화면·순위 계산·수집·저장 로직은 유지 | 관리자·광고주·기준선 3파일이 이전 배포 기준 `4a65c18`과 완전 일치. 역할 parity·5상태, 보호 잠금 21함수/23파일/11마이그레이션, API 226/226·플레이스 51/51·계약 28/28·인증 18/18·로컬·Vercel 전체 `check:release` 통과 | 코드 `852581e`·Production `dpl_CecSdRr18gJ9vFLqVNrHHxKRjx9e` 운영 반영. 릴리스 `852581e549d1`·서울 `icn1`·Supabase ready, 양 역할 800·600 제거·15칸 2단 확인 |
| 완료·배포 | N 상품 30일 순위 이미지 숫자 굵기 경량화 | 운영팀·광고주 다운로드 이미지의 일별 `N위` 숫자만 800에서 600으로 낮춤. 날짜·제목의 계층과 15일×2단 배치, 실제 화면, 순위 계산·수집·저장·AM/PM·대표값 로직은 유지 | 격리 브라우저 60개 순위값 600·날짜 800·2단·가로 넘침 0. 새 기준선, 역할 3종 parity·5상태, 보호 잠금 21함수/23파일/11마이그레이션, API 226/226·플레이스 51/51·계약 28/28·인증 18/18·로컬·Vercel 전체 `check:release` 통과 | 코드 `6b9e2b7`·Production `dpl_HNz8t2XLwcJ8nmmSxPKf5TTFdgTL` 운영 반영. 릴리스 `a1766afbfa48`·서울 `icn1`·Supabase ready, 양 역할 HTML 200·새 굵기 마커 확인 |
| 완료·배포 | N 상품 30일 순위 이미지 15일×2단 배치 | 운영팀·광고주 다운로드 이미지에만 일별 기록을 15칸씩 두 줄로 배치. 실제 화면의 가로 스크롤, 순위 계산·수집·저장·AM/PM·대표값 로직은 유지 | 격리 브라우저 30칸 `15+15`, 동일 84px, 가로 넘침 0. 새 기준선, 역할 3종 parity·5상태, 잠금 self-test·21함수/23파일/11마이그레이션, API 226/226·플레이스 51/51·계약 28/28·인증 18/18·로컬·Vercel 전체 `check:release` 통과 | 코드 `655473a`·Production `dpl_6XecRP7zgUWFcE7TvkwxFqKH1i4i`·운영 별칭 반영. 릴리스 `655473a0e06d`·서울 `icn1`·Supabase ready, 양 역할 HTML 200·로컬 빌드 SHA-256 일치 |
| 완료 | 인증 화면 검수 자료 전달 규칙 고정 | 로그인 필수 화면은 접근 불가능한 링크만 전달하지 않고, 비실데이터 격리 목업 또는 직접 확인 가능한 캡처를 역할별로 제공. Production 인증 우회·실계정 노출 금지, 접근 수단이 없으면 육안 검수 미완료로 명시 | 작업명세 운영 규칙·현재 상태·다음 작업 문서 동기화, 기능·화면·DB·서버·배포 파일 변경 없음, `git diff --check` 확인 | 배포 없음 |
| 완료·배포 | 총관리자 전용 대행사 연결 UI·UX 2차 압축·버튼 행 정렬 | `mml93-a01` Owner 모드의 좌우 분할과 큰 세로 카드를 전체 폭 단일 흐름으로 교체하고, 위로 뜨던 운영팀·광고주 CTA를 각각 `team-action`·`client-action` 영역에 고정해 입력칸과 같은 작성 행으로 정렬. 운영팀은 기존 단일 광고주 생성 구조·문구·placeholder를 유지하고 Owner 전용 요소를 숨김. `client.html`·DB·서버·생성 API·5대 보호 기능 변경 없음 | 1280×720 운영팀 입력·버튼 `y=542`, 광고주 입력·버튼 `y=654`, 전부 40px, overflow 0. Owner 전용 요소 10/0 분리, 역할 5상태·양 역할 parity·잠금 21함수·23파일·11마이그레이션·API/서버 226/226·플레이스 51/51·계약 28/28·인증 18/18·로컬·Vercel 전체 `check:release` 통과 | 코드 `1638124`·Production `dpl_HtCvfVaBtyWRzBiRkUrGowayuJXp`·운영 별칭 반영. `/health`·`/ready` 릴리스 `1638124c7eeb`·서울 `icn1`·Supabase ready, `/admin`·`/client` 200·관리자 grid-area 마커 확인 |
| 완료·배포 | 기존 5자리 광고주 로그인 호환 | 광고주 로그인만 5자리 후보를 활성 DB 정확 일치 검사까지 허용. 운영팀·총관리자 로그인과 신규 광고주·운영팀 생성은 6자리 이상 유지. 고객 코드 하드코딩·DB·화면·보호 기능 변경 없음 | 대상 22/22, API·서버 226/226, 플레이스 51/51, 역할 5상태·양 역할 parity·잠금 21함수·23파일·11마이그레이션·계약 28/28·인증 18/18·로컬·Vercel 전체 `check:release` 통과. 운영 DB 활성 5자리 2개·4자리 이하 0개. 두 계정 로그인·복원 200, 원문 코드 비노출, 4자리·5자리 운영팀·DB 미등록 5자리 400/401 차단 | 코드 `a440415` GitHub `main`·Production `dpl_3pyFqZVYjBHY84vmQH9eKLZEgthH` 반영. 운영 릴리스 `a440415e94de`·서울 `icn1`·Supabase ready, 양 역할 200·비인증 401 |
| 완료·배포 | 공개 전 확인 단일 압축 체크리스트 | 세 개 큰 개별 카드를 하나의 흰 패널 안 3개 목록 행으로 통합하고 데스크톱 강제 균등 높이 확장을 제거. 641~900px는 같은 패널 안 3열, 520px 이하는 3행으로 전환. 기존 태그·문구와 검수 데이터 유지 | 1280px 외부 카드 357px·내부 목록 200px·3행 각 66px·가로 넘침 0, 입력 검수 5/6·CSV `3,280만원·456%·1,080건`·브라우저 오류 0, 역할 5상태·양 역할 parity·잠금 21함수·23파일·11마이그레이션·서버 224/224·플레이스 51/51·계약 28/28·인증 18/18·로컬 및 Vercel 전체 `check:release` 통과 | 코드 `64a6e81` GitHub `main`·Production `dpl_7c5K42ccVF86a2e5P5YL4E5b425o` 반영. 운영 릴리스 `64a6e814766f`·서울 `icn1`·Supabase ready, 양 역할 200·비인증 401·관리자 빌드 SHA-256 일치. `client.html`·DB·서버·보호 기능 변경 없음 |
| 완료·배포 | 운영 입력 결과 여백·공개 확인 타이포 보정 | 핵심 결과·CSV 결과 행에 좌우 20px 여백, 150px 최소 라벨 열, 14px tabular 숫자폭과 안전한 줄바꿈 적용. 공개 전 확인 라벨은 중립 태그로 정돈하고 문장은 어절 단위 줄바꿈. 기존 계산·검수·공개 훅과 데이터는 유지 | 1280px 결과 6행 오른쪽 여백 21px·가로 넘침 0·확인 문구 한 글자 고립 0, 입력 검수 5/6·CSV `3,280만원·456%·1,080건`·브라우저 오류 0, 역할 5상태·양 역할 parity·잠금 21함수·23파일·11마이그레이션·서버 224/224·플레이스 51/51·계약 28/28·인증 18/18·로컬 및 Vercel 전체 `check:release` 통과 | 코드 `a3ba5b4`를 코드 `64a6e81`과 함께 GitHub `main`·Production `dpl_7c5K42ccVF86a2e5P5YL4E5b425o`에 반영. 운영 릴리스·양 역할 200·비인증 401·관리자 빌드 SHA-256 일치 |
| 완료·배포 | 운영 입력 프리미엄 정보 계층·행열 정렬 보정 | 운영 원본·3단계 진행 상태·핵심 결과·공개 전 확인을 우선 배치하고 공개 설정과 CSV 계산은 접이식 보조 영역으로 분리. 641~900px 진행·확인 영역은 균등 3열, 계산 결과는 공통 2열 기준선, 520px 이하는 한 열로 전환. 기존 업로드·검수·계산·공개 데이터 훅과 보호 기능은 유지 | 운영팀 단독 로컬 세션 검수·설정·CSV 계산·공개 반영, 1440·1180·1024·900·771·390px 가로 넘침 0·행열 기준선 일치·브라우저 오류 0, 역할 5상태·양 역할 parity·보호 잠금 21함수·23파일·11마이그레이션·서버 224/224·플레이스 51/51·서버 계약 28/28·Production 인증 18/18·로컬 및 Vercel 전체 `check:release` 통과 | 코드 `38c8a65`·`d4d9dc2` GitHub `main`·Vercel Production `dpl_66Ye7ppneaWEkzc8sqf6XyYTPG7e` 반영. 운영 릴리스 `d4d9dc2bcfd2`·서울 `icn1`·Supabase ready, 양 역할 HTML 200·비인증 세션 401·관리자 빌드 SHA-256 일치 |
| 완료·배포 대기 | 운영 홈 여백·정보 계층 재설계 | 한 화면에 압축됐던 빠른 실행·월 현황·운영 루틴·공개 전 확인을 독립된 전체 폭 영역으로 분리. 빠른 실행은 넓은 2×2 카드, 월 현황은 밝은 독립 카드, 네이비 운영 루틴은 얇은 타임라인, 공개 전 확인은 넓은 2열 목록으로 정리하며 기존 상태 데이터와 이동 기능은 그대로 유지 | 1440×900 데스크톱 상·하단, 390×844 모바일 상·하단 육안 검수, 모바일 가로 넘침 0, 매출·보고서 카드 화면 이동, 브라우저 로그 0, 기준선·역할 5상태·양 역할 parity·보호 잠금 21함수·23파일·11마이그레이션·서버 224/224·플레이스 51/51·Production 인증 18/18·전체 `check:release` | 사용자 육안 확인 전 배포하지 않음. 로컬 커밋만 보존 |
| 완료·배포 | 운영팀 운영 홈 프리미엄 압축·월 운영 현황 | 실제 기능은 유지하고 반복되던 연결·입력·공개·보고서 설명을 `빠른 실행 → 운영 루틴 → 공개 전 확인`으로 통합. 운영 루틴 하단에는 저장된 공개 매출·입력일과 서버 보고서함의 이번 달 기록·공개 상태만 사용해 `매출 입력`·`보고서 제출` 현황을 표시하며 완료값을 임의 생성하지 않음 | 데스크톱 1280·1440px, 모바일 390px 2열 현황 카드·가로 넘침 0, 매출 입력 전/완료·보고서 제출 전/검수 중/완료 상태와 화면 이동, 깨끗한 브라우저 오류 0, 역할 5상태·양 역할 parity·보호 잠금 21함수·23파일·11마이그레이션·기준선·서버 224/224·플레이스 51/51·Production 인증 18/18·전체 `check:release` | 코드 `bef7011` GitHub `main`·Vercel Production 반영. 운영 `/health`·`/ready` 릴리스 `bef7011dee54`·서울 `icn1`·Supabase ready, 관리자 번들의 새 현황 구성과 비인증 세션 401 확인 |
| 완료·배포 | 5대 핵심 기능 안정 기준 동결·복구 지점 고정 | 대표님이 현재 기능을 최선의 운영 기준으로 확정. 다른 작업의 승인 없는 보호 소스 변경·잠금 해시 갱신을 금지하고 신규 조회·SEO 점검·추적 등록·갱신은 계속 허용 | 잠금 현재값 21함수·23파일·11마이그레이션, 변조 self-test, 총관리자·연결 운영팀·단독 운영팀·광고주·해제 운영팀 5상태, 양 역할 parity, API·서버 224/224, 플레이스 51/51, 서버 계약 28/28, Production 인증 18/18, 공개 빌드/CSP·전체 `check:release` 통과. 운영 양 역할 파일 해시 일치·비인증 5기능 401·원격 품질/상품/플레이스 작업 성공 | 코드 `fbe3946` GitHub `main`·Vercel Production 반영. `/health`·`/ready` 릴리스 `fbe3946f45bd`·서울 `icn1`·Supabase ready. 기능 코드·DB·운영 데이터 변경 없음. 원격 복구 체크포인트 `checkpoint/core-five-stable-20260727` |
| 완료·배포 | 로그인 없는 N 상품 30일 자동 추적 대기열 보완 | 기존 09:00·15:00 및 시간별 보정 크론·비밀값은 정상이고, 1건×10회 상한 때문에 활성 71개 중 35개가 밀린 원인을 확정. 상품만 기존 순차 처리 5건×20회·최대 100건으로 확대하고 Vercel 기본 1건, DB 임대 잠금, 실패 재시도, 플레이스 직렬 수집은 유지 | 새 경계 단위검사와 상품 대상 31/31, API·서버 224/224, 플레이스 51/51, 서버 계약 28/28, 역할 5상태·핵심 5기능, 양 역할 parity, 잠금 self-test·현재 잠금 21함수·23파일·11마이그레이션, 공개 빌드/CSP, Production 인증 18/18, 전체 `check:release` 독립 2회 통과. 운영 무로그인 상품 26/26 성공·실패 0·`drained=true`, DB 상품·플레이스 대기 0·임대 0, 1280px·390px 양 역할 넘침·콘솔 오류 0 | 코드 `d576d0f`·Vercel Production 반영. `/health`·`/ready` 릴리스 `d576d0fcaeb7`·서울 `icn1`·Supabase ready, GitHub 상품 작업 `30238482719`·플레이스·원격 품질 성공. `admin.html`·`client.html`·DB 스키마·기존 데이터 변경 없음 |
| 완료·배포 | 역할·연결 상태 자동 회귀 게이트 | 총관리자·연결 운영팀·광고주 미연결 운영팀·광고주·해제 운영팀의 5상태와 키워드·SEO·N 상품·상품 30일·플레이스 30일 5기능을 교차 검증. 보고서·운영팀 관리 경계, 위조 헤더 교체, 해제 세션, 단독 운영팀 상품 8개·플레이스 5개 액션 권한 경로와 양 역할 화면 계약을 품질·릴리스에 고정 | `check:role-state-regression`, API·서버 222/222, 플레이스 51/51, 서버 계약 28/28, Production 인증 18/18, 역할 parity·기준선·공개 빌드/CSP·보호 잠금 21함수·23파일·11마이그레이션·전체 `check:release` 통과. 단독 운영팀 로컬 브라우저에서 두 추적 화면 입력·버튼 활성·과거 경고 0건 확인 | 코드 `afe5bfa` GitHub `main`·Vercel Production 반영. 운영 릴리스 `afe5bfa0a8a3`·서울 `icn1`·Supabase ready, 양 역할 파일 해시 일치, 비인증 5기능 API 401 확인. DB·운영 데이터·잠금 해시 변경 없음 |
| 완료·배포 | 운영팀 단독 N 상품·플레이스 30일 순위 격리 정상화 | 광고주 미연결 운영팀도 서버가 확인한 운영팀 코드 기반 `team-account` 격리 범위에서 두 추적의 조회·등록·갱신을 사용하도록 세션·API·화면 계약을 보완. 광고주 연결 후 기존 광고주 범위로 자동 전환하고 보고서·공개 데이터·원본 파일의 연결 전 차단 유지. 과거 관리자 코드 경고를 정확한 재로그인 안내로 교체 | 운영 DB 읽기 전용 확인, 역할·격리 대상 89/89, API·서버 220/220, 플레이스 51/51, 서버 계약 27/27, Production 인증 18/18, 역할 parity·잠금 self-test·현재 잠금 21함수·23파일·11마이그레이션·공개 빌드/CSP·전체 `check:release` 통과. 운영팀 단독 브라우저에서 두 메뉴·입력 활성·기존 경고 0건 확인 | 코드 `70f5c75`·Production `dpl_ByyyBPzAhoPTEmG7zgZ2awh8YWRo` 운영 반영. `/health`·`/ready` 릴리스 `70f5c7574ec9`·서울 `icn1`·Supabase ready, 양 역할 파일 해시 일치, 비인증 두 API 401, 운영 관리자 파일의 과거 경고 문구 부재 확인 |
| 완료·배포 | 프로젝트 파일 안전 정리·경로 단순화 | 실제 참조·빌드·배포 여부를 기준으로 `dist`·`.vercel/output`·샘플 출력·`.DS_Store`만 삭제하고 현재 구조와 충돌하며 참조가 없는 Next.js 안내 2개 제거. 초기 기획 8개는 `docs/planning`, 현재 운영 문서는 `docs` 루트로 분리하고 시작 경로를 고정 | 정리 전 Git·158개 추적 파일·ignored 파일·용량·참조 관계 확인. 약 18MB 재생성 산출물 제거 후 작업공간 dry-run clean, 로컬 문서 링크 22개, 핵심 경로·불필요 경로 부재, 잠금 21함수·23파일·11마이그레이션, API·서버 218/218, 플레이스 51/51, 서버 계약 27/27, Production 인증 18/18, 공개 빌드/CSP·전체 `check:release` 통과 | 코드 `548b997`·Production `dpl_D73LagXA6oRCJu3CqrHC8MuCGQtj` 운영 반영. `/health`·`/ready` 릴리스 `548b9973383e`·서울 `icn1`·Supabase ready, 양 역할 운영 파일 해시 일치, 비인증 5기능 API 401 확인 |
| 완료·배포 | 5대 핵심 기능 보호 잠금·3역할 회귀 확장 | 기존 `키워드 조회`, `N 상품 순위`, `N 30일 순위`, `N 플레이스 30일 순위` 잠금에 `SEO 확인`의 공통 점수 엔진·서버 자동 점검 수집기와 운영팀·광고주 조회·평가·표시 진입점을 추가. 실행 중 신규 조회·점검·추적은 계속 허용하고 승인 없는 소스 변경만 릴리스에서 차단 | 잠금 현재값 21함수·23파일·11마이그레이션과 모든 항목 변조 self-test, 운영팀·광고주·총관리자 5기능 권한, SEO 18/18 포함 대상 31/31, API·서버 218/218, 플레이스 51/51, 서버 계약 27/27, Production 인증 18/18, 역할 parity·기준선·공개 빌드/CSP·전체 `check:release` 통과 | 코드 `34431f5`·Production `dpl_AaVnUcMpHonkbS8no3DhSDutzU2g` 운영 반영. `/health`·`/ready` 릴리스 `34431f5b2504`·서울 `icn1`·Supabase ready, 양 역할·SEO 운영 파일 해시 일치, 비인증 5기능 API 401 확인 |
| 완료·배포 | 상품 SEO 전 키워드 동일 계산식 고정 | 상품·키워드별 예외값 없이 공통 정책 `uniform_keyword_evidence_v1`을 모든 조회 결과에 기록하고, 입력 키워드·상위 오가닉·카테고리·브랜드/제조사·대표 이미지·수기 리뷰·광고 제외 순위의 실제 근거만 바꿔 동일 배점을 적용. 양 역할 화면에 모든 키워드 동일 계산식 안내와 v13 캐시 적용 | 아이쉘 기존 76점 보존. 같은 부분 근거의 무선청소기·여성러닝화·차량용방향제 76점, 정확 키워드의 써큘레이터·온열찜질기·강아지사료·아이폰16케이스 95점 확인. 무관 상품·순위 경계 포함 SEO 18/18, API·서버 217/217, 플레이스 51/51, 서버 계약 27/27, Production 인증 18/18, 역할 parity·기준선·공개 빌드/CSP·전체 `check:release`, 보호 잠금 self-test·현재 잠금 13함수·21파일·11마이그레이션 통과. 소스·빌드 엔진 SHA-256 일치 | 코드 `3ba297f`·Production `dpl_GrTrGF9C3E7UgbTqLRzsPjUh6pX1` 운영 반영. `/health`·`/ready` 릴리스 `3ba297fad36d`·서울 `icn1`·Supabase ready, 양 역할 v13 및 운영 엔진 7개 교차 표본 재현 확인 |
| 완료·배포 | 상품 SEO 부분 관련성·브랜드·순위 점수 균형 보정 | exact 키워드는 10점, 동일 상품군·상위 핵심어 근거가 있는 부분 관련성은 6~8점으로 구분하고 무관 상품은 제외. 상위 핵심어 40% 이상 반영은 6점, 브랜드만 등록된 상품은 7점, 광고 제외 순위는 5위 10점·40위 9점·100위 8점·200위 5점·300위 3점으로 단계화 | 사용자 제공 아이쉘 상품 조건 `상품명 46자·정확 키워드 미포함·상품군 관련·상위 핵심어 2개·브랜드만 등록·리뷰 133개·45위`를 고정 회귀해 `16+6+15+7+10+14+8=76점` 확인. 무관 상품명 부분점수 차단과 5·40·100·200·300위 경계 포함 SEO 16/16, API·서버 215/215, 플레이스 51/51, 서버 계약 27/27, Production 인증 18/18, 양 역할 parity·기준선·공개 빌드/CSP·전체 `check:release`, 보호 잠금 self-test·현재 잠금 13함수·21파일·11마이그레이션 통과 | 코드 `ef6a4ba`·Production `dpl_29QtNh13TgGd6f9fyBPZ4o9DfRED` 운영 반영. `/health`·`/ready` 릴리스 `ef6a4bae9632`·서울 `icn1`·Supabase ready, 양 역할 v12 화면과 운영 엔진의 동일 표본 76점 재현 확인 |
| 완료·배포 | 상품 SEO `상품 노출 구조` 참고 카드 제거 | 운영팀·광고주 SEO 화면의 `상품 노출 구조` 요약·점검 카드·점수 기준 문구와 공통 평가 엔진 입력을 함께 제거. 원부형·단일형 상품 판별을 사용하는 N 상품 순위 기능은 변경하지 않고 SEO 0점 참고 항목만 제외 | SEO 회귀 13/13, API·서버 212/212, 플레이스 수집기 51/51, 서버 계약 27/27, Production 인증 18/18, 운영팀·광고주 parity, 기준선, 공개 빌드/CSP, 로컬·Vercel Production 전체 `check:release`, 보호 잠금 self-test·현재 잠금 13함수·21파일·11마이그레이션 통과. 운영 양 역할 HTML·공통 엔진에서 카드·문구 부재 확인 | 코드 `3cf5422`·Production `dpl_2Ch1ztEz6tDpqiGEH1xsDaD4nLxm` 운영 별칭 반영. `/health`·`/ready` 200, 릴리스 `3cf5422fdddc`, 서울 `icn1`, Supabase ready |
| 완료·배포 | 상품 SEO 안정 수집 7항목·리뷰 수기·트래픽 점수 보정 | 불안정한 공개 상세 크롤링 의존을 화면에서 제거하고 공식 검색 결과로 확인되는 `상품명·상위 상품 핵심어·카테고리·브랜드/제조사·대표 이미지·상품 노출 구조·광고 제외 오가닉 순위`만 자동 표시. 리뷰 수량만 직접 입력하며 노출 구조는 참고 항목으로 점수에서 제외. 다른 등록 항목이 모두 충족되면 5위 이내 95점·40위 이내 90점·40위 초과 또는 300위 완주 미노출 85점으로 고정 | SEO 회귀 13/13, API·서버 212/212, 플레이스 수집기 51/51, 서버 계약 27/27, Production 인증 18/18, 운영팀·광고주 parity, 기준선, 공개 빌드/CSP, 로컬·Vercel Production 전체 `check:release`, 보호 잠금 self-test·현재 잠금 13함수·21파일·11마이그레이션, `git diff --check` 통과. 양 역할 데스크톱 빌드에서 리뷰 입력·8항목·점수표 배치 육안 확인 | 코드 `b8f3680`·Production `dpl_7xwEPXcFwiL2yQYDnHzb5ZsfdrZD` 운영 별칭 반영. `/health`·`/ready` 200, 릴리스 `b8f3680775ce`, 서울 `icn1`, Supabase ready, 비인증 세션·상품 조회 401, 양 역할·공통 엔진 운영 파일 해시·바이트 일치 |
| 완료·배포 | 상품 SEO 5대 자동 진단 보완 | 공개 상품에서 확정 가능한 값만 사용해 `상품명 적합도·상품정보 적합도·리뷰 경쟁력·등록정보 완성도·순위/트래픽 진단`으로 간결하게 재구성. 할인·가격·배송·리뷰 포인트와 검색 수요·경쟁은 SEO 점수에서 제외 | SEO/공개상품 29/29, API·서버 211/211, 플레이스 51/51, 서버 계약 27/27, Production 인증 18/18, 운영팀·광고주 parity, 기준선, 공개 빌드/CSP, 데스크톱·390px 2차 검수, 로컬·Vercel Production 전체 `check:release`, 보호 잠금 13함수·21파일·11마이그레이션 통과. 외부 실상품은 네이버 429 제한을 사실대로 분류 | 코드 `f0ccbff`·Production `dpl_FA2KBDtbHGrWYa3p2zkkv7pQVd97` 운영 별칭 반영. `/health`·`/ready` 200, 릴리스 `f0ccbff5ea7f`, 서울 `icn1`, Supabase ready, 비인증 세션·SEO 401, 양 역할 운영 파일 해시·바이트 일치 |
| 완료 | 4대 핵심 기능 재잠금·3역할 회귀 점검 | 키워드 조회·N 상품 순위·N 30일 순위·N 플레이스 30일 순위의 기존 잠금 해시를 덮어쓰지 않고 현재 보호 상태와 변조 탐지를 재확인. 운영팀 단독/광고주 연결, 광고주 자기 범위, 총관리자 정확 계정·대상 선택·전용 도구 경계를 함께 점검 | 잠금 self-test·현재 잠금 13함수·21파일·11마이그레이션, 역할 parity 6도구·5 API, 기준선, 서버 계약 27/27, Production 인증 18/18, API·서버 208/208, 플레이스 수집기 51/51, 공개 빌드/CSP·전체 `check:release` 통과. 운영 `/health`·`/ready` 200·서울 `icn1`·Supabase ready, 비인증 세션·4개 기능 API 401, 양 역할 운영 파일 SHA-256 일치 | 코드·DB·잠금 해시 변경 없음. 배포 없음 |
| 완료 | 운영팀 단독 계정·광고주 후연결 정상화 | 광고주가 아직 없는 운영팀도 계정 발급 화면뿐 아니라 키워드 조회·N 상품 단건 순위·SEO 점검·Meta 광고 조회를 독립적으로 사용하도록 정확 경로만 허용. 이 작업 당시 두 30일 추적 차단 정책은 2026-07-27 운영팀 코드 기반 격리 추적으로 대체됐으며, 광고주 생성·연결 해제 직후 현재 세션을 자동 재발급하는 계약은 유지 | 단독 허용 4경로·유사 경로 및 광고주 데이터 4영역 차단·광고주 헤더 미주입·생성/해제 세션 전환 회귀 통과. 전체 API·서버 208/208, 플레이스 수집기 51/51, 서버 계약 27/27, Production 인증 18/18, 역할 parity·기준선·공개 빌드/CSP·4대 기능 잠금 13함수·21파일·11마이그레이션·전체 `check:release`·`git diff --check` 통과 | 코드 `7e2fbd5`·Production `dpl_HYfQD3avuxuqjipYfyQ1aHbUANDL`·운영 별칭 반영. `/health`·`/ready` 200, 서울 `icn1`, Supabase ready, 운영 관리자 빌드 해시 일치, 비인증 세션 401 확인 |
| 완료·배포 | N 플레이스 30일 부분조회·순위 UX 정상화 | 운영 DB 10개 최신 스냅샷과 현재 네이버 PC 오가닉 직접 수집을 대조. `N개 확인`은 순위값이 아니라 `rank=null`인 부분 조회의 확인 범위이며, 이를 일별 카드의 큰 순위 위치에 표시해 실제 순위처럼 보이던 UX를 양 역할에서 분리 | 10개 중 8개는 실제 순위 정상 저장. 호르몬치치 구월점은 현재 오가닉 100개 직접 수집에서도 정확 ID 미발견·부분 조회로 재현. 순위 미확정 일별 칸은 `-`, 확인 범위는 기존 안내·업체 지표에서만 표시. API·서버 208/208, 플레이스 수집기 51/51, 서버 계약 27/27, Production 인증 18/18, 역할 parity·기준선·공개 빌드/CSP·전체 `check:release`, 잠금 self-test·잠금 검사·`git diff --check` 통과 | 코드 `603a82c`·Production `dpl_4v46G5x7vBP8LJ4T4PBEqzj6TKLm` 운영 반영. `/health`·`/ready` 200, 서울 `icn1`, Supabase ready, 비인증 세션 401, 양 역할 운영 파일 SHA-256·새 표시 계약 일치. 핵심 잠금 유지 |
| 완료 | Vercel 서울 리전·로그인 세션 함수 분리·지연 최적화 | Vercel 함수 실행 지역을 Supabase와 같은 서울 `icn1`로 고정하고 Fluid Compute를 명시 활성화. `/api/session`을 장시간 순위 API catch-all과 별도 함수로 분리하고 로그인 rate-limit DB 요청을 독립 병렬 처리해 네트워크 왕복을 줄임. 기존 권한·세션·차단 정책과 4대 조회·추적 기능 잠금은 유지 | 대상 단위 61/61, 전체 API·서버 207/207, 플레이스 수집기 51/51, 서버 계약 27/27, Production 인증 18/18, 보호 기능 잠금 13함수·21파일·11마이그레이션, 일반·Production 환경 `check:release`, Vercel Production 빌드·공개 빌드/CSP·`git diff --check` 통과. 운영 `/health`·`/ready`·비인증 세션 연속 30/30·동시 15/15, 실패 0건 | 코드 `31b70e4`·Production `dpl_DZFaojbvvLfnGVVB7G7bVA3jzSgX`·운영 별칭 반영. 실제 함수와 `/health`의 `region=icn1` 확인 |
| 완료 | 광고주 코드 자동 제안 제거·명시 입력 강제 | 운영팀·총관리자 코드 생성 화면에서 다음 광고주 코드를 자동 입력하거나 노출하지 않고, 공백 제출 시 서버가 순차 코드를 대신 발급하지 않도록 차단 | 전용 7/7, API·서버 206/206, 플레이스 수집기 51/51, 서버 계약 24/24, 운영팀·광고주 parity, Production 인증 18/18, 보호 기능 잠금 13함수·21파일·11마이그레이션, 공개 빌드·CSP·전체 `check:release` 통과. 운영 `/health`·`/ready` 200, Supabase ready, 비인증 생성 401, 관리자 번들 직접 입력 문구·로컬 산출물 일치 확인 | 코드 `553a880`, Production `momentinsight-pms5xuqh4-momentlabs.vercel.app`·운영 별칭 반영. 릴리스 `553a8801e2fa` |
| 완료 | 상품 SEO 트래픽·리뷰 평균·태그·상품정보고시 재정렬 | 100점은 모든 자동 점검을 충족하고 정확 상품이 상위 5위일 때만 허용. 6위 이하에는 트래픽·노출 점수를 감점하고 `트래픽·노출 보완`을 바로 수정할 항목 최상단에 표시. 리뷰는 같은 키워드 상위 오가닉 상품 최대 5개의 실제 확인값 평균과 비교. 판매자 태그 10개와 상품정보제공고시의 `상세페이지 참조` 사용 여부를 공개 상품 상세에서 자동 확인. 공식 브랜드·중복 단어·홍보 문구·전화번호·특수문자·동종 카테고리 6개 점검 카드는 제거 | SEO 대상 25/25, `온열찜질기`·상품 `12149720593`·순위 11위가 100점이 아니며 트래픽 보완이 최우선인 회귀 포함 API·서버 204/204, 플레이스 수집기 51/51, 서버 계약 24/24, 운영팀·광고주 parity, 잠금 13함수·21파일·11마이그레이션, Vercel 빌드·CSP·전체 `check:quality` 통과 | 대표님 배포 승인. 코드 커밋·Production 반영·운영 실조회 검증 진행 |
| 완료 | Owner 부가세 포함 금액 역산·SEO 동시 배포 | 정확한 `mml93-a01` Owner 세션에만 제공되는 계산기의 입력 기준을 `부가세 포함 금액`으로 변경. 입력 합계금액은 그대로 유지하고 공급가액을 10/11 기준 원 단위 반올림해 역산하며 부가세액은 합계금액-공급가액으로 계산. 공개 HTML·운영팀·광고주 비노출과 복사 기능은 유지하고 검증 완료된 SEO 커밋과 한 릴리스로 배포 | 표본 `776,602원 → 공급가액 706,002원·부가세 70,600원·합계 776,602원`, 전용 27/27, API·서버 202/202, 플레이스 수집기 51/51, Owner 권한·공개 비노출·양 역할 parity·4대 기능 잠금 13함수·21파일·11마이그레이션·잠금 self-test·공개 빌드/CSP·전체 `check:release` 통과. 운영 `/health`·`/ready` 200, Supabase ready, 비인증 Owner API 401, 관리자 번들 `total` 요청·SEO v7 반영 확인 | 코드 `4a1c8be`·SEO `896c3f6`, GitHub `main`·Vercel Production·운영 별칭 반영. 릴리스 `8318a4d897a3` |
| 완료 | 네이버 상품명 가이드·상위 리뷰 비교 SEO 강화 | 네이버 공식 가이드 중 공개 데이터로 자동 판정 가능한 키워드 포함·50자·공식 브랜드/제조사·중복·홍보/혜택 문구·전화번호·특수문자 규칙을 근거별로 추가. 대상 상품과 같은 키워드의 상위 상품 공개 페이지 2~3개에서 리뷰를 실제 확인한 경우에만 중앙값과 비교하며, 모든 확인 항목이 양호하지만 순위가 40위 밖일 때 `트래픽 부족 가능성`을 별도 표시. 확인 불가능한 항목은 카드·점수·수정 목록에서 제외 | SEO 전용 23/23, API·서버 202/202, 플레이스 수집기 51/51, 양 역할 parity·기준선·4대 기능 잠금 13함수·21파일·11마이그레이션·서버 계약·Production 인증·Vercel 공개 빌드/CSP·전체 `check:quality` 통과. 로컬 브라우저 육안 검수는 앱의 localhost 차단으로 미완료이며 자동 DOM·빌드 검증으로 대체하지 않음 | 현재 배포 승인 없음. 로컬 커밋 후 배포 대기 |
| 완료 | 상품 SEO 기준 정정·미확인 항목 제거 | 상품명 권장 길이를 50자로 정정하고, 공개 화면에서 자동 확정할 수 없는 항목은 점검표·점수·수정 목록에서 완전히 제외. 상세페이지 8컷은 서버 공개 응답으로 안정적으로 셀 수 있을 때만 적용하며 현재 불확정 상태에서는 노출하지 않음. 상품 노출 표시는 `순위 N위`로 단순화 | 전용 17/17, API·서버 196/196, 플레이스 수집기 51/51, 서버 계약 24/24, Production 인증 18/18, 양 역할 parity·기준선·4대 기능 잠금 13함수·21파일·11마이그레이션·공개 CSP·전체 `check:release` 통과. 실제 상품 `12149720593`에서 `순위 11위`·`50자 이내`를 확인하고 `API 참고`·`자동 확인 불가`·`미확인`·`80%`·`상품정보고시` 노출 0건 확인 | 코드 `4326e24`, Production `momentinsight-9gcm2wujh-momentlabs.vercel.app`·운영 별칭 반영. `/health`·`/ready` 릴리스 `4326e24118b8`, Supabase ready, 비인증 401 확인 |
| 완료 | 상품 SEO 판매자 입력 제거·공개 화면 자동 확인 | 초보 사업자가 상세페이지·상품정보고시·할인율·리뷰 포인트를 직접 판단하지 않도록 네이버 상품 URL 기준 서버 자동 수집으로 전환. 리뷰·상세콘텐츠 등록·할인·리뷰 포인트는 공개 화면에서 자동 확인하고, 상품정보고시와 상세페이지 80% 충족처럼 공개 근거만으로 확정할 수 없는 항목은 추측 점수 없이 `자동 확인 불가`로 표시. 운영 첫 실조회에서 확인한 네이버 429 제한은 서버 500으로 바꾸지 않고 재시도 가능한 공개 소스 제한으로 안내하며 공식 상품 조회 결과는 계속 표시 | 실제 상품 `12149720593` 운영 재조회에서 상품명·동종 카테고리·가격·공식 API 참고 순번은 유지되고, 네이버 공개 페이지 429는 일반 서버 오류 없이 `일시적 조회 제한`과 `자동 확인 불가`로 표시됨을 확인. 운영 로그도 쇼핑 조회 200·SEO 공개 수집 429로 분리됐다. 자동 수집·평가 전용 17/17, API·서버 196/196, 플레이스 수집기 51/51, 서버 계약 24/24, Production 인증 18/18, 운영팀·광고주 parity, 기준선, 4대 기능 잠금 13함수·21파일·11마이그레이션, Vercel 빌드·공개 CSP 검사 통과 | 코드 `e28b08c`, Production `momentinsight-q2v31gtje-momentlabs.vercel.app`, 운영 별칭 반영 완료. `/health` 릴리스 `e28b08ce815f`, `/ready` Supabase ready, 비인증 401 확인 |
| 완료 | 상품 SEO 기본 기준 재설계 | 검색 수요·경쟁을 SEO 점수에서 완전히 분리하고 상품명 키워드·40자 이내, 동종 상위 상품 카테고리 일치, 리뷰, 상세페이지 80% 이상, 상품정보고시 직접 작성, 할인율, 리뷰 포인트의 8개 항목으로 재구성. 공개 API에서 확정할 수 없는 4개 운영 항목은 판매자 확인값만 반영하고, 전체 기본 항목이 양호한데 오가닉 노출이 약할 때만 트래픽·노출 점검 필요를 별도 진단 | 전용 회귀 8/8, API·서버 187/187, 플레이스 수집기 51/51, 서버 계약 23/23, Production 인증 18/18, 운영팀·광고주 공통 엔진·역할 parity, 4대 기능 잠금 13함수·21파일·11마이그레이션, 기준선, Vercel 빌드·공개 CSP 검사 통과. 운영 `/health` 릴리스 `86e106e91f87`, `/ready` Supabase ready, 양 역할 HTML·로그인 화면·비인증 401 확인 | 코드 `86e106e`·Production `momentinsight-7ttboawju-momentlabs.vercel.app`·운영 별칭 배포 완료 |
| 완료 | SEO 수동 유입·구매 입력 제거·자동 연동 경계 정리 | 최근 30일 유입수·구매수는 운영자 수동 입력과 공개 URL 추정으로 평가하지 않음. 두 입력란과 점수 반영을 양 역할에서 제거하고 상품명·카테고리·검색 시장·리뷰 근거만 표시. 유입·결제 통계는 판매자 인증형 네이버 커머스 API 연결이 실제로 구축된 경우에만 별도 자동 신호로 재도입 | 리뷰 상승·수동 유입·구매 값 미반영·4항목 고정 회귀 6/6, API·서버 185/185, 플레이스 수집기 51/51, 서버 계약 23/23, Production 인증 18/18, 양 역할 parity·수동 입력 부재, 4대 기능 잠금 13함수·21파일·11마이그레이션, 공개 빌드·CSP·전체 `check:release` 통과. 운영팀·광고주 데스크톱 및 390px UI에서 수동 입력 0건·가로 넘침 0건 확인 | 코드 `34730b2`, Production `momentinsight-mrp0qbc88-momentlabs.vercel.app`·운영 별칭 반영. `/health` live·`/ready` Supabase ready, 릴리스 `34730b2a559f` 확인. 운영팀 실화면 수동 입력 0건·리뷰 입력 유지·콘솔 오류 0건 확인 |
| 완료 | SEO 진단 v2 트래픽·구매·리뷰 신호 강화 | 리뷰 수가 많을수록 리뷰 신뢰 점수와 총점이 상승하도록 독립 신호로 유지. 최근 30일 유입수와 구매수를 직접 입력해 트래픽 상태·전환율을 별도 진단하고, 상품명·카테고리·시장 수요·리뷰와 함께 근거별 점수를 표시. 확인된 값과 직접 입력값을 구분하고 API 결과 순번은 SEO 점수와 분리한 참고값으로 유지. 리뷰만 입력된 경우에는 점수는 상승하되 데이터 신뢰도는 낮게 유지해 완성 상태로 과장하지 않음 | 리뷰 상승·리뷰 단독·트래픽 구간·전환율·미입력 신뢰도 회귀 7/7, 운영팀·광고주 동일 공통 엔진과 역할 parity, API·서버 186/186, 플레이스 수집기 51/51, 서버 계약 23/23, Production 인증 18/18, 4대 기능 잠금 13함수·21파일·11마이그레이션, 공개 빌드·CSP·전체 `check:release` 통과. 운영팀·광고주 데스크톱과 광고주 390px 2차 UI 검수 완료 | 기능 코드 `109191d`, Production `momentinsight-eseg8dnw8-momentlabs.vercel.app`·운영 별칭 반영. `/health` live·`/ready` Supabase ready·운영팀/광고주 HTML과 공통 엔진 바이트 일치 확인 |
| 완료 | 4대 조회·추적 핵심 기능 변경 잠금 확장 | 기존 N 상품·N 플레이스 30일 보호에 `키워드 조회`와 `N 상품 순위`의 운영팀·광고주 UI 및 핵심 서버 계약을 추가. 잠금은 개발 소스 변경·배포만 차단하고 사용자의 신규 키워드 조회, 상품 순위 조회, 상품·플레이스 추적 신규 등록·갱신은 계속 허용. 보호 범위 밖의 신규 기능 개발도 계속 가능 | 잠금 13함수·21파일·11마이그레이션, 모든 보호 함수·파일과 신규 마이그레이션 변조 차단 self-test, 양 역할 4기능 사용 경로·서버 create/refresh 경로 회귀, 역할 parity, API·서버 179/179, 플레이스 수집기 51/51, 서버 계약 23/23, Production 인증 18/18, 공개 빌드·CSP와 전체 `check:release` 통과 | 코드 `6c5d10d`, Production `momentinsight-htm9llc9v-momentlabs.vercel.app`·운영 별칭 반영. `/health` live·`/ready` Supabase ready, 릴리스 `6c5d10d1deef` 확인 |
| 완료 | N 30일 전체 갱신 진행 문구 간소화 | 양 역할의 전체 갱신 시작·진행 상태에서 내부 구현 표현 `안전 동시 갱신 2개`만 제거. 제한 동시성 2개·중첩 차단·서버 재대조·실패 재시도와 모든 순위 계산·저장·조회는 유지 | 대표님이 보호 UI 함수 변경을 명시 승인. 기준선·역할 parity·잠금 self-test와 승인된 운영팀/광고주 함수 해시만 갱신. 전체 `check:release`에서 API·서버 179/179, 플레이스 수집기 51/51, 서버 계약 23/23, Production 인증 18/18, 공개 빌드·CSP 통과. Production 운영팀 실갱신 25/25 완료, 광고주 동일 25개 로드, 양 역할 문구 0건·가로 넘침 0·콘솔 오류 0건 | 코드 `b2955e3`, Production `b2955e3142de` 반영 완료. `/health` live·`/ready` Supabase ready·기존 카드/이력 유지 확인 |
| 완료 | 키워드 종합차트 3년 범위·UI/UX 안정성 점검 | 서버가 종합차트용 월별 추이를 12개월만 요청해 3년 선택에도 13개 시점만 보이던 원인을 수정. 기존 1회 Hub 요청의 범위만 36개월로 확장하고 1년은 최근 12개월, 3년은 최근 36개월과 `YY.MM` 축으로 표시. 운영 2차 검수에서 확인한 모바일 3년 축 밀집은 데이터 36개를 유지한 채 라벨만 6개 기준으로 압축. 운영팀·광고주 프리미엄 카드·반응형을 보존하고 N 상품·N 플레이스 30일 보호 함수·서버·수집기·마이그레이션은 변경하지 않음 | Hub 실호출 HTTP 200·330ms·37개 월 시점, API·서버 179/179, 플레이스 수집기 51/51, 서버 계약 23/23, Production 인증 18/18, 역할 parity, 기준선, 순위 잠금 4함수·20파일·11마이그레이션, 공개 빌드·CSP, 전체 `check:release`, `git diff --check` 통과. 운영팀·광고주 각 3년 36개 시점·모바일 7개 축 라벨·가로 넘침 0·콘솔 경고/오류 0, 기존 1년 12개 시점 유지 확인 | 코드 `2f0b9b3`·모바일 보정 `eaaeed9`, Production `eaaeed9226d2` 반영 완료. `/health` live·`/ready` Supabase ready·양 역할 운영 검증 완료 |
| 완료 | 키워드 경쟁강도 수요·상품규모 비례 보정 | 대표키워드는 검색수요와 상품 등록 규모가 함께 클 때 경쟁이 높고, 수요가 충분해도 등록 상품이 적으면 저경쟁·고기회 후보가 되도록 전 키워드 공통 모델을 보정. 수요점수×절대 상품규모를 비례 결합하고 수요 대비 과잉 상품밀도를 별도 안전 신호로 사용하며 검색광고 경쟁도를 보조 반영. 실제 매출 데이터가 없으므로 `판매 기회율`은 매출 보장이 아닌 수요 대비 공급 공백 참고값으로 유지 | 신규 비례형 회귀 4개 포함 API·서버 169/169, 플레이스 수집기 51/51, 서버 계약 23/23, Production 인증 18/18, 운영팀·광고주 parity, 기준선, 순위 기능 잠금 4함수·20파일·11마이그레이션, 공개 빌드·CSP, 전체 `check:release`, `git diff --check` 통과. 표본 `71,400회·상품 약 34만·광고경쟁 보통`은 경쟁 `83·매우 높음`, 같은 수요·상품 2만은 `32·낮음`·판매 기회 `87·매우 높음` | 사용자 배포 승인. 로컬 커밋·Production 반영·운영 실조회 검증 진행 |
| 완료 | 키워드 조회 시장 3지표 프리미엄 요약 | 운영팀·광고주 키워드 조회에 `검색 수요`·`경쟁 강도`·`판매 기회율`을 0~100 점수와 간결한 진행 바로 표시. 검색 수요는 정확 월 검색량, 경쟁 강도는 검색광고 경쟁도와 쇼핑 상품수/검색량 포화도, 판매 기회율은 수요 65%와 역경쟁 35%를 결합하며 실제 매출 전환율이 아님을 명시. 값이 범위값이거나 근거가 부족하면 임의 점수를 만들지 않고 `확인 필요`로 표시하고 기존 연관 키워드 미리보기는 보존 | 신규 지표 단위 3/3 포함 API·서버 165/165, 플레이스 수집기 51/51, Production 인증 18/18, 운영팀·광고주 parity, 기준선, 순위 기능 잠금 4함수·20파일·11마이그레이션, 공개 빌드 8파일·인라인 6개/CSP 해시 4개, 전체 `check:release`, `git diff --check` 통과 | 사용자 배포 승인, 커밋·Vercel Production 반영 및 운영 실화면 검증 진행 |
| 완료 | N 상품·N 플레이스 30일 핵심 기능 변경 잠금 | 다른 기능 개발이 두 30일 추적의 운영팀·광고주 UI 함수, 서버 추적·크론, 플레이스 수집, 워크플로, 순위 DB 마이그레이션을 우발적으로 바꾸면 품질·릴리스 검사를 즉시 차단. 사용자의 신규 키워드 조회·상품/플레이스 추적 등록·갱신은 그대로 허용하며 새 순위 마이그레이션 자동 탐지와 명시적 승인 없는 잠금 기준 갱신·우회를 금지 | 정상 잠금 4개 함수·20개 파일·11개 마이그레이션, 가상 함수 변조 차단 self-test, `rankFeatureLockIsBuildOnlyAndUsageStaysOpen`, 전체 `check:release`: API·서버 162/162, 수집기 51/51, 역할 parity·기준선·공개 빌드·CSP·Production 인증 18/18 통과. 운영 신규 조회·추적 버튼 활성, 상품 25개·플레이스 10개 기존 이력 로드 확인 | Vercel Production·운영 별칭 반영 완료 |
| 완료 | N 상품·플레이스 30일 전체 갱신 사이트 공통 보장 | `mml93-a01`은 운영 실측 표본일 뿐 기능 기준 계정이 아님을 코드·문서에 명확히 구분. 운영팀·광고주는 현재 세션의 광고주 범위에서 동일하게 갱신하고, 정기 크론은 계정 필터 없이 사이트 전체 대기열을 처리하는 계약을 상품·플레이스 양쪽 회귀 검사로 고정 | `6fd007d`와 실제 코드 3개 커밋 대조. 임의 `agency-b02` 범위 및 사이트 전체 크론, 상품 27/27·플레이스 46/46, 역할 parity·기준선·전체 `check:release`·Production 인증 18/18 통과 | Vercel Production·운영 별칭 반영 완료 |
| 완료 | 홈페이지 서비스 안내 기능명 N 표기 통일 | 팝업의 순위 기능명 3개만 내부 메뉴와 동일한 `N 상품 순위`·`N 30일 순위`·`N 플레이스 30일 순위`로 변경. 팝업 크기·노출·닫기·1주일 숨김과 기능 로직은 보존 | 전체 `check:quality`, 기준선, Vercel 공개 빌드·CSP 검사 통과. 운영 데스크톱 348×489px, 가로 넘침·브라우저 경고/오류 0건, 새 기능명 각 1건 노출 확인 | Vercel Production·운영 별칭 반영 완료 |
| 완료 | N 상품·플레이스 30일 전체 갱신 안전 가속 | 상품 전체 갱신은 제한 동시성 2개·자동/수동 중첩 차단·서버 상태 재대조 후 진짜 미완료만 1회 재시도. 플레이스는 직렬 수집을 유지하고 완주/정상 소진 목록은 공유하며, 시간 마감/스크롤 한도 목록은 정확 Place ID가 이미 포함된 후속 항목에만 3분 재사용. 미발견·ID 없음·선택자 폴백은 새 수집 | 전체 `check:release`: API·서버 160/160, 수집기 51/51, 서버 계약 22/22, Production 인증 18/18, 양 역할 parity·CSP·공개 빌드·diff 통과. 운영 상품 25/25·115초·오류 0, 플레이스 10개·721초·오류 0, DB 상품 snapshot 1,985·플레이스 366 보존/증가 | 코드 `c71d565`, Vercel `c71d565f4140` live·Supabase ready, Render `2026-07-22-safe-exact-match-cache-v20` healthy. 운영 2차 검수 완료 |
| 완료 | 키워드 연령별 비중 API 집계 정상화 | 네이버 쇼핑인사이트의 월별 최고값=100 상대 클릭지수를 1년 단순 합산하던 왜곡을 제거. 비교 가능한 최신 완료 월 한 구간에서만 10·20·30·40·50대 이상을 합계 100%로 계산하고 진행 중인 월·응답에서 생략된 0값 연령대를 안전 처리. 양 역할 제목·설명·툴팁을 `연령별 쇼핑 클릭 비중`으로 통일 | 공식 NAVER API Hub 명세 대조, 운영 기존 응답 확인, 신규 4/4, API·서버 158/158, 수집기 49/49, 서버 계약 22/22, Production 인증 18/18, 역할 parity·CSP 공개 빌드·`check:release`·diff 통과 | 코드 `a4d68f3`, Production `a4d68f324d9b` 반영. `/health`·`/ready`·양 역할 HTML·보호 API 401 확인 |
| 완료 | 플레이스ID `1531240094` 실제 노출 미추적 긴급 정상화 | 의료 검색의 네이티브 `hospital/list`를 일반 `place/list?display=300`으로 바꿔 빈 목록을 읽던 원인을 수정. 정확한 Naver 프레임 호스트·경로·검색 문맥과 의료 `display=70`을 보존하고 맛집 300개 확장·광고 제외·정확 ID·기존 이력은 유지 | 실조회 `종로3가한의원` 3위·`종로한의원` 12위, 정확 ID 일치. `홍대 맛집` 7위·`부평 맛집` 100개 partial 회귀. 수집기 47/47, API·서버 154/154, 서버 계약 22/22, Production 인증 18/18, 전체 `check:release`·적대적 URL 검수·diff 통과 | 사용자 배포 승인, 코드 커밋 후 Render/Vercel 운영 반영·새 snapshot 검증 진행 |
| 완료 | 운영팀·광고주 로그아웃 로그인 화면 복귀 회귀 수정 | 광고주 성공 경로의 `clearClientAuth()` 누락을 바로잡고, 양 역할 모두 클릭 즉시 민감 화면을 잠근 뒤 서버 세션 종료를 5초 제한·최대 2회·JSON `ok=true`로 확인. 로그인·복원·보고서·원본·PPTX·계정·Meta·키워드·SEO·N상품의 이전 generation/role/scope 응답을 폐기하고 상품·플레이스 입력·필터·결과도 교차 계정 전에 초기화 | 전체 `check:release`: API·서버 154/154, 플레이스 44/44, 서버 계약 22/22, Production 인증 18/18, 역할 parity·CSP·문법·diff 통과. 자동 복원 회귀 보정 및 독립 적대적 2차 검수 P0/P1 0건. 실제 광고주 세션 로그아웃·새로고침 잠금·운영팀 잠금·콘솔 0건 확인 | 코드 `b052e85`, Production `b052e8597fb4`, `/health`·`/ready`, 양 역할 페이지 200, 보호 API 401 운영 반영 완료 |
| 완료 | 플레이스 오가닉 순위 실위치 전수 재검증 v16 | `allSearch` 미리보기·가상 스크롤 누락·중첩 프로모션·Actor 배열 순번을 운영 순위에서 제거. 네이버 PC 실제 장소 목록의 광고 제외 행만 사용하고 절대 마감 시각·1600px viewport로 cold start를 포함해 확인 범위를 확장. 본 서버는 정확 source·rankEvidence를 강제해 구버전·오염 응답을 snapshot 전에 거부하며 기존 현재값·30일 이력을 보존하고 재시도. `admin.html`·`client.html`·N상품·이력 로직은 변경하지 않음 | 로컬 `홍대 맛집` 100개/7위·`부평 맛집` 100개/null. 운영 snapshot `홍대 맛집` 97개/7위·`부평 맛집` 77개/null, native source·전체 지표 coverage 확인. API·서버 154/154, tracker 43/43, 수집기 44/44, 서버 계약 22/22, Production 인증 18/18, 전체 `check:release`·독립 검수 P0/P1 0 통과 | 코드 `5014d1a`, Vercel `/health`·`/ready`, Render `2026-07-20-native-organic-deadline-v16` 운영 반영 완료. 기존 이력 보존 |
| 배포 대기 | 네이버 API Hub 이관·쇼핑 검색 종료 대응 | Search Trend·Shopping Insight·blog/local을 `legacy|hub|auto`로 분리하는 공통 어댑터와 공급자별 인증·경로·오류 분류를 구현하고 Production을 `hub`로 명시 전환. Hub 읽기 요청은 네트워크·408·425·429·5xx만 최대 1회, 2초 이내 대기 후 재시도하며 인증·권한·잘못된 요청은 재시도하지 않음. 쇼핑 검색은 종료 전까지 legacy 전용으로 격리 | NCP Hub 실키로 Search Trend HTTP 200·30개 시계열, Shopping Insight age HTTP 200·153개 데이터 확인. 전환 릴리스 `9705862` Production Ready·운영 `/health` 200. 일시 503 후 1회 복구 회귀 포함 API·서버 179/179, 플레이스 수집기 51/51, 서버 계약 23/23, Production 인증 18/18, 순위 기능 잠금·공개 빌드·전체 `check:release`·`git diff --check` 통과 | 안정화 변경은 로컬 커밋 후 배포 대기. 2026-07-31 종료되는 쇼핑 검색의 검증 가능한 새 상품 순위를 별도 확정해야 함 |
| 완료 | 플레이스 검색결과 전체 업체 지표 정상화 | 대상 매장 일치 여부와 무관하게 키워드 검색에서 실제 확인한 모든 오가닉 업체의 블로그·방문 합계를 coverage와 함께 저장하고 키워드 월검색량을 별도 병합. 값 없음은 `-`, 명시적 0은 `0`으로 양 역할에 동일 표시. 불완전 coverage와 검색량 `<10` 상한은 확정 숫자로 저장하지 않음. 기존 순위·30일 이력·광고 제외는 보존 | `부평 맛집` 54개 전체 블로그 56,310·방문 173,749, `강남 맛집` 54개 전체 블로그 61,503·방문 145,192. API·서버 152/152, 수집기 35/35, Production 인증 18/18, 역할 parity·CSP·`check:release`·`git diff --check` 통과 | 사용자 배포 승인, 운영 반영 진행 |
| 완료 | 부평 맛집 플레이스·상품 원부 연속 추적 긴급 정상화 | 플레이스ID `2019299673`은 정확 ID·URL 좌표·공식 상호명을 우선하고 부분 조회에서 순위를 발명하지 않도록 보강. 상품ID `12649811979`는 최근 30일의 정확한 검증 원부ID만 이어받아 정확 상품과 함께 광고 제외 상위 300개에서 비교. 기존 추적 행·스냅샷·운영팀/광고주 UI는 보존 | 플레이스 실제 URL에서 `팽오리농장 부평점` 확인, 네이버 공개 목록 54개 부분 조회를 사실대로 보존. 운영 상품 스냅샷 원부 `57907660073`이 `음파 전동칫솔` 15위·`전동칫솔` 25위로 저장됨. `check:release`, API·서버 147/147, 플레이스 32/32, Production 인증 18/18, `git diff --check` 통과 | 코드 `3fb98b9`, Vercel 운영 별칭·Render 수집기 `v11` 반영 완료. 체크포인트 `checkpoint/rank-hotfix-20260719-2048` 원격 보관 |
| 완료 | Owner 코드 생성 화면 정렬·프리미엄 보완 | 1280px 이상에서는 운영팀·광고주 CTA를 동일한 2행 2열에 고정하고, 1220px 이하에서는 한 열·전폭 CTA로 전환. 생성 카드의 배경·테두리·간격·입력·버튼 위계만 전용 클래스 범위에서 정돈했으며 Owner 권한과 모든 생성 훅·서버 요청은 보존 | 로컬 1440·1280·1180·1024·390px에서 동일 우측 정렬 또는 전폭 전환, 가로 넘침 0 확인. `check:quality`, 서버 계약 22/22, API·서버 133/133, 플레이스 29/29, Production 인증 18/18, 공개 빌드·`git diff --check` 통과. `client.html` 변경 없음 | 커밋 `e648fc6`, 운영 별칭 반영 완료 |
| 완료 | 사용 중 기능 비소실 보호·배포 게이트 | API·세션·네트워크 오류가 기존 30일 상품·플레이스 추적 목록과 이력을 빈 화면으로 덮어쓰지 못하도록 양 역할 UI·서버 계약·자동 배포 게이트를 전수 점검하고 보강 | `check:release`, API·서버 133/133, 플레이스 29/29, Production 인증 18/18, 운영 `/health`·`/ready`, 관리자·광고주 화면, 보호 API 401, Render 수집기 `v10` 확인 | 커밋 `920854b` 포함 Vercel 릴리즈 `e648fc6c6519`·Render `2026-07-19-organic-selector-fallback-resilience-v10` 반영 완료 |
| 완료 | 보안 세션 후 30일 순위 목록 복구 | DB의 상품 88개·플레이스 13개와 이력은 보존된 상태에서, 총관리자 기본 조회 코드가 `session`으로 남아 두 목록 API가 403을 반환하고 UI가 빈 목록처럼 표시한 회귀를 수정. 총관리자 대상 코드는 서버가 확인한 실제 활성 코드로 기본화하고 API 오류 시 기존 카드를 지우지 않음 | 원본·스냅샷 보존, 대소문자 불일치 0건, 역할별 조회 계약, 서버 계약 22/22, API·서버 122/122, 플레이스 26/26, 전체 `check:quality`, CSP 통과. Production 총관리자 상품 25개·플레이스 10개, 광고주 격리 범위 상품 25개·플레이스 1개와 기존 일별 이력 확인 | 커밋 `7459d49`, 운영 별칭 반영 완료 |
| 완료 | 보안 전용 릴리즈·Owner 부가세 도구 격리 | Production `b37ca5f` 기준으로 세션·CSRF·속도 제한·권한 범위·업로드·SSRF·오류·CSP·공급망·크론·DB 권한만 보강. 이전 이메일/매직링크와 월간/PPT/보고서 목업은 제외. 부가세 계산기는 공개 HTML에 넣지 않고 정확한 `mml93-a01` Owner 세션에만 서버 전달·서버 계산 | `check:release`: 서버 계약 22/22, API·서버 122/122, 플레이스 26/26, Production 인증 14/14, 공개 빌드·공급망·`git diff --check` 통과. Supabase 3개 마이그레이션과 Vercel 민감 환경변수 4개 적용. 운영 점검에서 중첩 API 404·다중 쿠키 로그아웃 누락을 보정하고 로그인 전/광고주 비노출, Owner 계산·복사, 로그아웃 후 401 재검증 | 코드 `cc548b2`, Production `momentinsight-82hrf7a65-momentlabs.vercel.app` · 운영 별칭 반영 완료 |
| 완료 | 메인 기능 쇼케이스 우선순위·그룹 보정 | `Product Intelligence`를 `Trust Standard` 위로 이동하고, 현재 데이터 카드와 30일 추적 카드를 두 그룹으로 분리. 네이비 패널을 저채도 잉크 네이비로 보정 | 섹션 순서·그룹별 정확한 카드 구성 기준선, 1440×900·390×844 레이아웃, 문서 가로 넘침·카드 잘림·콘솔 오류 0건, 독립 2차 검토·운영 재검증 통과 | 커밋 `9cdbaad`, Production `momentinsight-8rljvs8ue-momentlabs.vercel.app` · 운영 별칭 반영 완료 |
| 완료 | 메인 홈페이지 익명 기능 쇼케이스 | 실제 광고주 상품·키워드·ID·매장 정보를 사용하지 않고 상품 오가닉 순위, 30일 상품 추적, 플레이스 추적, 키워드 시장 분석을 중립 예시 데이터 4카드로 압축. 기존 반복 섹션 2개를 1개로 교체 | 실고객 문자열·9자리 이상 ID·외부 URL·이미지 금지 기준선, `check:quality`, 1440×900·390×844 육안검수, 카드 4개·모바일 가로 탐색·텍스트 잘림·콘솔 오류·문서 가로 넘침 0건 | 커밋 `8d78d01` 포함 Production `momentinsight-8rljvs8ue-momentlabs.vercel.app` · 운영 별칭 반영 완료 |
| 완료 | 메인 홈페이지 프리미엄 보완 2차 | 문서·모바일 렌더 기반을 바로잡고, 팝업 외곽 규격·위치·노출 로직은 보존한 채 헤더·히어로·샘플 대시보드·신뢰 기준·운영 흐름·CTA·푸터의 시각 위계를 정돈. 모바일 반복 카드는 가로 탐색으로 압축 | `check:quality`, `build:vercel`, `git diff --check`, 1440×900·390×844 브라우저 육안검수, 팝업 348×489px·모바일 높이 489px, 닫기·1주일 숨김, 모바일 헤더 65px 한 줄, 가로 넘침·콘솔 오류 0건 | 커밋 `530839f` 포함 Production `momentinsight-8rljvs8ue-momentlabs.vercel.app` · 운영 별칭 반영 완료. 폐기된 `0886833` 미포함 |
| 완료 | N 30일 순위 슬롯 `상품` 문구 정리 | 일별 PM/AM 슬롯에서 정확 상품인 경우 중복되는 `상품` 문구만 숨기고, 원부 구분과 순위 조회·저장·대표값 판정 로직은 그대로 보존 | 양 역할 런타임 출력 `PM`·`AM · 원부`, 전용 릴리즈 기준선, 전체 `check:quality`, 운영 로그인 실데이터 각 414개 슬롯의 정확 상품 문구 0건·원부 구분 유지·콘솔 0건, 승인 SHA와 운영 파일 일치 | 커밋 `368408d`, Production `momentinsight-jly55k3zm-momentlabs.vercel.app` · 운영 별칭 반영 완료. 거절된 `0886833`은 로컬 이력에서 제거 |
| 완료 | N 30일 순위 오전·오후 행 정렬 복구 | `PM · 상품`·`AM · 원부`처럼 기준 문구가 붙은 슬롯 라벨만 고정 폭에서 두 줄로 접혀 순위 숫자가 아래로 밀리는 표시 오류를 공통 CSS로 수정. 순위 조회·저장·갱신·대표값 판정 로직은 변경하지 않음 | 운영팀·광고주 각 207개 일별 카드의 오전·오후 순위 기준선 차이 0px, 기준 라벨 줄바꿈·잘림 0건, 390px 동일 정렬, 콘솔 0건, `check:quality` 독립 2회·운영 재검증 통과 | 커밋 `01935d2`, Production `momentinsight-fhgibit9c-momentlabs.vercel.app` · 운영 별칭 반영 완료 |
| 완료 | 플레이스 30일 순위 컴팩트 프리미엄 UI | 운영팀·광고주 플레이스 30일 카드의 데이터·버튼·그룹·기록은 유지하고, 일별 기록을 140px 카드·2×2 보조지표·낮은 그림자·작은 상태 배지로 재구성. 모바일 상태 영역은 한 줄 가로 탐색으로 바꿔 핵심 순위가 먼저 읽히도록 통일 | 두 역할 데스크톱 카드 858×264px, 390px 카드 248×285px, 문서 가로 넘침·긴 수치 잘림·콘솔 경고/오류 0건, `check:quality` 독립 2회 및 운영 재검증 통과 | 커밋 `7710008`, Production `momentinsight-m19imug8x-momentlabs.vercel.app` · 운영 별칭 반영 완료 |
| 완료 | 전 키워드 원부·단일형 대표 순위 모델 식별 보강 | `써큘레이터`/상품ID `11687310806`의 정확 상품에는 키워드가 있으나 원부 제목에는 모델명만 있어 기존 키워드 공통 포함 조건이 원부를 누락한 원인을 확정. 원부는 모델번호 완전 일치 또는 기존 키워드 근거에 브랜드·상위 카테고리 일치까지 충족할 때만 연결하고 정확 상품 ID 판정은 그대로 보존 | 공식 API 원부 8위·정확 상품 59위, 아이템스카우트 원부 9위·정확 상품 60위 교차확인, 다른 모델 접두 유사값 제외, `치아미백제`·`전동 칫솔` 회귀, 운영팀·광고주 실조회·링크·390px·콘솔 0건, `check:quality` 독립 2회, 네이버 환경·Supabase 통과 | Production `momentinsight-cjx4bkodl-momentlabs.vercel.app` · 운영 별칭 반영 완료 |
| 완료 | 상품 순위 추적 광고 완전 제외 보강 | 단건 조회, 추적 생성·수동 갱신·자동 크론·Supabase 스냅샷 전 경로에서 `isAdProduct`·`adId`·sponsored/paid·비오가닉 삽입 타입을 제거하고 대표 순위·상위 결과를 오가닉만 저장 | 광고 혼입·오가닉 오탐 방지 단위검사, 네이버 실화면 product 40/supersaving 5 분리, Supabase 실스냅샷 `organic_only`, 양 역할 UI, `check:quality` 2회, 운영 재조회 통과 | Production `momentinsight-iddnfo068-momentlabs.vercel.app` · 운영 별칭 반영 완료 |
| 완료 | 네이버 상품 대표 순위·페이지 표기 정상화 | 단건 조회도 정확 상품과 관련 원부 중 오가닉 순위가 더 높은 결과를 대표 순위·상품 형태로 채택하고, 40개 보기 기준 페이지·페이지 내 순위를 함께 표시. 정확 상품 카드는 별도 보존 | 로그인 네이버 실화면·API 교차검증, 운영팀·광고주 로컬 실조회, 데스크톱·390px 육안검수, 콘솔 0건, `check:quality` 독립 2회 통과. `전동칫솔` 원부 34위(1p 34)·정확 상품 168위(5p 8) | 배포 승인 범위·운영 재검증 전 대기 |
| 완료 | 네이버 상품 30일 대표 순위 원부 비교 | 단건 상품 ID 일치는 유지하되 30일 추적 저장 시 정확 상품과 관련 원부의 공식 API 순번을 비교해 숫자가 더 낮은 쪽을 대표 순위로 기록. 스냅샷과 카드에 `상품` 또는 `원부` 근거를 함께 표시 | 순위 선택 단위검사, `check:rank-matching`, `check:baseline`, 실조회 `치아미백제` 43/9→9위·`치아미백` 60/14→14위, 전체 `check:quality` 2회, 소스·빌드 HTML 스크립트 문법/마커, `git diff --check` 통과. 로컬 브라우저는 보안 정책으로 접근 차단 | 별도 배포 지시 전 대기 |
| 배포 대기 | UI/UX 1차 고도화·상태 진실성 | 운영 홈의 가짜 집계·예시 브랜드·과거 날짜를 제거하고 실제 관리 화면으로 이동하는 작업 카드로 전환. 광고주 데이터 있음/없음 상태, 보고서 빈 상태, 모바일 요약 밀도와 역할 공통 스크롤 복귀를 보강 | 전체 `check:quality`, 서버 13/13, 플레이스 25/25, Vercel 빌드, Supabase HTTP 200, 1280px·390×844 DOM/육안 검수, 빈/데이터 있음 회귀, 역할별 스크롤 `0`, 브라우저 로그 0건 | 운영 영향 보고 후 진행 |
| 완료 | 네이버 상품 판매자 ID 정확 일치·300위 완주 보강 | 카드 UI는 유지. API `productId`와 결과 링크의 판매자 상품ID를 분리해 다른 링크 상품의 오일치를 차단하고, 첫 정확 일치 뒤에도 선택 범위 300개까지 확인. 정확 카드 상세는 입력 원본 URL 유지 | `전동칫솔` 원문·상품 페이지 대조: 원부 33위·동일 입력 상품 173위·300개 확인. `치아미백제` 7위/48위 정상 회귀, 잘못된 키워드 300개 미발견, 전체 `check:quality` 2회, 환경·Supabase, 운영팀 1280px·광고주 390×844 운영 실조회·콘솔 0건 통과 | Production `momentinsight-mmeqlbh43-momentlabs.vercel.app` · 운영 별칭 반영 완료 |
| 완료 | 오가닉 노출 카드 컴팩트 프리미엄화·링크 분리 | 운영팀·광고주 단건 상품 순위의 노출 카드를 낮고 촘촘하게 재설계하고, 관련 원부를 네이버 민트 톤으로 조정. 상품명은 조회 키워드 검색 결과, `상품 열기`는 기존 상품 상세로 분리 연결 | 전체 `check:quality`, 링크 회귀 기준선, 운영 실조회 48위·관련 원부 7위, 두 역할 데스크톱 카드 127px·이미지 84px, 두 역할 390×844 카드 276px·이미지 72px·`scrollWidth=390`, 콘솔 오류 0건, `/health`·HTML 200 | Production `momentinsight-c4ylvfjb4-momentlabs.vercel.app` · 운영 별칭 반영 완료 |
| 완료 | 운영팀·광고주 사이드바 UI 통일 | 운영팀 콘솔의 `운영 → 키워드·SEO → 순위 조회·추적 → 광고 조사` 분류를 광고주 화면에도 동일 적용. 두 역할의 사이드바 폭, 브랜드 마크, 메뉴 간격, hover·active, 인증 패널을 딥네이비 프리미엄 공통 스타일로 통일하고 모바일 메뉴 순서도 같은 정보 구조로 정렬 | 전체 `check:quality`, 공통 쉘 릴리즈 기준선, 운영팀 1280px·광고주 데스크톱 운영 실화면, 광고주 390×844 운영 실로그인, `scrollWidth=390`, 두 역할 콘솔 오류 0건, 라이브 `/health`·HTML 200 확인 | Production `momentinsight-1eo93tbd5-momentlabs.vercel.app` · 운영 별칭 반영 완료 |
| 완료 | 연속 개발 작업명세·충돌 방지 운영 계약 | 이전 작업 이력을 유지한 채 매 작업의 사전 상태 확인, 오토세이브, 중복 검색, 미커밋 변경 보존, 최소 범위 수정, 2차 검증, 운영 문서 동기화, 논리 단위 커밋을 프로젝트 고정 규칙으로 명시 | 운영 문서 필수 규칙 검색, `check:baseline`, `git diff --check`, 최종 diff·상태 확인 | 배포 없음 |
| 배포 대기 | 네이버 플레이스 한도 초과 폴백·순위 정렬 정상화 | `부평 맛집`/`2019299673` 운영 실패를 Apify 월 사용 한도 초과로 확정. 계정 공통 한도 오류 시 자체 네이버 지도 수집기로 전환하고 Actor 명시 순번을 우선 정렬. 300개 미완주 결과는 확인 범위 이후 미검증으로 표시 | 대상 URL 식별 `팽오리농장 부평점`, 자체 오가닉 54개 확인, 수집기 25개·플레이스 서버 9개·전체 `check:quality` 통과 | Apify 한도 해제 후 300개 완주 검증 필요 |
| 완료 | 네이버 상품 정확 일치 및 동일 판매처 결과 보강 | 판매자 상품 URL을 원부 카탈로그 ID로 자동 승격하던 추론을 제거하고 URL의 상품ID `5145848584`를 정확 일치 기준으로 고정. 같은 원부·다른 상품ID·다른 판매자·광고 상품은 정확 상품으로 집계하지 않으며, 동일 판매처 오가닉 결과는 정확 상품과 별도 상품으로 구분 표시. 공식 API가 광고 위치를 제공하지 않는 한계도 화면에 명시 | `check:quality`, `check:env:naver`, `check:supabase`, 실조회 오가닉 48위·동일 판매처 2건·정확 상품 1건 확인, 관리자/광고주 1440px·390px 브라우저 조회와 콘솔/가로 넘침 검사, Production `/health`·API·관리자 화면 재검증 | 배포 완료 |
| 완료 | 네이버 플레이스 자동추적 20계정 대비 안정화 | Supabase 추적 건을 처리 임대로 원자 선점해 중복 실행을 차단하고, 무료 Render Chromium은 한 번에 1건만 처리하도록 직렬화. GitHub Actions는 오전 9시·오후 3시 처리 창마다 최대 20건을 나눠 호출하며 실패 건은 지수형 재시도, 정상 미노출은 오류가 아닌 유효 결과로 기록. 동일 키워드 후보 목록은 수집기 메모리 캐시로 재사용하고 실제 확인 개수만 응답 | 실제 Supabase 통합 테스트 2회: `구월동 맛집`/`2045794152` 오가닉 1위 저장, `1565776290` 상위 54개 미노출 정상 저장. 두 건 모두 `last_error=null`, 처리 임대 해제, 다음 09시 예약 확인. 수집기 401/429, 서버·baseline·Vercel build·diff 검사 통과 | 배포 진행 |
| 완료 | 네이버 플레이스 Render 통합 검증 | 저장된 플레이스ID가 있으면 단축 URL을 매번 브라우저로 다시 해석하지 않고 지도 목록 조회를 바로 시작하도록 수집 경로를 단축. Vercel API에서 Render 수집기 호출 후 Supabase 스냅샷까지 저장되는 전체 경로 확인 | 운영 API 실조회 `구월동 맛집`/`2045794152` 오가닉 1위, 62개 확인, 34초, `lastError=null`; 광고 카드 ID `1565776290`은 오가닉 62개 내 미노출, 39초, `lastError=null`; Render 릴리스 `2026-07-12-fast-id` 확인 | 완료 |
| 완료 | 플레이스 수집기 무료 서버 안정 모드 | 실제 네이버 지도 목록을 한 번만 끝까지 로딩해 확인 가능한 플레이스ID·오가닉 순위·리뷰 수를 읽도록 변경. Render 무료 인스턴스에서는 장시간 반복 스크롤을 기본 중단해 Vercel 요청 시간 초과를 막고 `checkedCount`에 실제 확인 범위만 기록 | `구월동 맛집` 실조회 ID `2045794152` 오가닉 1위 5.2초, 광고 카드 ID와 미존재 ID 각각 상위 54개 미노출 3~4초, 수집기 문법·baseline·server·rank cron·rank matching·Vercel build·diff 검사 통과 | 배포 진행 |
| 완료 | 플레이스 URL 자동 식별 및 300위 이내 수집 보강 | 상호명 입력 없이 플레이스 URL에서 ID·상호명을 확인하고, 화면 문구를 `300위 이내`로 통일. 기본 검색 Actor가 0건 또는 미인식 출력을 반환하면 올바른 `abotapi` 검색 입력으로 한 번만 대체 호출하며, 실제 고유 오가닉 300개를 확인한 경우에만 완료 처리 | 수집기 19/19, 플레이스 핸들러 6/6, baseline, server syntax, Vercel build, diff 검사 통과. 실제 운영 Actor 300개 완주 여부는 배포 후 실조회로 최종 확인 | 배포 진행 |
| 완료 | 네이버 플레이스 지도 오가닉 수집기 정상화 | 폐기된 지도 iframe 의존을 제거하고 현재 네이버 지도 PC 목록에서 광고를 제외한 실제 장소 ID, 오가닉 순위, 방문/블로그 리뷰 수를 직접 읽도록 Render 수집기를 교체. 외부 응답은 화면에 필요한 필드만 남겨 경량화하고 목록 끝 로딩으로 서버 요청 시간을 단축 | 수집기 문법 검사, `구월동 맛집`/장소ID `2045794152` 실조회 오가닉 1위 일치, 광고 장소ID `1565776290` 미노출, 미존재 장소 미노출, 응답 내부 HTML 제거, 운영 범위 조회 약 7초 확인 | 배포 진행 |
| 부분 완료 | 네이버 플레이스 30일 순위 추적 분리 기반 | 기존 네이버 상품 30일 순위 데이터와 섞지 않도록 플레이스 전용 Supabase 테이블, 서버 API, 크론 API, 광고주 화면 메뉴/입력/기록 UI를 별도로 추가. 외부 순위 공급자가 없을 때는 기존 네이버 OpenAPI 검색 API로 상호명 기준 상위 결과를 확인하며, 공식 API 한계상 300위 전체 추적은 전문 순위 소스 연결 시 확장 | 서버 핸들러 문법 검사, 클라이언트 스크립트 문법 검사, 09시/15시 다음 갱신 계산 확인, `check:server`, `build:vercel` 통과 | 배포 없음 |
| 완료 | 프로젝트 구조 경량화 및 화면 소스 정리 | 아임웹 복붙용 중복 파일과 통합보기 파일을 제거하고, 실제 배포 화면 원본을 `src/pages/home.html`, `src/pages/admin.html`, `src/pages/client.html`로 단순화. 빌드 스크립트와 릴리즈 기준선을 새 구조 기준으로 갱신 | `clean:workspace:dry`, `check:baseline`, `build:vercel`, `git diff --check`, 파일 구조/보안 추적 여부 확인 | 배포 대기 |
| 완료 | 네이버 30일 순위 상태 요약 압축 | 자동추적 상태 영역의 중복 8칸 요약을 1줄 상태바와 3개 핵심 지표로 정리하고, 광고주/운영팀 원본 및 복붙용 파일을 동기화. 기존 baseline도 새 UI 구조 기준으로 갱신 | `check:baseline`, `build:vercel`, `git diff --check`, 로컬 정적 서버 화면 확인 | 배포 대기 |
| 완료 | 순위추적 필터 및 제품 개발 지침 확장 | AGENTS.md에 광고주/운영팀/총관리자 권한, MVP 우선순위, UI/검증 원칙을 상세화. 광고주/운영팀 네이버 30일 순위 화면에 키워드·상품명·상품번호 검색, 전체/확인 필요/상승/하락 필터, 표시 개수 카운터를 추가하고 복붙용 파일까지 동기화. 릴리즈 기준선에 필터 UI/로직 누락 방지 체크 추가 | `check:server`, `check:rank-cron`, `check:rank-matching`, `check:baseline`, `build:vercel`, `git diff --check`, 로컬 HTTP 200, 브라우저 DOM/콘솔 확인 | 배포 진행 |
| 완료 | 순위추적 주말 자동화 검수 및 실패 원인 노출 | 토요일 오후 이후 다음 순위 추적 시간이 일요일 오전 9시로 계산되는지 전용 검사를 추가하고, GitHub Actions 크론에서 `MI_RANK_CRON_SECRET`이 비어 있으면 즉시 명확한 오류로 중단하도록 보강. 실제 실패 원인은 GitHub Actions Secret 누락으로 확인했으며, Vercel Production Secret과 같은 값을 GitHub 저장소 Secret에 등록해야 자동 호출이 성공 | GitHub Actions 실패 로그 확인, `check:rank-cron`, `check:rank-matching`, `check:baseline`, `build:vercel`, `git diff --check`, YAML 문법 검사, 라이브 비인증 401 차단 확인 | 배포 대기 |
| 완료 | 운영팀별 공개데이터 격리 | 관리자와 광고주 화면의 공개데이터 저장소를 운영팀/광고주 코드별로 분리하고, 운영팀 계정에서 총관리자 `mml93-a01` 기본값이나 다른 광고주 공개데이터가 보이지 않도록 수정. 원본 업로드 파일 보관도 코드별로 분리하고 광고주 화면의 코드 검증 전 데모 동기화를 제거 | `check:baseline`, `build:vercel`, `git diff --check`, HTML 스크립트 문법 검사, 로컬 브라우저 관리자/광고주 코드별 분리 마커 확인 | 배포 대기 |
| 완료 | 운영팀 단일 광고주용 엑셀 양식 간편화 | 운영팀 1개당 광고주 1개 연결 기준으로 엑셀에서 광고주 코드 입력을 제거하고, 월간 매출/일별 원천/보고서/일정/인사이트 입력만 남긴 간편 양식으로 재구성. 열 폭과 대시보드 메모 영역을 넓혀 `####`와 글자 눌림을 방지하고 관리자 업로드 화면 정렬 및 기본 양식 다운로드 문구를 보정 | 엑셀 수식/표시 오류 스캔 0건, 사용법/대시보드/월간 입력 렌더링 확인, `check:baseline`, `build:vercel`, `git diff --check` | 배포 대기 |
| 완료 | 운영팀 기본 양식 다운로드 배포 | 운영 시트 템플릿을 `public/downloads` 배포 자산으로 추가하고, 운영 입력 화면에서 새 운영팀이 기본 양식을 먼저 다운로드한 뒤 광고주별 복사본을 업로드하도록 흐름 보강 | `check:baseline`, `build:vercel`, `git diff --check`, 관리자 HTML 스크립트 문법 검사, `public/dist` 템플릿 파일 확인 | 배포 대기 |
| 완료 | 운영 원본 파일 업로드·다운로드 보관 | 운영 입력 화면에 원천 엑셀/CSV/PDF 파일 선택 UI를 추가하고, 업로드한 파일을 브라우저 보관 후 같은 화면에서 다운로드/삭제할 수 있게 정리. 보고서 CSV에는 원본 파일명과 업로드 시간을 함께 기록 | `check:baseline`, `build:vercel`, `git diff --check`, 관리자 HTML 스크립트 문법 검사, 빌드 산출물 마커 확인 | 배포 대기 |
| 완료 | 순위추적 오전/오후 자동 수집 기준 전환 및 카카오 문의 CTA | GitHub Actions 순위추적 호출을 한국시간 오전 9시/오후 3시로 변경하고, 서버 `next_check_at`도 다음 오전/오후 수집 슬롯으로 계산하도록 보정. 홈페이지/광고주/운영팀 화면 우측 하단에 모먼트인사이트 카카오톡 채널 문의 버튼 추가. 실제 자동 호출은 GitHub Secret `MI_RANK_CRON_SECRET` 등록 후 활성화 | `check:baseline`, `build:vercel`, `git diff --check`, HTML 스크립트 문법 검사, 빌드 산출물 마커 확인 | 배포 대기 |
| 완료 | 운영 헬스체크·순위추적 자동화 보강 | `/health`를 `/api/health`로 연결하고, Vercel Hobby 제한 때문에 Vercel Cron 선언은 제거했습니다. 보안된 `/api/naver-rank-cron` 엔드포인트는 유지해 GitHub Actions가 오전 9시/오후 3시에 호출할 수 있게 준비했습니다. 순위추적 카드에는 상태, 최근 갱신, 다음 갱신, 마지막 메시지를 표시 | `check:baseline`, `build:vercel`, `git diff --check`, HTML 스크립트 문법 검사, 라이브 `/health`·cron 인증 차단·화면 마커 확인 | 배포 대기 |
| 완료 | 주요 CTA 버튼 상태 피드백 통일 | 조회, 저장, 연결 확인, 생성 버튼 등 공통 CTA에 hover, active, focus-visible, disabled 상태를 추가해 클릭감과 로딩 중 대기 상태를 명확히 표시 | `check:baseline`, `build:vercel`, `git diff --check`, HTML 스크립트 문법 검사, 빌드 산출물/라이브 마커 확인 | 배포 대기 |
| 완료 | 순위추적 조작 피드백 보강 | 순위추적 삭제/갱신 버튼과 드래그 핸들에 hover, active, focus-visible, disabled 상태를 추가하고 삭제 진행 중에는 `삭제 중` 상태가 보이도록 정리 | `check:baseline`, `build:vercel`, `git diff --check`, HTML 스크립트 문법 검사, 빌드 산출물/라이브 마커 확인 | 배포 대기 |
| 완료 | 보고서 정책 통일 및 순위추적 롤링 보정 | 보고서 문구를 운영팀 검수 후 공개, 광고주 보고서함 다운로드 방식으로 통일하고 네이버 순위추적은 30일 뒤 종료하지 않고 계속 추적하되 최근 30일 기록만 표시하도록 서버 로직 보정 | `check:baseline`, `build:vercel`, `git diff --check`, 서버/HTML 문법 검사, 빌드 산출물/라이브 마커 확인 | 배포 대기 |
| 완료 | 광고주 공개 보고서함 다운로드 복구 | 광고주 대시보드의 보고서 전달 현황을 공개 보고서함으로 정리하고, 운영팀이 공개한 reports 데이터만 CSV로 다운로드할 수 있게 버튼과 다운로드 핸들러 추가 | `check:baseline`, `build:vercel`, `git diff --check`, HTML 스크립트 문법 검사, 빌드 산출물/라이브 마커 확인 | 배포 대기 |
| 완료 | 광고주 화면 접속 유형 선택 보강 | 광고주 로그인 화면에서도 광고주/운영팀을 클릭 선택할 수 있게 하고, 운영팀 선택 시 운영팀 코드 접속 화면으로 이동하도록 연결 | `check:baseline`, `build:vercel`, `git diff --check`, 로컬 HTTP 200 및 빌드 산출물 마커 확인 | 배포 대기 |
| 완료 | 총관리자 관계도 및 운영팀 로그인 선택 UX | 활성 계정 전체보기를 `총관리자 mml93-a01` 아래 운영팀-광고주 연결 관계형 목록으로 재구성하고, 로그인 첫 화면을 광고주/운영팀 클릭 선택형으로 정리 | `check:baseline`, `build:vercel`, `git diff --check`, 로컬 HTTP 200 및 빌드 산출물 마커 확인 | 배포 대기 |
| 완료 | 권한 해제 후 동일 코드 재발급 보정 | 광고주/운영팀 광고주 코드가 권한 해제 후 `paused`로 남아 있어도 같은 코드 입력 시 신규 insert가 아니라 기존 행을 `active`로 재활성화하도록 보정. Edge 함수의 광고주 코드 검증도 active만 허용하도록 정리 | 실제 로컬 API DB 상태 전이 2회 반복 검수, `check:baseline`, `build:vercel`, `git diff --check` | 배포 대기 |
| 완료 | 활성 계정 목록 전체보기 분리 | mml93-a01 활성 계정 카드는 요약/미리보기만 남기고, 운영팀·광고주 전체 목록은 별도 전체보기 화면에서 번호형 슬롯으로 관리하도록 분리. 전체보기에서도 권한 해제 가능 | `check:baseline`, `build:vercel`, `git diff --check`, HTML/JS 정적 검사 | 배포 대기 |
| 완료 | 릴리즈 기준선 검사 추가 | 향후 기능 추가 시 기존 관리자/광고주/총관리자/순위추적 기준이 깨지면 바로 실패하도록 `check:baseline` 자동 검사를 추가 | `check:baseline`, `build:vercel`, `git diff --check`, 라이브 HTML/API 확인 | 배포 대기 |
| 완료 | 총관리자 계정 권한 확장 | `mml93-a01` 총관리자가 운영팀 없이 광고주 코드를 직접 발급할 수 있게 UI/API 호출을 분기하고, 총관리자 순위추적은 서버와 DB 트리거 모두 50개 제한 예외로 처리 | `build:vercel`, `git diff --check`, 서버 문법 검사, 원본/복붙/빌드 산출물 정적 검사, 신규 Supabase 마이그레이션 확인 | 배포 대기 |
| 완료 | 관리자 UI/UX 권한 문맥 보강 | 관리자 화면 상단에 현재 권한 모드(총관리자/운영팀)를 표시하고, 대행사 코드 화면에 Owner/운영팀 역할 차이와 광고주 1개 제한 안내를 추가. 운영팀 전체 메뉴 접근은 유지 | `build:vercel`, `git diff --check`, 원본/복붙/빌드 산출물 메뉴 10개 확인, 권한 문구/Owner 전용/권한해제 코드 유지 확인, 로컬 HTTP 200 확인 | 배포 대기 |
| 완료 | 운영팀 메뉴 과잠금 복구 | 운영팀 로그인 시 전체 관리자/도구 메뉴가 `대행사 코드` 하나로 제한되던 과도한 잠금을 제거하고, 기존 메뉴 접근은 유지하되 Owner 전용 코드 발급 블록만 숨기도록 복구 | `build:vercel`, `git diff --check`, 빌드 산출물 메뉴 10개 확인, 메뉴 제한 코드 제거 및 Owner/권한해제 코드 유지 확인 | 배포 대기 |
| 완료 | Owner/운영팀 코드 생성 화면 분리 | `mml93-a01`에서만 운영팀 코드 생성/활성 계정 전체 조회 화면을 노출하고, 운영팀은 광고주 1개 생성과 연결 광고주 권한 해제만 가능하도록 UI 모드를 분리 | `build:vercel`, 원본/복붙/빌드 동기화 검색, 로컬 모킹 API 운영팀 로그인/광고주 1개 제한/Owner 화면 노출 검증, 운영팀 권한 해제 API 검증, 라이브 HTML 반영 확인 | 배포 완료 |
| 완료 | 운영팀 코드 로그인 및 재활성화 보정 | 운영팀 코드 검증 API를 추가하고, 운영팀 코드로 관리자 화면 진입 후 광고주 1개 생성이 가능하도록 UI 권한 흐름을 분리. revoked 운영팀 코드는 같은 코드로 재활성화되도록 수정 | `build:vercel`, 서버 함수 모의 DB 테스트, 로컬 브라우저 모의 API 로그인/광고주 생성 테스트, 라이브 HTML/API 읽기 검증, 원본/복붙/빌드 동기화 확인 | 배포 완료 |
| 완료 | 운영팀 선생성 안내 및 광고주 코드 사전차단 | 활성 운영팀이 없을 때 광고주 발급용 운영팀 코드를 비우고, 먼저 운영팀 코드를 만들도록 안내. 임의 운영팀 코드 입력 시 서버 오류 전에 UI에서 차단 | 라이브 API activeTeams=0 확인, `build:vercel`, 원본/복붙 파일 동기화 검색, 로컬/라이브 브라우저 로그인/목록/사전차단 검증 | 배포 완료 |
| 완료 | 총관리자 비밀값 분리 | `MI_SUPER_ADMIN_CODE`를 Vercel Production 민감 환경변수로 설정하고, 관리자 UI의 mml93-a01 자동 비밀값 입력 fallback 제거 | Vercel env 확인, 라이브 API 401/200 권한 테스트, `build:vercel`, 구버전 자동 입력 로직 검색 | 배포 대기 |
| 완료 | 광고주 코드 비노출 및 순위추적 권한 보강 | 광고주 대행사 연결 화면과 공개 검증 API에서 운영팀/대행사 코드 노출을 제거하고, 표시용 대행사명과 연동 상태만 노출하도록 정리. 순위추적은 active 광고주와 동일 접근 코드 조합만 허용 | `build:vercel`, `check:env`, 정적 산출물 코드 노출 검색, 로컬/라이브 API validate/401/200 테스트, 브라우저 로그인/비노출/새로고침 잠금 확인 | 배포 완료 |
| 완료 | 운영팀/광고주 코드 관리 간소화 | 관리자 화면에서 운영팀 코드와 광고주 코드를 바로 생성하고, mml93-a01에서 활성 계정 조회 및 권한 해제를 할 수 있도록 서버/API/UI 보완 | `build:vercel`, `check:env`, 로컬 API 401/200/400/403 JSON 테스트, 브라우저 로그인/목록/빈값 검증 | 배포 완료 |
| 완료 | 광고주 코드 입력 잠금 | 광고주 페이지 자동 인증 복구를 제거하고 진입 시 기존 코드/순위 접근 코드를 초기화, API 요청 fallback 적용 | `build:vercel`, 로컬 브라우저 잠금/입장/새로고침 재잠금/무효 코드 거부 확인 | 배포 완료 |
| 완료 | 메인 계정 코드 발급 범위 고정 | 구버전 직접 기준값을 제거하고 `mml93-a01` 메인 계정 코드 입력칸과 서버 검증을 추가 | `build:vercel`, 총관리자 API 403/200/409 테스트, 잔여 코드 검색 | 배포 없음 |
| 완료 | 계정 코드 및 순위추적 보존 하드닝 | 기본 운영 코드를 `mml93-a01`로 통일하고, 관리자/광고주 로그인 잠금, 총관리자 코드 발급 API, 순위추적 50개 제한을 보완 | `build:vercel`, `check:env`, API 401/200/409 권한 테스트, 광고주/관리자 로그인 브라우저 확인, 민감키 패턴 검색 | 배포 없음 |
| 완료 | 순위 추적 접근코드 분리 및 배포 | `agencyCode`를 비밀값처럼 쓰지 않도록 순위 추적 조회/쓰기 접근 코드를 분리하고 GitHub push 기반 Vercel 배포 준비 | `build:vercel`, `check:env`, API 401/200/404 권한 테스트, preflight 확인, 민감키 패턴 검색 | 배포 대기 |
| 완료 | 순위 추적 API 권한 잠금 | `/api/naver-rank-trackers` 기본 코드 fallback 제거, POST 쓰기 작업 코드 검증, `run-due` 크론 전용 분리, 순위 응답 내부 식별자 축소, 로컬 미리보기 CORS 헤더 보완 | `build:vercel`, API 401/403 차단 응답, 소스/복붙용 diff, 정적 산출물 확인 | 배포 없음 |
| 완료 | UI/UX E2E 1차 개선 | RTF 개발 지시서 기준으로 첫 진입, 사용자 흐름, CTA, 빈/오류 상태, 반응형을 우선 점검 및 개선 | `build:vercel`, 소스/복붙용 diff, 로컬 브라우저 홈·광고주·관리자 입력 상태 확인 | 배포 없음 |
| 완료 | 네이버 상품 순위 페이지 오표기 제거 | 공식 쇼핑 검색 API 결과 배열의 168번째 항목을 실제 네이버 쇼핑 `5페이지 8위`로 환산하던 로직을 제거. 판매자 상품ID 일치는 유지하되 화면과 API 응답을 `공식 API 결과 순번·실제 화면 위치 미검증`으로 명확히 구분 | 운영/로컬 API 실조회, `check:rank-matching`, `check:baseline`, `check:quality` 2회, 운영 관리자 브라우저, `git diff --check` | Production `momentinsight-1z4jt31ot-momentlabs.vercel.app`, 운영 별칭 반영 |
| 완료 | 네이버 오가닉 순위 기준 보정 | 광고 제외 오가닉 기준과 40개 단위 전체 순위 계산 적용 | `check:rank-matching`, `build:vercel`, 생성 HTML 문구 확인 통과 | 배포 없음 |
| 완료 | 홈페이지 메인 문구 정리 | 첫 화면 H1을 `모먼트 인사이트`와 `마케팅 사이트`로 분리해 가독성 개선 | `build:vercel`, 로컬 브라우저 미리보기 확인 | 배포 없음 |
| 완료 | 네이버 SEO 순위/설명 보정 | 상품 URL/상품ID 정확 매칭으로 순위 오판 방지, SEO 부족점 설명 구체화 | `check:rank-matching`, `check:env:naver`, `build:vercel` 통과 | 배포 없음 |
| 완료 | 프로젝트 구조 정리 | 문서, 서버, Supabase, 아임웹 코드 폴더를 단일 개발 폴더로 정리 | Git 관리 확인 | 이전 배포 반영 |
| 완료 | 홈페이지형 메인 | 고객용 첫 화면, 관리자/광고주 진입 구조, 검색 메타 태그 구성 | Vercel 빌드 확인 | 이전 배포 반영 |
| 완료 | 광고주 대시보드 | 광고주 전용 대시보드, KPI, 보고서, 일정, 키워드 조회 화면 구성 | 화면 및 빌드 확인 | 이전 배포 반영 |
| 완료 | 관리자 대시보드 | 광고주 데이터 입력, 공개 데이터 관리, 광고주 화면 미리보기 구성 | 화면 및 빌드 확인 | 이전 배포 반영 |
| 완료 | 네이버 키워드 조회 | 검색광고 API, 데이터랩, 쇼핑 검색 API 기반 키워드 데이터 조회 | 서버 문법 및 API 연결 확인 | 이전 배포 반영 |
| 완료 | 네이버 상품 순위 추적 | 상품 URL/상품ID 기준 순위 추적, 오전 9시/오후 3시 기록 구조, 드래그 정렬/삭제 | 로컬/라이브 화면 확인 | 이전 배포 반영 |
| 완료 | 네이버 SEO 확인 | 키워드, 상품 URL, 리뷰수 기반 상품 노출 준비도 점수 화면 구성 | 빌드 확인 | 이전 배포 반영 |
| 완료 | 홈페이지 SEO 키워드 정리 | 검색 타깃을 `마케팅 사이트`, `모먼트 인사이트`로 정리하고 `모먼트랩스` 제거 | 라이브 메타 확인 | 이전 배포 반영 |
| 완료 | 작업 폴더 정리 | `clean:workspace` 스크립트로 재생성 가능한 산출물 정리 | 정리 명령 및 빌드 확인 | 배포 없음 |
| 완료 | 작업명세 오토세이브 도입 | 개발 내역을 작업명세서로 관리하고 완료 후 로컬 커밋까지만 진행하는 운영 규칙 추가 | `work:autosave`, 문법 검사, 정리 명령 확인 | 배포 없음 |
| 완료 | 30일 순위 그룹 이동 결함 보정 | 그룹명 입력 후 체크 시 입력값이 사라지는 문제를 막고, 선택 항목 없이 그룹 생성 성공처럼 보이던 흐름을 차단했습니다. 관리자/광고주 30일 순위 화면 모두 동일 적용 | HTML 스크립트 문법 검사, 로컬 API 그룹 이동/복구 테스트, `check:baseline`, `check:rank-matching`, `check:rank-cron`, `build:vercel`, `git diff --check` | 배포 없음 |
| 완료 | 30일 순위 자동 갱신 누락 방지 | GitHub Actions 예약 실행을 09시/15시 슬롯 전용에서 30분 간격 due catch-up 방식으로 변경하고, Vercel 1일 1회 백업 Cron도 09:07 KST로 조정했습니다. 서버는 기존처럼 `next_check_at`이 지난 항목만 실제 갱신하므로 불필요한 네이버 조회를 늘리지 않습니다. | GitHub Actions schedule API 조회, 운영 tracker 최신 갱신/다음 갱신 확인, `check:rank-cron`, `check:baseline`, `check:rank-matching`, `build:vercel`, `git diff --check` | 배포 대기 |
| 완료 | 자동 순위추적 운영 환경값 분리 점검 | GitHub Actions 호출용 `MI_RANK_CRON_SECRET`과 Vercel Cron 자동 Authorization용 `CRON_SECRET`을 별도 필수 운영값으로 분리했습니다. 두 값은 같은 비밀값이어야 하며, Vercel Hobby의 1일 1회 백업 Cron이 401로 막히는 실수를 배포 전 검사에서 잡도록 보강했습니다. | `check:baseline`, `check:rank-cron`, `build:vercel`, 운영 cron 비인증 401 확인 | 배포 대기 |
| 완료 | 30일 순위 그룹 필터 UX 보완 | 그룹 필터를 `그룹 보기` 컨트롤로 명확히 표시하고, 선택 그룹이 있을 때만 네이비 강조 상태가 보이도록 수정했습니다. 그룹 이동 후에는 특정 그룹만 남아 보이지 않게 전체 그룹으로 자동 복귀합니다. | `check:baseline`, `check:rank-matching`, `build:vercel`, DOM 기능 테스트, 운영 페이지 응답 확인 | 배포 대기 |
| 완료 | 로그인 유지 및 개발중 안내 보강 | 관리자/광고주 코드 로그인은 새로고침 후에도 저장된 코드로 서버 재검증을 거쳐 유지되도록 보정. 코드 입력 화면 상단에 홈페이지 이동 로고를 추가하고, 메타 광고 조사는 메뉴와 화면에서 개발중으로 명확히 표시. 홈페이지 첫 화면에는 현재 사용 가능한 핵심 도구를 작은 개발중 배너로 안내 | `check:baseline`, `check:rank-matching`, `check:rank-cron`, `build:vercel`, `git diff --check`, 로컬 정적 서버 마커 확인 | 배포 대기 |
| 완료 | 운영 메뉴 IA 및 플레이스 30일 순위 명칭 정리 | 보고서 관리를 운영 카테고리로 이동하고 운영/키워드·SEO/순위 조회·추적/광고 조사로 재분류. 관리자·광고주 화면의 플레이스 명칭을 `네이버 플레이스 30일 순위`로 통일하고 구현 설명과 반복 문구를 축약. 플레이스 입력은 데스크톱 1행, 모바일 1열로 고정 | `check:baseline`, `check:server`, `check:rank-cron`, `check:rank-matching`, `check:keyword-trend`, `build:vercel`, `git diff --check`, 1280px·390px 브라우저 시각/오버플로/메뉴 이동 확인 | 배포 승인 |
| 완료 | 서비스 안내·운영 오류 안전화·품질 게이트 | 홈페이지 서비스 안내를 안정화된 5개 도구 기준으로 갱신하고 네이버 플레이스 30일 순위를 추가. 운영 API의 500 오류에서 DB 오류·환경변수명이 노출되지 않도록 응답을 정리하고, PR/main 변경마다 전체 품질 검사를 실행하는 GitHub Actions를 추가 | `check:quality`, API 오류 안전화 4개 테스트, 플레이스 API 8개 및 수집기 22개 테스트, Supabase RLS/추적 예약 조회, 1280px·390px 브라우저 시각·오버플로 확인 | 배포 대기 |

## 현재 진행 원칙

현재부터 새 작업은 아래 순서로만 처리합니다.

1. `git status`, 최근 커밋, 작업명세, 작업 상태, 다음 작업 확인
2. `npm run work:autosave` 실행 후 작업명세서에 항목 추가
3. `rg`로 기존 구현·문서·테스트 검색 및 중복 여부 확인
4. 기존 미커밋 변경을 보존하며 최소 범위로 코드 또는 문서 수정
5. 대상 테스트와 다른 관점의 2차 검증 실행
6. 작업명세와 핵심 운영 문서에 완료·보류·검증 근거 동기화
7. `git diff --check`, 최종 diff, Git 상태 확인
8. 한 작업 단위로 로컬 커밋
9. 현재 승인과 필수 검증이 모두 있을 때만 `git push`와 배포 실행

## 프로젝트 고정 기억

앞으로 작업을 이어갈 때 아래 기준은 다시 흔들지 않는다. 새 기능을 추가하더라도 기존 로그인, 권한, 데이터 격리, 순위추적, 보고서 운영 흐름을 깨지 않는 범위에서만 수정한다.

### 계정과 권한

- 총관리자 계정은 `mml93-a01`이다.
- 총관리자는 운영팀 코드 생성, 광고주 직접 생성, 활성 계정 조회, 권한 해제를 담당한다.
- 운영팀은 계정당 광고주 1명만 연결한다.
- 광고주는 운영팀 또는 총관리자가 발급한 광고주 코드로만 접속한다.
- 광고주 화면에는 운영팀 코드, 내부 메모, API 기준, 비밀값, 내부 오류 상세를 노출하지 않는다.
- 로그아웃 기능은 유지하고, 새로고침 시에는 저장된 코드로 서버 재검증 후 세션을 복구한다.

### 현재 사용 가능 도구

- 네이버 상품 단건·30일 기능의 현재 코드 경계는 독립 수집기의 `mi.naver-shopping-organic-window.v1`이다. 종료된 쇼핑 검색 API 결과 순번은 신규 순위 소스로 사용하지 않는다.
- Meta 광고 조사는 메뉴와 화면에서 `개발중`으로 표시한다.
- 네이버 플레이스 순위 추적은 상품 순위와 분리된 별도 기능으로 다룬다.
- 외부 API 기준과 내부 계산식은 광고주 화면에 설명하지 않는다.

### 네이버 순위 추적

- 네이버 상품 숫자는 한 `collectionId`·`collectedAt`에서 검증된 `naver_shopping_organic_list`의 광고 제외 연속 순번이다. 300위 성공은 전용 워커가 요청 범위를 완주하고 최근 `pw-*`, `checked_count=300` 원자 snapshot으로 증명해야 한다.
- 페이지와 페이지 내 위치는 이 오가닉 순번을 40개 기준으로 파생하되, 개인화된 사용자 화면을 별도 검증한 값으로 주장하지 않는다.
- 판매자 상품 URL은 URL에 포함된 상품ID와 판매자가 모두 일치할 때만 정확 상품으로 처리한다.
- 원부 카탈로그 URL을 명시적으로 입력한 경우에만 원부 순위 모드를 사용하며, 같은 원부라도 다른 상품ID 또는 다른 판매자는 정확 상품으로 승격하지 않는다.
- 30일 순위 추적은 30일 후 중단하지 않는다. 계속 수집하고 화면에는 최근 30일만 보여준다.
- 자동 갱신 기준은 매일 오전 9시와 오후 3시다.
- 자동 갱신 누락 방지를 위해 GitHub Actions와 Vercel 백업 Cron을 함께 점검한다.
- 선택 이미지 저장, 그룹 보기, 그룹 생성 및 이동, 그룹 해제는 기존 추적 데이터가 날아가지 않도록 보존 우선으로 수정한다.

### 네이버 API·상품 수집 기준

- NAVER API Hub의 Search·Search Trend·Shopping Insight는 각 지원 지표에만 사용하며 N 상품 오가닉 순위로 해석하지 않는다.
- 네이버 Developers의 쇼핑 검색 API는 API Hub 이관 제외 상태로 2026-07-31 종료됐다. 현재 순위 경로는 종료 API가 아닌 검증된 독립 수집 계약만 허용한다.
- 키워드 조회와 상품 순위 관련 환경변수는 배포 환경변수로만 관리하고, 키 값은 코드와 문서에 남기지 않는다.
- 네이버 플레이스 300위 전체 추적은 공식 검색 API만으로 완전 구현되지 않는다. 별도 공급자를 연결할 때도 실제 PC 오가닉 목록과 동일한 source·rankEvidence가 검증되지 않으면 운영 순위로 저장하지 않는다.

### 보고서와 운영 입력

- 주간보고서는 운영 부담이 크므로 기본 운영 범위에서 제외한다.
- 운영팀은 월간 기준으로 매출, 광고비, 노출, 클릭, 전환율, 전체 구매수, 전체 매출을 입력한다.
- 월간 합계는 일별 입력에서 자동 집계되도록 설계한다.
- 보고서는 엑셀 원본 입력값과 운영팀이 작성한 로드맵/액션플랜을 바탕으로 PPTX 또는 PDF 산출물로 확장한다.
- 로드맵과 액션플랜은 AI가 임의로 계산하는 값이 아니라 운영팀이 직접 작성하는 운영 판단 영역이다.

### UI/UX 기준

- 전체 톤은 딥네이비, 화이트, 그레이 중심의 B2B SaaS 프리미엄 톤을 유지한다.
- 글이 많아지면 먼저 줄이고, 다음으로 접기/더보기/상태 배지로 정리한다.
- 긴 상품명, 코드, 검색량, 순위는 한 줄 정렬과 말줄임을 우선하고 레이아웃 겹침이 생기면 배포하지 않는다.
- 버튼 hover, active, disabled, focus-visible 상태는 기본 품질로 유지한다.
- 카카오 문의 버튼과 개발중 안내 배너는 안내형 톤으로 두고, 위협적이거나 저렴해 보이는 표현은 피한다.

### 전문 개발사·UI/UX 협업 기준

- 대표님의 요청은 핵심 요구로 반영하되, 그대로 구현하는 것이 최선인지 개발·제품·UI/UX 관점에서 독립적으로 검토한다.
- 검토 기준은 실제 운영 목적, 사용자 역할별 흐름, 데이터 진실성, 보안, 접근성, 반응형, 정보 밀도, 유지보수성과 기존 기능 회귀 위험이다.
- 더 나은 대안이 있거나 요청안에 위험이 있으면 근거·영향·권고안을 팩트 중심으로 먼저 설명한다. 단순 동의나 취향 위주의 변경은 하지 않는다.
- 디자인은 정보 계층, 여백, 정렬, 타이포그래피, 상태·오류 표현, CTA, 데스크톱·모바일 실사용성을 함께 검수해 전문 B2B SaaS 품질을 유지한다.
- 대표님의 최종 결정권을 존중하되 요청 범위를 넘어서는 변경은 임의 적용하지 않는다. 승인된 범위 안에서 최소 수정과 충분한 검증으로 구현한다.
- 완료 보고에는 사용자 요청 반영 사항과 개발사 독립 권고를 구분하고, 검증된 사실과 남은 위험을 숨기지 않는다.
- 2026-07-28 원칙 고정 코드 `874a676`을 Production `dpl_NsxhroTpR5enjVvMi4BkHb92cVDf`에 반영했다. 운영 릴리스 `874a6763fa2b`·서울 `icn1`·Supabase ready, 양 역할 화면 200·비인증 세션 401을 확인했다.

### 검증과 배포

- 성공 보고 전에는 `work:autosave`, 서버 문법 검사, 빌드, 핵심 API 또는 화면 마커 검증을 실행한다.
- 검증하지 못한 항목은 성공으로 보고하지 않는다.
- 대표님이 배포 승인한 경우에만 push와 배포를 진행한다.
- 배포 전후로 `git status`, 최신 커밋, 라이브 `/health`, 핵심 API 200/401 응답을 확인한다.

### 2026-07-12 플레이스 URL 전용 300위 보강

- 상호명 입력 없이 키워드와 네이버 플레이스 URL만 등록한다.
- 화면의 조회 범위는 `300위 고정`이 아니라 `300위 이내`로 통일한다.
- 기본 Actor가 비정상 응답 또는 300개 미만 미노출을 반환하면 300개 수집 Actor로 재조회한다.
- 메인 API는 300개 수집기의 225초 실행 예산보다 먼저 요청을 취소하지 않는다.
- 실제 300개 확인 전에는 `300위 이내 없음`으로 확정하지 않고 부분 확인 상태로 남긴다.
- 짧은 `naver.me` URL은 HTTP 리다이렉트와 페이지 메타데이터에서 플레이스 ID와 상호명을 자동 확인한 뒤 순위를 조회한다.
- 300개 전용 수집 Actor의 `PlaceId`, `Name`, `NaverMapUrl` 출력도 동일한 플레이스 후보로 정규화한다.
- 로컬 수집기 테스트 22개, 플레이스 핸들러 테스트 6개, Vercel 빌드와 릴리스 기준 검사를 통과했다.
- 운영 실조회에서 기존 0건 항목은 오가닉 57위, 기존 50개 부분 조회 항목은 오가닉 81위로 확인되었고 두 기록 모두 `checkedCount=300`, `total=300`으로 저장됐다.
- 짧은 `naver.me` URL만 전달한 등록 요청도 플레이스 ID와 상호명을 자동 식별해 기존 추적 기록과 정확히 연결했다.

## 다음 후보 작업

| 상태 | 작업 | 판단 |
| --- | --- | --- |
| 대기 | 네이버 SEO 확인 v2 | 점수 기준을 `네이버 상품 노출 준비도`로 재정의하고 경쟁상품 비교, 신뢰도 표시, 순위 변화 검증을 추가 |
| 대기 | 순위 추적 DB 하드닝 | `client_id` nullable, `agency_code` FK 부재, 광고주 공개 view 부재는 Supabase 마이그레이션으로 별도 보완 필요 |
| 대기 | 코드 로그인 세션 전환 | 예측 가능한 운영팀·광고주 코드를 데이터 API의 실질 인증값으로 사용 중이다. 기존 계정 접근을 끊지 않도록 Supabase Auth 또는 서버 발급 단기 세션으로 단계 전환하고 코드 검증 rate limit을 함께 도입해야 함 |
| 대기 | 아임웹 원샷 코드 모듈화 | 관리자/광고주/홈페이지 HTML 중복을 줄이고 생성 스크립트 기준으로 관리 |
| 대기 | 원격 배포 이력 정리 | Vercel에서 삭제할 배포 URL을 대표님이 지정하면 해당 배포만 정리 |

### 2026-07-22 의료 검색 느린 프레임 fail-closed 보강

- 운영 v17에서 Render의 느린 프레임 연결이 6초를 넘으면 네이티브 `hospital/list`를 놓치고 일반 `place/list`로 재진입하는 2차 원인을 확인했다.
- 네이티브 목록 대기를 20초로 늘리고 중립 검색을 1회 재시도한다. 두 번 모두 목록 URL이 없으면 `naver_map_native_list_frame_not_found`로 중단해 잘못된 순위를 저장하지 않는다.
- 플레이스 수집기 49/49와 전체 `npm run check:release`를 통과했고, 로컬 실조회는 정확 ID `1531240094` 기준 `종로3가한의원` 3위·`종로한의원` 10위다.
- `admin.html`, `client.html`, Supabase 스키마와 기존 30일 이력은 변경하지 않는다.
- 운영 v18 재실행에서 `종로3가한의원` 3위·`종로한의원` 10위가 새 snapshot으로 저장됐다. 두 건 모두 정확 ID `1531240094`, 광고 제외 네이버 PC 목록 70개, 오류 없음·재시도 0을 확인했다.

### 2026-07-22 키워드 수요·상품 공급 비례 경쟁강도

- 대표 키워드는 검색량과 쇼핑 상품 등록 규모가 함께 클수록 포화 경쟁을 강하게 반영한다.
- 검색량이 충분하면서 상품 등록이 적은 키워드는 수요 대비 공급 공백이 있는 저경쟁·고기회 SEO 후보로 분류한다.
- 종합 경쟁강도는 `검색수요×상품규모`, `수요 대비 상품밀도`, `검색광고 경쟁도`를 함께 사용하며 판매 기회율은 실제 매출 전환율로 표현하지 않는다.
- 검색광고 원본 경쟁도는 연관 키워드 참고표에 보존하고, 조회 요약·판단 메모·SEO 점검·추천 작업은 종합 경쟁강도를 사용한다.
- 운영팀과 광고주 화면의 동일 적용을 릴리스 기준선으로 고정했다. N 상품·N 플레이스 30일 순위 코드는 변경하지 않았고 보호 잠금 검사를 통과했다.
- `써큘레이터` 기준 71,400회·상품수 약 340,000개는 경쟁강도 83 `매우 높음`, 동일 수요·상품수 20,000개 시뮬레이션은 경쟁강도 32 `낮음`·판매 기회율 87 `매우 높음`으로 검증했다.
- 전체 `npm run check:release`에서 API·서버 169/169, 플레이스 수집기 51/51, 서버 계약 23/23, Production 인증 18/18, 공개 빌드 CSP 검사를 통과했다.
- 커밋 `c380eb7`과 Production `dpl_CVzoKZ3NnnkXEuNMM7D2MyEKczVu`를 운영 별칭에 반영했다. 라이브 릴리스 `c380eb7622c1`에서 `써큘레이터` 71,400회·상품수 348,978개·경쟁강도 83 `매우 높음`·판매 기회율 69 `높음`, 동일한 조회 요약과 포화 키워드 추천 문구를 재확인했다.

### 2026-07-28 대행사 연결 화면 역할 정리

- 요청: 대행사 연결의 공개 데이터 설정·중복 연결 상태·내부 권한 구조·공개 기준 안내를 정리하고 검증 후 배포한다.
- 판단: `공개 데이터 설정`은 실제 저장 훅이 연결된 유일한 입력 영역이므로 삭제하지 않고 `공개 관리`로 이동한다. 대행사 연결에는 운영팀·광고주 발급과 활성 계정 관리만 남긴다.
- 보존 범위: 공개 데이터·메모 저장 기능, 계정 API·DB, `client.html`, 보호된 키워드·SEO·상품/플레이스 순위 조회·추적 기능은 변경하지 않는다.
- 검증: 브라우저 1280×720 대행사 연결·공개 관리 육안 확인, 역할 5상태, 운영팀·광고주 parity, 보호 잠금, 기준선, API·서버 226/226, 플레이스 51/51, 서버 계약 28/28, Production 인증 18/18과 전체 `check:release`를 통과했다.
- 배포: 코드 `72e1576`을 GitHub `main`과 Vercel Production `dpl_3CwNgQnXgkJMBNS5K9zWLD3hbZWs`에 반영했다. 운영 릴리스 `72e157695c52`·서울 `icn1`·Supabase ready, 관리자·광고주 HTML 200과 배포 관리자 HTML의 역할 분리를 확인했다.

### 2026-07-28 대행사 연결 활성 계정 집계형 보완

- 요청: 생성 카드의 `선택 01`·`필수 02` 문구를 제거하고 활성 계정은 개별 행 대신 운영팀·광고주 운영 수만 표시한 뒤 배포한다.
- 구현: 대행사 연결 요약은 기존 API의 활성 배열 길이를 사용해 `운영팀 N팀 운영 중`·`광고주 N곳 운영 중` 2행만 표시한다. 상세 연결·권한 해제용 `전체보기`는 유지한다.
- 보존 범위: 계정 생성·해제 API와 DB, 광고주 페이지, 보호된 키워드·SEO·상품/플레이스 순위 조회·추적 기능은 변경하지 않는다.
- 검증: 1280×720 배지 0건·요약 2행·개별 행 0건·가로 넘침 0, 역할 5상태·양 역할 parity·보호 잠금·CSP·API/서버 226/226·플레이스 51/51·서버 계약 28/28·Production 인증 18/18과 전체 `check:release`를 통과했다.
- 배포: 코드 `e29339f`을 GitHub `main`과 Vercel Production `dpl_8he7h77pd7Zct8k4h92JsSdxPdRD`에 반영했다. 운영 릴리스 `e29339f8a0b1`·서울 `icn1`·Supabase ready, 양 역할 HTML 200·배포 관리자 집계형 마커·상세 권한 관리 보존·CSP 해시를 확인했다.

### 2026-07-30 업무 운영 워크플로 1차 개발

- 상태: 로컬 구현·자동 검증 완료 / 배포 대기
- 목표: 운영팀이 광고주 연결 전에도 내부 업무를 등록·관리하고, 각 업무에서 `광고주에게 일정 공개`를 선택한 항목만 광고주 일정표에 노출한다.
- 1차 범위: 월간 캘린더, 오늘·예정 업무, 업무 등록·수정·삭제, 담당자·상태·우선순위·내부 메모, 광고주 선택 공개, 역할별 서버 저장·조회.
- 공개 계약: 기본값은 내부 비공개이며 광고주에게는 공개 제목·일정·상태·공개 안내만 반환한다. 내부 메모·운영팀 코드·다른 광고주 데이터는 반환하지 않는다.
- 계정 계약: 광고주가 없는 운영팀도 업무 운영을 사용할 수 있다. 광고주가 연결된 뒤에만 해당 업무의 공개 전환을 허용한다.
- 제외 범위: 자유 블록 편집기, 업무 템플릿, 광고주 요청함, 반복 일정·자동 알림, 성과 데이터 자동 연결은 이번 1차에서 구현하지 않는다.
- 보존 범위: 키워드 조회, SEO 확인, N 상품 순위, N 30일 순위, N 플레이스 30일 순위의 수집·계산·저장·화면 코드는 변경하지 않는다.
- 배포: 현재 요청은 개발 시작이며 Production 배포 승인은 별도로 확인한다.
- 검증 결과: 업무 전용 5/5, 서버 계약 29/29, API·서버 232/232, 플레이스 51/51, 역할·양 화면 parity, 보호 잠금, 공개 빌드·CSP, `git diff --check`, 전체 `npm run check:quality` 통과.
- 미완료: 로컬 주소가 앱 브라우저·Chrome 정책에 차단돼 역할별 실화면 육안 검수는 완료하지 못했다. DB 마이그레이션 적용·push·Production 배포는 하지 않았다.

### 2026-07-30 업무 일정 드래그 이동·확인 저장

- 상태: 구현·자동 검증·Production 배포 완료 / 실계정 이동 확인 대기
- 요청: 업무 카드를 길게 누르고 다른 날짜에 놓으면 즉시 이동해 보이되, 일정 변경 팝업에서 최종 확인 후 저장한다.
- 구현: 데스크톱 HTML Drag and Drop과 모바일·펜 320ms long-press pointer 이동을 함께 지원한다. 이동 대상 날짜를 강조하고 새 날짜로 낙관적 렌더링한 뒤 `기존 일정 → 변경 일정`을 비교한다.
- 안전 계약: 확인 전에는 서버를 변경하지 않는다. 취소·ESC·바깥 클릭·저장 실패는 원래 날짜로 복구한다. 확인 시 시작 시각과 종료 간격, 종일 여부, 상태·우선순위·담당자·내부 메모·광고주 공개값을 보존한다.
- 역할 계약: 이동 저장은 기존 업무 수정 권한과 동일한 총관리자·운영팀만 사용한다. 광고주는 공개 일정 읽기만 유지하며 비공개 업무·내부 메모는 전달하지 않는다.
- 보존 범위: `client.html`, Supabase 스키마·운영 데이터, 보호된 키워드 조회·SEO·N 상품·N 30일·N 플레이스 30일 순위 기능은 변경하지 않는다.
- 검증: 업무 6/6, 전체 API·서버 235/235, 플레이스 51/51, 서버 계약 29/29, Production 인증 18/18, 역할 5상태, 기준선, 서버 문법, 보호 잠금 21함수·23파일·11마이그레이션, 공개 빌드 9파일·인라인 6개·CSP 해시 4개와 전체 릴리스 검사를 로컬·Vercel에서 통과했다.
- 배포: 코드 `d1e08f3`을 GitHub `main`과 Vercel Production `dpl_HQ6xoQJua3Cg5B46CvhM5d1RakuV`에 반영했다. 운영 `/health`·`/ready`는 릴리스 `d1e08f3607db`·서울 `icn1`·Supabase ready, 관리자·광고주 HTML 200, 비인증 업무 API 401이며 관리자 배포본의 드래그·날짜 대상·확인 팝업 마커와 광고주 읽기 전용 분리를 확인했다.

### 2026-07-30 업무 등록 팝업·날짜 칸 전체 클릭 개선

- 상태: 구현·1차/2차 검증·Production 배포 완료 / 실계정 사용감 확인 대기
- 요청: 업무 등록 팝업을 현재 프리미엄 톤앤매너로 정리하고 날짜 숫자뿐 아니라 캘린더 네모의 빈 영역을 눌러도 해당 날짜의 일정 등록창을 연다.
- 구현: 팝업의 입력·선택·메모·공개 스위치·헤더·하단 버튼을 딥네이비 디자인 토큰으로 통일했다. 입력부만 스크롤하고 헤더와 취소·저장 영역은 고정해 낮은 화면과 모바일에서도 주요 행동이 항상 보인다.
- UX 보완: 신규 등록에서 CSS가 `hidden` 속성을 덮어 `삭제`가 보이던 결함을 차단했다. 일정 수정에서만 삭제를 표시한다. 날짜 셀 전체에 클릭 포인터와 가벼운 `+` hover 안내를 적용한다.
- 클릭 계약: 날짜 숫자는 기존처럼 등록하며 셀의 빈 영역도 `data-work-cell-date`로 같은 등록창을 연다. 일정 카드 클릭은 기존 수정, 카드 드래그는 기존 날짜 이동으로 우선 처리해 충돌하지 않는다.
- 보존 범위: 업무 API·DB·운영 데이터, 광고주 읽기 전용 일정, 기존 드래그 이동·확인 저장, `client.html`, 키워드·SEO·N 상품·N 30일·N 플레이스 30일 순위 기능은 변경하지 않는다.
- 검증: 로컬 브라우저 1280px·390×700 시각 검수, 빈 셀 `2026-07-08` 클릭 후 등록창과 `2026-07-08T09:00` 자동 입력, 신규 삭제 버튼 비노출을 확인했다. 전체 API·서버 235/235, 플레이스 51/51, 서버 계약 29/29, Production 인증 18/18, 역할 5상태, 보호 잠금 21함수·23파일·11마이그레이션, 공개 빌드·CSP와 전체 `check:quality`가 통과했다.
- 배포: 코드 `1bf6859`을 GitHub `main`과 Vercel Production `dpl_7tQpFm3W5RKY7UvYbLpxrCyJqEqz`에 반영했다. 운영 `/health`·`/ready`는 릴리스 `1bf68593e3af`·서울 `icn1`·Supabase ready, 관리자·광고주 HTML 200, 비인증 업무 API 401이다. 관리자 배포본의 날짜 셀·팝업·공개 스위치·신규 삭제 숨김 마커와 광고주 읽기 전용 분리를 확인했다.

### 2026-07-30 온열찜질기 오원부 3위 차단·배포

- 상태: 수정·이력 복구·Production 배포·새 코드 실갱신 완료
- 증상: 상품 `12149720593`, 키워드 `온열찜질기`의 입력 상품이 아닌 카탈로그 `59031763223`을 관련 원부로 재사용해 현재 3위·원부로 잘못 기록했다.
- 원인: 네이버 상품유형 `2`는 원부에 묶이지 않은 단일상품인데도 동일 제조사·카테고리·키워드 조건만으로 다른 카탈로그를 관련 원부로 채택했고, 이후 스냅샷의 카탈로그 ID를 강한 검증 없이 연속성 값으로 재사용했다.
- 수정 원칙: 원부 미매칭 단일상품에는 관련 원부를 연결하지 않는다. 과거 연속성은 최초 직접 근거와 상품유형 안전 조건을 모두 만족할 때만 재사용하고, `prior_verified_catalog_id` 자체는 검증 근거로 인정하지 않는다.
- 데이터 복구: 해당 추적기의 잘못 연결된 원부 기록은 같은 조회에서 이미 수집한 정확 상품 순위로 교정하고 현재·최고·최저 순위를 다시 계산한다.
- 보존 범위: 광고 제외 오가닉 300위 조회, 정확 상품 ID 매칭, 검증된 원부형 상품의 대표 순위, 플레이스 순위, 키워드 조회, SEO 확인, 역할별 권한은 유지한다.
- 배포 승인: 대표님이 원인 수정·회귀검사·기존 업무 운영 기능을 포함한 Production 배포를 승인했다.
- 검증: 결함 표본은 잘못된 원부 3위가 있어도 정확 상품 76위만 선택하는 회귀검사로 고정했다. 집중 31/31, 전체 API·서버 234/234, 플레이스 51/51, 서버 계약 29/29, Production 인증 18/18, 보호 잠금·역할 parity·공개 빌드와 전체 `check:release`를 통과했다.
- 데이터 복구: 오원부 `59031763223`·`58525255512`가 연결된 스냅샷 52개를 같은 조회에서 저장된 정확 상품 순위로 교정했다. 현재는 상품ID `12149720593`, 네이버 API 상품ID `89694231298`, 상품유형 `2`, 정확 상품 76위·2페이지 36위이며 잘못된 원부 연결은 0개다.
- 배포: 코드 `fa07878`과 업무 운영 `5f73982`를 GitHub `main` 및 Vercel Production `dpl_DfTvnkrm9Fwbyuri3ie4bxm2gU15`에 반영했다. `/health`·`/ready`는 릴리스 `fa07878aef0b`, 서울 `icn1`, Supabase ready를 반환한다.
- 운영 재검증: 새 Production 코드로 2026-07-30 17:48 KST 자동 갱신을 다시 실행했다. 300개 전체 확인, 정확 상품 76위, `trackingRankSource=exact_product`, 상품유형 `2`, 관련 원부 ID·순위 없음, 오류·재시도 0건이며 GitHub Actions `30528004934`도 성공했다.

### 2026-08-02 N 쇼핑 GitHub 클라우드 자동 수집 구조

- 상태: 구조·회귀검증·GitHub 실 IP canary 완료 / GitHub 418 차단으로 자동 운영 비활성
- 목표: Mac이 꺼져도 GitHub 서버가 KST 09:00·15:00에 광고 제외 오가닉 300개를 수집한다. 300개 완주 결과만 기존 signed worker 원자 저장 계약으로 반영하고 실패·보안확인·부분 수집은 마지막 정상 순위와 이력을 보존한다.
- 구조: 무저장 수동 canary와 실제 저장 workflow를 분리했다. 실제 저장 workflow는 repository variable `MI_NAVER_SHOPPING_CLOUD_ENABLED=true`가 없으면 schedule·수동 실행 모두 skip하므로 canary 통과 전에 운영 데이터에 접근하지 않는다.
- 출처 분리: GitHub 수집 collection ID는 `gh-<run>-<attempt>-<digest>`로 남겨 기존 `pw-*` Mac 수집 이력과 구분한다. 기존 이력은 삭제·재작성하지 않고 Mac Chrome 경로는 비상용으로 유지한다.
- 보안·실패 안전: GitHub-hosted Actions·공식 Playwright Chromium·공개 네이버 쇼핑 allowlist만 허용한다. 쿠키·비밀번호 추출, CAPTCHA 풀이·우회 코드는 없으며 첫 300개 canary가 실패하면 DB claim도 시작하지 않는다.
- 현재 실증: 동일 headless 공개 경로의 로컬 canary와 GitHub-hosted Azure `eastus` canary가 모두 네이버 `naver_http_418`로 차단됐다. GitHub Actions run `30753247124`는 설치·브라우저 기동까지 성공하고 수집 단계에서만 실패했으며 DB claim·쓰기는 0건이다.
- Safari 교차검증: GitHub-hosted macOS 15 ARM의 실제 Apple SafariDriver canary run `30753696199`도 Safari 기동과 세션 생성까지 성공한 뒤 첫 검색 화면에서 `naver_access_blocked`로 중단됐다. Azure `westus` 공용 IP였으며 DB claim·쓰기는 0건이다.
- 운영 판단: Chromium·Safari 모두 GitHub 미국 공용 IP에서 차단됐으므로 브라우저 종류가 아닌 실행 IP 제약으로 판정한다. GitHub 공용 IP 수집은 반복 실행 대상에서 제외하고 `MI_NAVER_SHOPPING_CLOUD_ENABLED`를 설정하지 않는다. 유료 서버·프록시·브라우저 공급자는 임의 개설하지 않으며 기존 Mac Chrome 비상 경로와 마지막 정상값을 유지한다.
- 검증: 신규 계약 4/4, 전체 API·서버 368/368, 플레이스 51/51, 쇼핑 수집기 49/49, 공급망·순위 일정·보호 잠금 검사를 통과했다. 관리자·광고주 화면, Supabase 스키마·운영 데이터, 기존 순위 계산은 변경하지 않았다.

### 2026-08-09 N 쇼핑 가격비교 v1.0.11 안정화·배포

- 상태: 구현·1차/2차 검증·Production 배포·Mac 브리지 동기화 완료 / 네이버 네트워크 제한 해제 후 다음 실수집 관찰
- 요청: 크롬 업데이트 후 가격비교 순위를 최대한 안정적으로 수집하고 보안확인·접속 제한 반복을 줄인 뒤 완료 확인 후 배포한다.
- 구현: 첫 요청 30~45초, 페이지 간 45~75초, 자동 보충 30분, 한 회차 1개 키워드 순차 처리로 낮췄다. 가격비교 8페이지·광고 제외·정확 300개·원자 저장·실패 시 마지막 정상값 보존 계약은 유지한다.
- 검증: 네이티브 호스트 12/12, API·서버 393/393, 플레이스 51/51, 쇼핑 51/51, 서버 계약 37/37, Production 인증 18/18, 보호 잠금 22함수·58파일·14마이그레이션, 전체 `npm run check:release`, `git diff --check`를 통과했다. Production gate에서 공식 상단 실응답과 최근 원자 `checkedCount=300` 증거를 함께 확인했다.
- 배포: 코드 `c29b381`을 GitHub `main`과 Vercel Production `dpl_DottuAAAw2adGYwBTC1xAvK1xxps`·운영 별칭에 반영했다. 운영 `/health`·`/ready`는 릴리스 `c29b3812dd8f`, 서울 `icn1`, Supabase ready다. Production 반영 뒤 Mac 브리지를 동일 코드로 재설치했다.
- 운영 한계: 현재 네이버 가격비교 탭은 네트워크 접속 제한 상태다. 제한 중에는 재호출·부분 저장을 하지 않으며 기존 정상 순위와 30일 이력을 유지한다. 제한 해제 후 다음 순차 수집을 운영 관찰한다.

### 2026-08-09 동빈 → 개발 프로필 원격 갱신

- 상태: 구현·전체 릴리스 검증 완료 / Production 반영 승인 대기.
- 동빈 프로필의 N 상품 갱신·300위 조회가 서버 단일 wake를 만들고 `Profile 5`가 1분 주기로 신호를 확인한다.
- 신호가 있을 때만 최대 1건을 실행하며 남은 작업은 기존 30분 catch-up과 고정 일정으로 처리한다. 신호가 없으면 네이버를 열지 않는다.
- Supabase RLS·service-role 전용 RPC, signed worker `claim-wake`, 확장 `1.0.12`, 관리자·광고주 안내와 CSP·보호 잠금을 함께 갱신했다.
- `npm run check:release`, API·서버 397/397, 플레이스 51/51, 쇼핑 51/51, 서버 계약 38/38, Production 인증 18/18 통과.
- Supabase 적용·Production 배포·Mac 브리지/확장 재설치·운영 실클릭은 승인 후 진행한다.

### 2026-08-09 N 쇼핑 접속 제한 자동 복구

- 네이버 네트워크 접속 제한·HTTP 418·429는 미완주 저장 없이 즉시 중단하고 2시간·6시간·12시간·24시간 단계형 대기 후 1건만 자동 재시도한다.
- 대기 중에는 제한 탭 DOM만 확인하며 새로고침·검색 요청을 만들지 않는다. CAPTCHA·보안확인은 우회하지 않고 사용자가 완료하면 1분 내 자동 감지해 1건을 재개한다.
- 확장 `1.0.13`과 다음 재시도 시각 안내를 추가하고 Supabase·GitHub/Vercel Production·Profile 5 배포를 순서대로 완료했다.
- 운영 릴리스 `062ba5935d15`, `동빈(개발)`의 22:56:10 KST 이후 1건 자동 재시도, 기존 `동빈(Default)` 확장 비활성화를 확인했다. 제한 중 새 네이버 요청과 300위 저장은 실행하지 않았다.

### 2026-08-09 N 쇼핑 전체 순환 수집 v1.0.14

- 오전 9시·오후 3시는 광고주 안내로 유지하고 내부 30분 회차가 사이트 전체 활성 추적을 멱등 등록한 뒤 oldest-first로 키워드 1개씩 계속 처리하도록 분리했다.
- 운영 읽기 기준 활성 59건·고유 키워드 47개·due 키워드 47개·활성 lease 0건이다. 오류가 없을 때 약 23.5시간에 한 바퀴이며 동시 브라우저 수집은 사용하지 않는다.
- 접속 제한·418·429의 2/6/12/24시간 보호 대기, CAPTCHA 비우회, 실패 시 마지막 정상값·30일 이력 보존과 회복 후 1건 재개 계약은 유지한다.
- native host·local worker·signed handler 50/50, API·서버 397/397, 플레이스·쇼핑 각 51/51, 서버 계약 38/38, Production 인증 18/18, 보호 잠금·공개 빌드·CSP와 전체 `npm run check:release`, `git diff --check`를 통과했다.
- 코드 `49bcd6e`를 GitHub/Production에 반영하고 운영 릴리스 `49bcd6e0d896`·서울 `icn1`·Supabase ready·양 화면 200을 확인했다. Mac 런타임 14/14 파일과 `Profile 5` scheduler, 고정 확장 ID allowlist, 소스 manifest 1.0.14도 동기화했다.
- Chrome 보안 정책상 `chrome://extensions` 원격 재로드는 차단됐다. 강제 종료나 우회 없이 사용자가 `동빈(개발)`에서 재로드를 한 번 누른 뒤 로드 버전 1.0.14와 제한 해제 후 신규 원자 300개·다음 oldest-first 재개를 확인한다.

### 2026-08-09 N 쇼핑 당일 전체 순환·복구 v1.0.15

- 요청: 네트워크 제한 오류가 하루 전체 수집을 날리지 않도록 24시간 보호 대기를 제거하고, 전용 데스크탑에서 하루 안에 전체 순위를 계속 추적할 수 있게 한다.
- 구현: 내부 전체 순환을 30분에서 20분으로 단축해 고유 키워드 47개를 오류가 없을 때 약 15시간 40분에 처리한다. 네트워크 제한·418·429는 `30분 → 60분 → 120분` 뒤 1건만 재개하고 이후에도 120분을 상한으로 유지한다.
- 보존: 순차 1건, 멱등 대기열, oldest-first, HMAC·nonce·lease, CAPTCHA 비우회, 실패 시 마지막 정상값·30일 이력 보존 계약은 변경하지 않는다.
- 완료 조건: 대상·전체 릴리스 검사, 보호 잠금 갱신, Production과 작업용 브리지 동기화, Chrome 확장 1.0.15 재로드, 신규 `pw-chrome-*`·`checked_count=300` 실증을 단계별로 확인한다.
- 검증: native host 12/12, API·서버 397/397, 플레이스·쇼핑 각 51/51, 서버 계약 38/38, Production 인증 18/18, 보호 잠금 22함수·60파일·15마이그레이션과 전체 `npm run check:release`를 통과했다.
- 배포: 코드 `16a0488`, Production `dpl_J23S2KoAt34TbC4gafyxneBdADjG`, 운영 릴리스 `16a04882ff83`·서울 `icn1`·Supabase ready·양 화면 200을 확인했다. Mac 브리지는 저장소와 14/14 파일 일치, `Profile 5`, LaunchAgent exit 0, 확장 소스 1.0.15다.
- 남은 실증: Chrome 보안 정책상 서비스 워커 재로드는 사용자가 `동빈(개발)`의 확장 관리에서 한 번 눌러야 한다. 이후 로드 1.0.15와 첫 신규 원자 300개를 확인한다.
