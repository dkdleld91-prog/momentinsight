# 모먼트 인사이트 개발 폴더 가이드

이 폴더가 개발·검증·배포에 사용하는 단일 프로젝트입니다.

## 작업을 다시 시작할 때 보는 순서

1. `docs/WORK_STATUS.md`: 현재 운영 상태와 마지막 배포
2. `docs/NEXT_ACTIONS.md`: 지금 이어서 할 일
3. `docs/08-work-spec-autosave.md`: 전체 작업명세와 오토세이브
4. `docs/TEST_EVIDENCE.md`: 테스트·배포 증거
5. `docs/README.md`: 나머지 문서 위치

완료되거나 교체된 과거 실행 계획은 `docs/archive`에 보관합니다. 현재 작업을 시작할 때는 보관 문서를 지시로 해석하지 않습니다.

초기 기획 자료는 `docs/planning`에 분리되어 있습니다. 현재 상태를 확인할 때는 기획 문서부터 읽지 않습니다.

## 실제 운영 파일

| 목적 | 경로 |
| --- | --- |
| 홈페이지 | `src/pages/home.html` |
| 운영팀·총관리자 화면 | `src/pages/admin.html` |
| 광고주 화면 | `src/pages/client.html` |
| 공통 SEO 점수 엔진 | `public/seo-evaluation.js` |
| 서버 API | `src/server` |
| N 쇼핑 공통 서버 경로 | `src/server/naver-shopping` |
| Vercel 진입점 | `api` |
| DB 기준 | `supabase/migrations` |
| 플레이스 수집기 | `tools/naver-place-rank-collector` |
| N 쇼핑 수집기 | `tools/naver-shopping-rank-collector` |
| 운영 엑셀 양식 | `03_운영시트_템플릿` |
| 네이버 연동 안내 | `05_네이버_API_연동` |
| Supabase 점검 도구 | `06_Supabase_연동` |
| 배포 설정 | `vercel.json`, `.github/workflows` |

## 보존해야 하는 로컬 영역

- `.git`: Git 이력
- `.env.local`, 하위 폴더의 `.env.local`: 실제 비밀 환경변수
- `.vercel/project.json`, `.vercel/.env.production.local`: Vercel 연결·로컬 환경
- `node_modules`, `tools/naver-place-rank-collector/node_modules`: 현재 테스트와 빌드 의존성

비밀값 파일은 삭제하거나 Git에 추가하지 않습니다.

## 삭제해도 다시 만들어지는 영역

- `dist`
- `.vercel/output`
- `03_운영시트_템플릿/outputs`
- `supabase/.temp`
- `.DS_Store`
- `*.inspect.ndjson`

정리 전 확인:

```bash
npm run clean:workspace:dry
```

안전 정리:

```bash
npm run clean:workspace
```

루트 `node_modules`까지 지우는 `npm run clean:workspace:deps`는 의존성을 다시 설치할 때만 사용합니다.

## 고정 운영 원칙

- 화면 수정은 `src/pages`, 서버 수정은 `src/server`, DB 수정은 `supabase/migrations`에서만 합니다.
- N 쇼핑 설정·준비상태·장애 판정은 `src/server/naver-shopping`, 실제 수집기는 `tools/naver-shopping-rank-collector`에서 관리합니다.
- Supabase는 DB·인증·Storage로 사용하고, 운영 API는 `src/server`의 Vercel 백엔드 한 경로만 사용합니다.
- 작업 전 `npm run work:autosave`를 실행합니다.
- `src/pages/admin.html` 또는 `src/pages/client.html`을 수정하면 두 화면을 함께 검증합니다.
- `dist`나 `.vercel/output`을 원본처럼 수정하지 않습니다.
- `git push`, Vercel 배포, Supabase 배포는 대표님의 현재 승인 후에만 진행합니다.
