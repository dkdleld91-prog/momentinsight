# src

향후 애플리케이션 코드가 들어갈 영역입니다.

초기 구현 권장 순서:

1. `app`에 Next.js 라우트 구성
2. `components`에 공통 레이아웃과 카드, 표, 배지 구현
3. `features/dashboard`에 광고주 대시보드 구현
4. `features/kpi`에 KPI 입력과 리포트 구현
5. `features/reports`, `features/schedule`, `features/admin` 순서로 확장

