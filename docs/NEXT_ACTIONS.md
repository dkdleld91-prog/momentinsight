# Next Actions

## 모든 다음 작업의 선행 절차

1. Git 상태·최근 커밋·작업명세·현재 상태·다음 작업을 확인한다.
2. `npm run work:autosave` 후 이번 작업 항목을 `진행 중`으로 등록한다.
3. 기존 코드·문서·테스트를 검색해 중복 구현과 변경 충돌 가능성을 확인한다.
4. 최소 범위 수정 후 대상 테스트와 다른 관점의 2차 검증을 수행한다.
5. 운영 문서, `git diff --check`, 최종 diff·상태를 확인하고 논리 단위로 커밋한다.
6. 현재 작업의 승인과 필수 검증이 모두 충족된 경우에만 push·배포한다.

## 현재 작업

- `전동칫솔`/상품 `12649811979`의 `5페이지 8위` 오표기는 공식 API 배열 순번을 실제 화면 위치로 환산한 데서 발생했음을 확정했다.
- 페이지 환산을 제거하고 모든 역할 화면을 `공식 API 결과 순번·화면 위치 미검증·상품 ID 일치` 기준으로 통일했다.
- 로컬 API 실조회와 전체 품질검사 2회를 통과했으며, 공식 API 순번을 실화면 위치로 환산하지 않는 안전 수정본의 운영 배포를 진행한다.

- 운영 홈의 가짜 집계와 예시 브랜드를 제거하고 실제 상태 화면으로 이동하는 작업 카드로 교체했다.
- 광고주 데이터 있음/없음 상태, 보고서 빈 상태, 데스크톱·390px 반응형과 메뉴 스크롤 복귀를 구현하고 검증했다.
- 전체 품질검사, Supabase 연결, 필수 환경 검사를 통과했으며 운영 데이터와 DB는 변경하지 않았다.
- 이번 UI 변경은 운영 화면에 영향을 주므로 로컬 커밋 후 배포 영향 보고 단계로 둔다.
- 판매자 상품ID와 네이버 API 상품ID를 분리하고 첫 일치 후에도 300위 범위를 완주하도록 보강했다.
- `전동칫솔` 실조회는 관련 원부 33위, 입력 URL 상품 173위, 300개 확인이며 실제 입력 상품 페이지와 동일함을 원문·브라우저로 확인했다.
- 카드 디자인은 유지했고 정확 상품 `상품 열기`는 입력한 브랜드 상품 URL로 고정했다.
- 전체 품질검사 2차, Production 운영팀·광고주 재검증과 배포를 완료했다.
- 오가닉 상품 노출 카드를 컴팩트 프리미엄 레이아웃으로 조정하고 관련 원부를 네이버 민트·그린 톤으로 변경했다.
- 상품명은 조회 키워드 검색 결과, `상품 열기`는 실제 상품 상세로 연결되도록 관리자·광고주 양쪽을 수정하고 전체 품질검사를 통과했다.
- Production에서 관리자·광고주 실조회, 데스크톱·390px 반응형, 콘솔 오류와 두 링크 목적지를 확인하고 운영 별칭 배포를 완료했다.
- 운영팀·광고주 사이드바 정보 구조와 프리미엄 스타일 통일, Production 배포, 실제 광고주 데스크톱·390px 모바일 검증을 완료했다.
- 플레이스 Actor 한도 초과 원인을 확정하고 자체 수집 폴백, 명시 순위 정렬, 부분조회 진실 표기를 구현했다.
- 코드와 전체 품질검사는 통과했지만 300개 완주 실조회 전이므로 배포 완료로 처리하지 않는다.

## 다음 우선순위

1. 공식 대체 엔드포인트가 공개되면 Developers 쇼핑 검색 API 종료 전 NAVER API HUB 전환을 별도 구현·검증한다.
2. 실제 쇼핑 화면 순위가 필요하면 네이버 접근 제한·약관·세션 차이를 감안한 별도 수집기를 설계하고 API 순번과 분리 저장한다.
3. Apify 계정 월 사용 한도를 해제하거나 새 유효 토큰을 Render에 연결한 뒤 `부평 맛집`/`2019299673`을 300개 완주로 재검증한다.

## 배포 중단 조건

- 정확 상품ID가 아닌 카탈로그 또는 다른 판매자 상품이 대상 순위로 표시됨
- 관련 원부가 정확 상품의 대상 ID 또는 현재 순위를 변경함
- 브랜드 또는 상위 카테고리가 다른 상품이 관련 원부 카드로 표시됨
- 광고 결과가 오가닉 순위에 포함됨
- 관리자·광고주 어느 한쪽에서 관련 원부·정확 상품 카드 구분이 누락됨
- 데스크톱·모바일 겹침, 잘림, 가로 스크롤 또는 콘솔 오류 발생
- 전체 품질검사나 실조회 실패
- 공식 API 결과 순번을 실제 네이버 쇼핑 화면 페이지·위치로 표시함
- `상품 ID 일치`를 실화면 노출이 검증된 `정확 상품 순위`처럼 표시함
- 상품명이 키워드 검색 결과가 아닌 상품 상세로 연결되거나 `상품 열기`가 상품 상세 이외의 주소로 연결됨
- 판매자 상품 조회가 결과 링크의 판매자 상품ID가 아닌 API `productId`만으로 일치함
- 300위 선택 조회가 정확 상품 발견 지점에서 중단되어 `checkedCount`가 300보다 작음
