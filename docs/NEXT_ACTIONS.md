# Next Actions

## 즉시

1. 최종 diff와 비밀값 포함 여부를 확인한다.
2. 검증된 변경만 커밋한다.
3. 승인된 Production 배포를 실행한다.
4. 라이브 `/health`, 네이버 상품 순위 API, 관리자·광고주 화면을 재검증한다.
5. 라이브 검증 결과를 `WORK_STATUS.md`와 `TEST_EVIDENCE.md`에 반영한다.

## 다음 우선순위

1. 순위 추적 DB의 광고주 범위와 RLS를 별도 마이그레이션으로 더 단단히 고정한다.
2. 운영팀·광고주 코드 인증을 서버 발급 단기 세션 또는 Supabase Auth로 단계 전환한다.
3. 광고 상품 위치가 필수라면 데이터 출처, 이용약관, 갱신 주기, 실패 기준을 먼저 확정하고 별도 수집기로 설계한다.

## 배포 중단 조건

- 정확 상품ID가 아닌 카탈로그 또는 다른 판매자 상품이 대상 순위로 표시됨
- 광고 결과가 오가닉 순위에 포함됨
- 관리자·광고주 어느 한쪽에서 결과 구분이 누락됨
- 데스크톱·모바일 겹침, 잘림, 가로 스크롤 또는 콘솔 오류 발생
- 전체 품질검사나 실조회 실패
