# Next Actions

## 모든 다음 작업의 선행 절차

1. Git 상태·최근 커밋·작업명세·현재 상태·다음 작업을 확인한다.
2. `npm run work:autosave` 후 이번 작업 항목을 `진행 중`으로 등록한다.
3. 기존 코드·문서·테스트를 검색해 중복 구현과 변경 충돌 가능성을 확인한다.
4. 최소 범위 수정 후 대상 테스트와 다른 관점의 2차 검증을 수행한다.
5. 운영 문서, `git diff --check`, 최종 diff·상태를 확인하고 논리 단위로 커밋한다.
6. 현재 작업의 승인과 필수 검증이 모두 충족된 경우에만 push·배포한다.

## 현재 작업

- 상품 SEO 확인은 검색 수요·경쟁을 점수에서 분리하고 상품명 키워드, 40자, 동종 카테고리, 리뷰, 상세페이지 80%, 상품정보고시 직접 작성, 할인율, 리뷰 포인트의 8개 기준으로 운영 반영을 완료했다. 자동 확인과 판매자 확인을 분리하며 미확인 항목에는 점수를 주지 않는다. SEO가 양호하고 노출 근거만 약할 때는 트래픽 부족을 확정하지 않고 `트래픽·노출 점검 필요`로 안내한다.
- 코드 `86e106e`와 Production 릴리스 `86e106e91f87`에서 운영팀·광고주 HTML 200·동일 8개 기준, `/health` live, `/ready` Supabase ready, 보호 API 비인증 401, 양 역할 로그인 화면 정상 로드를 확인했다. 다음 로그인 실사용 검수에서는 판매자 확인 0/4→4/4와 실제 상품의 자동 카테고리 비교 결과만 추가 관찰하며, 점수 계약과 4대 보호 기능은 별도 승인 없이 변경하지 않는다.

- `키워드 조회`, `N 상품 순위`, `N 30일 순위`, `N 플레이스 30일 순위`를 사이트 공통 4대 보호 기능으로 관리한다. 운영팀·광고주 진입 함수와 키워드/상품/플레이스 핵심 서버 계약은 승인 없이 변경되면 품질·릴리스가 실패해야 한다. 잠금 13함수·21파일·11마이그레이션과 모든 항목 변조 self-test를 유지하되, 신규 조회·추적 등록·갱신과 보호 범위 밖 신규 기능 개발은 계속 열어 둔다.

- N 상품 30일 전체 갱신 상태에서는 사용자에게 불필요한 내부 처리량 문구를 노출하지 않는다. 운영팀·광고주 모두 진행률 `X/Y`만 표시하되 내부 제한 동시성 2개·중첩 차단·서버 재대조·실패 재시도 계약은 유지한다. 코드 `b2955e3`·Production `b2955e3142de`에서 운영팀 실갱신 25/25 완료, 광고주 동일 25개 로드, 양 역할 문구 0건·기존 카드/이력 유지·콘솔 오류 0건을 확인했으므로 완료 상태로 유지한다.

- 키워드 종합차트 3년 범위는 Production 반영과 운영팀·광고주 최종 검수를 완료했다. 1년 12개 시점, 3년 36개 시점, 모바일 7개 축 라벨, 가로 넘침·콘솔 경고/오류 0건을 확인했다. 이후 키워드 UI 작업에서도 `keywordThreeYearTrendWorks` 기준선과 순위 기능 잠금을 함께 통과해야 한다.
- 이후 키워드·순위 관련 변경 요청은 보호 해시를 임의 갱신하지 말고 명시적 승인과 변경 사유를 먼저 기록한다. 승인된 경우에도 대상 회귀, 양 역할 parity, 전체 self-test와 `check:release`를 모두 통과한 뒤에만 새 기준을 고정한다.

- NAVER API Hub 이관 가능 범위는 Production `hub` 전환과 NCP 실호출까지 완료했다. Search Trend는 HTTP 200·30개 시계열, Shopping Insight age는 HTTP 200·153개 데이터를 반환했고 전환 릴리스 `9705862`와 운영 `/health` 200을 확인했다. Hub 읽기 요청은 네트워크·408·425·429·5xx에 한해 최대 1회만 재시도하고 401/403 등 영구 오류와 최대 대기시간은 닫힌 상태로 유지하는 안정화도 전체 릴리스 검사를 통과해 배포 대기다.
- 현재 상품 단건·N 30일·SEO가 사용하는 쇼핑 검색은 Hub로 보내지 않고 legacy 전용으로 고정했다. 공식 최신 공지 기준 2026-07-31 24:00 종료·공식 대체 API 없음이므로, Hub 전환 완료와 별개로 P0 순위 소스 교체가 필요하다. 새 소스가 실제 네이버쇼핑 오가닉 화면과 광고 제외·정확 상품ID·검증 원부ID·순번을 독립 검증하기 전에는 API 배열 순서를 화면 순위로 대체하지 않는다.

- 키워드 시장 경쟁강도는 검색수요×상품규모 비례값과 수요 대비 상품밀도를 함께 보는 모델로 재보정했고 전체 릴리스 검사를 통과했다. `써큘레이터` 표본은 `83·매우 높음`, 동일 검색량·상품 2만 표본은 `32·낮음`·판매 기회 `87·매우 높음`으로 분리된다. 사용자 배포 승인이 있으므로 로컬 커밋·Production 반영 후 운영팀·광고주 실조회에서 새 경쟁점수·판매 기회율·콘솔 오류를 최종 확인한다.

- N 상품·N 플레이스 30일 기능은 보호 잠금 상태로 운영 반영됐다. 잠금은 사용자의 신규 키워드 조회·상품/플레이스 추적 등록·갱신을 막지 않고, 다른 개발 작업이 운영팀·광고주 추적 함수, 전용 서버·크론·수집기·워크플로·순위 DB 마이그레이션을 바꿀 때 품질·릴리스만 실패시킨다. 두 기능의 소스 변경은 대표님의 명시적 승인과 잠금 해제 사유, 대상·전체 회귀 검증 후에만 새 해시를 등록한다.

- `6fd007d`는 `mml93-a01` 전용 기능 커밋이 아니라 실측 문구만 수정한 문서 커밋이다. N 상품·플레이스 30일 전체 갱신은 각 운영팀·광고주 세션 범위에서 공통 적용되고 정기 크론은 사이트 전체 대기열을 처리한다. 임의 광고주 범위와 전체 크론을 상품·플레이스 양쪽에서 자동 검증하는 회귀 보호를 추가해 전체 `check:release`를 통과했고 운영 반영을 완료했다.

- 홈페이지 서비스 안내 팝업의 순위 기능명 3개를 내부 메뉴와 같은 `N` 표기로 통일했고 전체 `check:quality`·공개 빌드·데스크톱/모바일 시각 검증 후 운영 반영을 완료했다. 기능·팝업 규격은 그대로다.

- N 상품·플레이스 30일 전체 갱신 안전 가속은 사이트 공통 기능으로 운영 반영과 2차 실측까지 완료했다. 당시 표본 계정의 상품 25건은 안전 동시성 2개·자동/수동 중첩 방지·서버 상태 재대조로 115초, 화면/DB 25/25·오류 0건이다. 플레이스 10건은 직렬 수집을 유지하되 부분 목록도 정확 Place ID가 이미 포함된 후속 항목에만 재사용해 721초, 화면 10개 완료·DB 오류 0건이다.
- 운영 버전은 Vercel `c71d565f4140`, Render `2026-07-22-safe-exact-match-cache-v20`이며 Supabase ready다. 다음 개선은 고유 키워드 자체의 네이버 목록 탐색 시간을 줄이되 정확 ID·광고 제외·네이티브 오가닉 근거·부분조회 진실성을 유지할 수 있는 공식 검증 소스가 확보된 경우에만 별도 진행한다. 현재 `부평 맛집` 77개 미발견은 오류가 아니라 이후 범위를 확인하지 못한 사실 상태로 유지한다.

- 키워드 연령별 차트의 1년 상대지수 단순 합산을 제거하고 최신 완료 월의 쇼핑 클릭 구성비로 수정·배포했다. 공식 명세·신규 4개 회귀·전체 `check:release`, 운영팀/광고주 공통 UI·CSP와 Production `a4d68f324d9b` health/ready를 확인했다.
- 다음 인증 실사용 조회에서는 `온열찜질기` 응답의 `agePeriod`, 5개 연령 비중 합계 100%, 양 역할 동일 출력과 콘솔 0건을 확인한다. 운영 전 1년 합산값은 비교 근거로만 남기고 새 기준으로 재사용하지 않는다.

- 플레이스ID `1531240094`의 의료 키워드 미추적은 네이버의 `hospital/list` 경로를 일반 `place/list?display=300`으로 덮어써 빈 목록으로 만든 수집기 결함으로 확정했다. 네이버가 생성한 의료 목록 문맥과 `display=70`을 보존하고, 비네이버 프레임 URL은 거부하며 기존 맛집 300개 확장은 유지한다.
- 로컬 실조회는 광고 제외 정확 ID 기준 `종로3가한의원` 3위·`종로한의원` 12위다. `홍대 맛집` 7위·`부평 맛집` 100개 부분 미발견 회귀와 전체 `check:release`를 통과했다. 기존 추적 행·30일 이력·양 역할 UI는 보존한 채 승인된 push·Render/Vercel 배포·운영 새 snapshot 검증을 진행한다.

- 운영팀·광고주 로그아웃 즉시 로그인 화면 복귀와 교차 세션 지연 응답·민감 입력 차단을 완료했다. 코드 `b052e85`·Production `b052e8597fb4`에서 실제 광고주 세션 자동 복원→로그아웃→새로고침 잠금 유지, 운영팀 로그인 잠금, 양 페이지 콘솔 오류 0건과 보호 API 401을 확인했다.
- 로그아웃 운영 오류는 완료 상태다. 다음 개발은 현재 코드·이력·세션 기능을 보존한 채 네이버 API Hub 이관 가능 API와 2026-07-31 쇼핑 검색 종료 대응을 별도 작업으로 진행한다.

- 플레이스 순위 숫자가 실제 네이버 PC 목록 위치와 달라진 원인을 `allSearch` 미리보기 순번 오인, 가상 목록 끝 점프에 따른 중간 행 누락, 카드 내부 프로모션 `li` 혼입, 외부 Actor 순번 신뢰로 확정하고 모두 제거했다. 실제 장소 목록 최상위 카드·정확 플레이스ID·광고 제외·네이티브 근거 표식만 운영 순위로 인정한다.
- v16은 Render cold start를 포함하는 절대 마감 시각과 1600px viewport를 사용한다. `홍대 맛집`/`1907427831`은 100개 확인·오가닉 7위, `부평 맛집`/`2019299673`은 100개 확인·미발견·rank null이다. 공개 목록이 100개에서 끝나므로 101~300위는 발명하지 않는다. 부분 조회 current null·재시도·불완전 캐시 금지와 기존 이력 보존은 유지한다.
- 본 서버가 `source=naver_map_pc_list_collector`와 `rankEvidence=naver_pc_organic_list`를 강제하므로 구버전·오염 공급자 응답은 저장되지 않는다. 전체 릴리스 검사와 독립 재검수를 통과했고 Render v16·Vercel 운영 반영 후 `홍대 맛집` 97개/7위, `부평 맛집` 77개/null 새 snapshot을 확인했다. 공개 목록 밖 78~300위는 별도 검증 소스 전까지 미검증으로 유지한다.
- 2026-07-15 네이버 메일은 공식 공지와 일치한다. 다만 쇼핑 검색 API는 API Hub 이관 대상이 아니라 2026-07-31 종료·대체 API 없음이며, Search Trend·Shopping Insight·일반 Search 일부만 Hub로 이관할 수 있다. 현재 legacy 실호출은 200이어서 이번 플레이스 장애와는 별개다.

- 플레이스 키워드 검색의 전체 오가닉 업체 리뷰 지표 집계와 월검색량 병합을 완료했다. 대상 매장이 확인 범위 밖이어도 검색된 모든 업체가 블로그·방문 합계에 포함되며, 누락값은 실제 0으로 오인되지 않게 `-`로 표시한다.
- DB 신규 컬럼이나 기존 스냅샷 재작성 없이 현재 `place.metrics` JSON 계약을 확장했다. `topPlaces`는 20개 제한을 유지해 30일 이력 조회·저장 부하를 키우지 않는다. 전체 릴리즈 검사와 두 실키워드 수집을 통과했으며 승인된 운영 배포와 새 스냅샷 확인을 진행한다.

- 상품 원부 연속 추적과 플레이스 정확 ID·상호명·좌표 처리를 수정·배포했다. 기존 상품·플레이스 추적 행과 30일 스냅샷은 삭제·초기화하지 않았다.
- 상품 두 키워드는 검증 원부 `57907660073`의 새 순위 15위·25위를 운영 DB에 저장했고, 플레이스 `2019299673`은 상호명 `팽오리농장 부평점`을 복원했다. 네이버 공개 목록 54개 이후는 429 접근 제한 때문에 미검증 상태이므로 외부 300위 수집 제공자 한도 복구 전까지 미노출로 확정하지 않는다.

- Owner 코드 생성 화면의 운영팀·광고주 CTA 정렬과 전용 프리미엄 카드 보완을 완료했다. 1280px 이상은 동일 우측 열, 1220px 이하는 한 열 전폭으로 전환하며 기존 생성 훅·권한·API는 그대로 유지한다.
- 전체 품질·Production 인증·공개 빌드와 1440·1280·1180·1024·390px 반응형 검증을 통과했으며 커밋 `e648fc6`을 운영 별칭에 반영했다. `client.html`, 순위 수집·저장·추적, 보고서, DB는 변경하지 않았다.

- 사용 중 기능 비소실 보호 커밋 `920854b`를 Vercel 릴리즈 `e648fc6c6519`와 Render 수집기 `2026-07-19-organic-selector-fallback-resilience-v10`에 반영했다.
- 운영 `/health`·`/ready`, 관리자·광고주 200, 보호 API 비인증 401, 브라우저 콘솔 오류 0건을 확인했다. 별도 후속 작업으로 상품 정확 판매자 상품이 300위 밖일 때 기존 검증 원부 ID를 이어 추적하는 연속성 결함을 수정해야 한다.

- DB에 그대로 남아 있는 상품 88개·플레이스 13개 추적을 다시 노출하는 복구를 완료했다. `session` placeholder 차단과 API 오류 시 기존 카드 보존을 양 역할 화면에 적용했다.
- 전체 `check:quality`, CSP 공개 빌드, Production 총관리자 상품 25개·플레이스 10개, 광고주 격리 범위 상품 25개·플레이스 1개와 기존 이력을 확인했다. 복구 커밋 `7459d49`는 운영 별칭에 반영됐다.

- 보안 전용 릴리즈를 운영 Production 기준 `b37ca5f` 위에서 분리 완료했다. 이메일·매직링크 목업과 월간/PPT·보고서 실험 변경은 포함하지 않는다.
- 부가세 계산기는 정확한 `mml93-a01` Owner 세션에서만 서버가 화면 조각을 제공하고 금액을 계산한다. 운영팀·광고주·익명 사용자와 공개 정적 산출물에는 계산기 구현이 존재하지 않는다.
- Supabase 보안 마이그레이션과 Vercel 민감 환경변수 등록, `check:release` 전체 검증을 마쳤다. 보안 코드 릴리즈 `cc548b2`를 Production과 운영 별칭에 반영했고 `/health`·`/ready`·권한 차단·Owner 계산·복사·로그아웃 실화면까지 재검증했다.

- 메인 기능 쇼케이스를 `Trust Standard` 위로 이동하고 `현재 데이터`·`30일 순위 추적` 두 그룹으로 분리하는 로컬 작업을 완료했다.
- 저채도 잉크 네이비로 배경을 보정하고 그룹별 모바일 가로 탐색을 적용했다. 1440×900·390×844에서 문서 가로 넘침·카드 잘림·콘솔 오류는 모두 0건이다.
- `homeFeatureShowcasePriorityAndGroups`가 섹션 순서와 그룹별 정확한 카드 구성을 검사한다. `9cdbaad`와 Production `momentinsight-8rljvs8ue-momentlabs.vercel.app`을 운영 별칭에 반영했고 운영 재검증까지 완료했다.

- 메인 홈페이지에 실제 광고주 자료를 사용하지 않는 4카드 기능 쇼케이스를 로컬 구현했다. 중복된 2개 소개 섹션을 제거하고 오가닉 상품 순위, 상품·플레이스 30일 추적, 키워드 시장 분석을 한 섹션으로 압축했다.
- 사용자 제공 스크린샷·실제 상품 이미지·고객명·상품명·키워드·ID·가격·조회 시각·외부 링크는 포함하지 않았다. 예시 데이터 고지와 개인정보 차단 릴리즈 기준선을 추가했다.
- 전체 품질검사와 1440×900·390×844 로컬·운영 육안검수를 통과했다. 운영 HTML과 로컬 `dist`의 바이트 일치, 그룹별 2카드, 가로 넘침·콘솔 오류 0건을 재확인했다.

- 메인 홈페이지 프리미엄 보완 2차를 로컬에서 완료했다. 팝업 외곽 규격·위치·노출 로직은 유지하고 문서·모바일 기반, 헤더, 히어로, 샘플 대시보드, 반복 카드, 운영 흐름, CTA, 푸터만 정돈했다.
- 전체 품질검사와 1440×900·390×844 육안검수, 팝업 닫기·1주일 숨김, 가로 넘침·콘솔 검사를 통과했다. 운영팀·광고주 화면과 순위 기능은 수정하지 않았다.
- 사용자 승인에 따라 프리미엄 보완 커밋 `530839f`를 포함한 최종 홈페이지를 Production에 반영했다. 폐기된 `0886833`은 배포 이력에 포함하지 않았다.

- `N 30일 순위`의 작은 일별 슬롯에서 정확 상품 기준의 `상품` 문구만 제거해 `PM`·`AM`으로 표시했다. 관련 원부는 `PM · 원부`·`AM · 원부`로 계속 구분한다.
- 운영팀·광고주 공통 렌더링만 최소 수정하고 크롤링·순위 조회·저장·대표값 판정은 건드리지 않았다. 런타임 출력, 전용 릴리즈 기준선, 전체 품질검사, 광고주 390px·운영팀 데스크톱 빌드와 콘솔을 검증했다.
- 승인 커밋 `368408d`만 원격 `main`과 Production `momentinsight-jly55k3zm-momentlabs.vercel.app`에 반영했다. 운영팀·광고주 로그인 실데이터 각 414개 슬롯에서 정확 상품의 `상품` 문구 0건, 원부 구분 유지, 콘솔 오류 0건과 운영 파일의 승인 SHA 일치를 확인했다. 거절된 홈페이지 커밋 `0886833`은 로컬 이력에서 제거했다.

- `N 30일 순위`에서 선택 기준이 붙은 PM/AM 라벨만 두 줄로 접혀 순위 숫자가 아래로 밀리던 표시 오류를 운영팀·광고주 공통 CSS에서 수정했다.
- 순위 기능 코드는 변경하지 않고 일별 슬롯의 한 줄 라벨·안쪽 여백·폰트 크기만 맞췄다. 두 역할 각 207개 카드와 390px에서 숫자 기준선 차이 0px, 라벨 줄바꿈·잘림 0건을 확인했다.
- 코드 커밋 `01935d2`을 `main`에 푸시하고 Production `momentinsight-fhgibit9c-momentlabs.vercel.app`·운영 별칭에 배포했다. 운영팀·광고주 각 207개 카드의 기준선·라벨·콘솔 재검증까지 완료했다.

- 플레이스 30일 순위의 과도한 카드 높이·큰 숫자·반복 지표를 운영팀·광고주 공통 컴팩트 프리미엄 UI로 재구성했다.
- 일별 140px 카드와 2×2 보조지표, 낮은 그림자, 딥네이비 현재 순위 계층을 적용하고 모바일 상태 배지는 한 줄 가로 탐색으로 압축했다. 기존 조회·갱신·삭제·그룹·공유·30일 데이터는 보존했다.
- 데스크톱·390px 양 역할 실화면에서 문서 가로 넘침·긴 수치 잘림·콘솔 경고/오류 0건을 확인했고 전체 품질검사를 독립 2회 통과했다.
- 코드 커밋 `7710008`을 `main`에 푸시하고 Production `momentinsight-m19imug8x-momentlabs.vercel.app`·운영 별칭에 배포했다. 운영 `/health`와 양 역할 데스크톱·390px·콘솔 재검증까지 완료했다.

- `써큘레이터`/판매자 상품ID `11687310806`의 원부 제목에 키워드가 빠져 관련 원부가 누락된 원인을 모델번호 기반 관계 판정으로 보강했다.
- 모델번호 완전 일치와 브랜드·상위 카테고리 일치를 함께 요구하고, 다른 모델 접두 유사값은 원부 후보에서 제외한다. 정확 상품 ID·판매자 링크 일치와 광고 제외 규칙은 변경하지 않는다.
- 로컬 API·운영팀·광고주 화면은 원부 8위·정확 상품 59위·300개 확인으로 일치했고, 전체 품질검사 2회·환경·Supabase·390px·콘솔 검증을 통과했다.
- 코드 커밋 `d9b97ca`를 `main`에 푸시하고 Production `momentinsight-cjx4bkodl-momentlabs.vercel.app`·운영 별칭에 배포했다. 운영 API와 양 역할 화면 재검증까지 완료했다.

- 상품 순위 단건·추적·크론·스냅샷은 광고 전용 플래그와 비오가닉 삽입 타입을 순위 계산 전후에 제거하고 `rankPolicy=organic_only`를 저장한다.
- 실제 네이버 1페이지의 오가닉 40개·`supersaving` 5개 분리, Supabase 실스냅샷, 전체 품질검사 2회와 양 역할 화면 검증을 통과했다. Production 배포 후 운영 API·추적·Supabase 저장 근거까지 재검증했다.
- 단건·30일 추적 모두 정확 상품과 검증된 관련 원부의 오가닉 순위 중 숫자가 더 낮은 쪽을 대표 순위·상품 형태로 사용한다.
- 관리자·광고주 화면은 대표 순위를 `N위`, `N페이지 N위`로 표시하고, 원부·정확 상품 카드를 모두 보존한다.
- `전동칫솔`/`12649811979`은 로그인 네이버 가격비교 실화면과 로컬 API에서 원부 34위(1페이지 34위), 정확 상품 168위(5페이지 8위)로 일치했다.
- 전체 품질검사 2회와 관리자·광고주 실조회·모바일 육안검수를 통과했다. 커밋 `6424f58`을 `main`에 푸시하고 Production 배포·운영 재검증까지 완료했다.

- 운영 홈의 가짜 집계와 예시 브랜드를 제거하고 실제 상태 화면으로 이동하는 작업 카드로 교체했다.
- 광고주 데이터 있음/없음 상태, 보고서 빈 상태, 데스크톱·390px 반응형과 메뉴 스크롤 복귀를 구현하고 검증했다.
- 전체 품질검사, Supabase 연결, 필수 환경 검사를 통과했으며 운영 데이터와 DB는 변경하지 않았다.
- 이번 UI 변경은 운영 화면에 영향을 주므로 로컬 커밋 후 배포 영향 보고 단계로 둔다.
- 판매자 상품ID와 네이버 API 상품ID를 분리하고 첫 일치 후에도 300위 범위를 완주하도록 보강했다.
- `전동칫솔` 실조회는 관련 원부 33위, 입력 URL 상품 173위, 300개 확인이며 실제 입력 상품 페이지와 동일함을 원문·브라우저로 확인했다.
- 카드 디자인은 유지했고 정확 상품 `상품 열기`는 입력한 브랜드 상품 URL로 고정했다.
- 전체 품질검사 2차, Production 운영팀·광고주 재검증과 배포를 완료했다.
- 오가닉 상품 노출 카드를 컴팩트 프리미엄 레이아웃으로 조정하고 관련 원부를 네이버 민트·그린 톤으로 변경했다.
- 상품명은 조회 키워드 검색 결과, `상품 열기`는 실제 상품 상세로 연결되도록 관리자·광고주 양쪽을 수정하고 전체 품질검사를 통과했다.
- Production에서 관리자·광고주 실조회, 데스크톱·390px 반응형, 콘솔 오류와 두 링크 목적지를 확인하고 운영 별칭 배포를 완료했다.
- 운영팀·광고주 사이드바 정보 구조와 프리미엄 스타일 통일, Production 배포, 실제 광고주 데스크톱·390px 모바일 검증을 완료했다.
- 플레이스 Actor 한도 초과 원인을 확정하고 자체 수집 폴백, 명시 순위 정렬, 부분조회 진실 표기를 구현했다.
- 코드와 전체 품질검사는 통과했지만 300개 완주 실조회 전이므로 배포 완료로 처리하지 않는다.

## 다음 우선순위

1. P0: 2026-07-31 쇼핑 검색 API 종료 전에 실제 네이버쇼핑 화면 수집기 또는 검증 가능한 라이선스 공급자로 상품 단건·N 30일·SEO 순위 소스를 교체한다. 광고 제외·정확 상품ID·검증 원부ID·화면 순번을 독립 검증하고, 새 소스가 준비될 때까지 마지막 정상 기록을 보존하며 API 배열 순서를 화면 순위로 표기하지 않는다.
2. P0: Search Trend·Shopping Insight·blog/local용 legacy/Hub 분리 어댑터와 오류 분류는 구현·검증했다. NCP Hub 앱·구독·별도 키를 준비하고 Production legacy 고정 상태에서 Preview hub 실호출·표본 비교 후 전환한다. Shopping Insight 카테고리 ID는 종료되는 쇼핑 검색 결과에서 분리해 명시 저장한다.
3. 플레이스 101~300위가 사업상 필요하면 실제 네이버 오가닉 목록과 동일함을 증명하는 별도 공급자를 검토한다. Apify 배열은 진단용으로만 유지하고 `naver_pc_organic_list` 근거가 없는 순위는 운영 스냅샷에 저장하지 않는다.

## 배포 중단 조건

- 정확 상품ID가 아닌 카탈로그 또는 다른 판매자 상품이 대상 순위로 표시됨
- 브랜드·카테고리가 다른 원부가 대표 순위로 선택되거나 대상 상품ID가 덮어써짐
- 브랜드 또는 상위 카테고리가 다른 상품이 관련 원부 카드로 표시됨
- 광고 결과가 오가닉 순위에 포함됨
- 관리자·광고주 어느 한쪽에서 관련 원부·정확 상품 카드 구분이 누락됨
- 데스크톱·모바일 겹침, 잘림, 가로 스크롤 또는 콘솔 오류 발생
- 전체 품질검사나 실조회 실패
- 대표 순위가 원부·정확 상품 중 숫자가 더 낮은 오가닉 순위와 다름
- 40개 보기 페이지 계산이 `ceil(rank/40)` 및 페이지 내 `((rank-1)%40)+1`과 다름
- 상품명이 키워드 검색 결과가 아닌 상품 상세로 연결되거나 `상품 열기`가 상품 상세 이외의 주소로 연결됨
- 판매자 상품 조회가 결과 링크의 판매자 상품ID가 아닌 API `productId`만으로 일치함
- 300위 선택 조회가 정확 상품 발견 지점에서 중단되어 `checkedCount`가 300보다 작음
- 플레이스 외부 응답에 네이티브 PC 오가닉 source·rankEvidence가 없거나 서로 불일치함

## 즉시 확인: 의료 플레이스 v18

1. 완료: Render `/health`가 `2026-07-22-native-medical-list-v18`을 반환한다.
2. 완료: `종로3가한의원`·`종로한의원` 두 tracker의 정확 플레이스 ID `1531240094` 새 snapshot을 확인했다.
3. 완료: 두 snapshot은 `source=naver_map_pc_list_collector`, `matched=true`, 70개 확인이며 서버의 네이티브 오가닉 근거 검증을 통과했다.
4. 실패 시 기존 순위나 유사 상호로 대체하지 않고 정확 오류를 남긴다.

## 키워드 경쟁강도 배포 후 확인

1. 완료: Production `써큘레이터` 실조회에서 검색 수요 97, 경쟁강도 83 `매우 높음`, 판매 기회율 69 `높음`을 확인했다.
2. 완료: 같은 화면의 조회 결과와 운영 판단 문구도 `매우 높음` 및 대표 포화 키워드 안내로 일치한다.
3. 완료: 운영팀·광고주 정적 역할 동등성, `/health`, `/ready`, 비인증 키워드 API 401을 확인했다.
4. 완료: 연관 키워드 표는 각 행의 검색광고 원본 경쟁도를 유지하며 종합 경쟁강도로 오인 표기하지 않는다.
